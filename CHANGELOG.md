# Changelog

Todo cambio notable a este proyecto se documenta aquí. Sigue [Keep a Changelog](https://keepachangelog.com/es-ES/1.1.0/) y [Semantic Versioning](https://semver.org/lang/es/).

## [Unreleased]

## [0.13.1] - 2026-07-15

### Fixed

- **Robustecer la Validacion 1.5 de `/mefisto-sequential` ante shells sin word-splitting (issue #274)**: el bloque bash embebido en `.claude/commands/mefisto-sequential.md` corria en el shell del invocador -- zsh en macOS -- que, a diferencia de bash, no hace word-splitting de una variable sin comillas por defecto (`SH_WORD_SPLIT` off). Los 4 loops `for X in $VAR` del bloque (sobre `$BATCH`, `$DEPS`, `$LABELS_TO_CLEAR`) iteraban una sola vez con la cadena completa en vez de una vez por elemento: al ejecutar `/mefisto-sequential 267 269 270 271 272` bajo zsh, `gh issue view "267 269 270 271 272"` fallaba con `invalid issue format` y el bloque caia en el `else`, imprimiendo `Validacion 1.5 OK` **sin haber validado ni mutado ningun label** -- falso positivo, observado dos veces en la misma sesion. **CA-1**: se envuelve el bloque completo en `bash <<'BASH' ... BASH` (heredoc *quoted*, sin reescribir los loops), mismo patron ya usado por `commands/onboard.md` para el mismo motivo. **CA-2/CA-3/CA-5**: verificado bajo zsh con mocks de `gh` -- un batch multi-issue ya no dispara `invalid issue format` y procesa issue por issue; los casos canonicos de lanzamiento (`44 43 45`) y aborto (`43 44`, "Mueve #44 antes de #43") se preservan identicos a como corrian bajo bash. **CA-4**: auditoria confirmada -- ninguna otra instancia desprotegida del patron `for <var> in $<VAR>` fuera de un heredoc bash en `.claude/commands/` ni `commands/` (las de `onboard.md` ya estaban protegidas). Archivo modificado: `.claude/commands/mefisto-sequential.md`.

## [0.13.0] - 2026-07-15

### Added

- **ADR-0026: colas de Service Bus con sesion para fan-in y serializacion por clave de aggregate (issue #269)**: draft cross-repo del consumidor `Cosmos-SincoERP/Cosmos.ControlPlane` (@luisfelipediaz) -- el harness solo modelaba topic-por-evento (ADR-0001), fan-out puro; no cubria el fan-in con estado de una saga/process manager, donde varios eventos convergen en una decision sobre el mismo aggregate y el manejo concurrente rompe la concurrencia optimista de Marten (dead-letter ciego). Es el issue fundacional de un epico de 4 (bloquea a #270 infra, #271 endpoint y #272 guia del productor). **CA-1**: nuevo `docs/adr/0026-colas-sesion-fan-in-serializacion-por-clave.md` fija la doctrina de decision topic (fan-out, default) vs queue con sesion (fan-in, excepcion deliberada y exclusivamente del lado consumidor -- el productor siempre publica a topics, ADR-0001/0023), la topologia (subscripciones sin sesion que hacen auto-forward a un unico queue con `requires_session = true`, restriccion de Azure verificada: una entidad con sesion no puede ser fuente de auto-forward) y el efecto sobre ADR-0004 (elimina la clase de error de concurrencia que hoy termina en dead-letter ciego, sin alterar la doctrina de eventos de fallo vs excepciones). **CA-2**: se cross-referencian, sin contradiccion, ADR-0001 (fan-out complementado, no reemplazado), ADR-0006 (excepcion de naming), ADR-0023 (namespace interno del BC sin cambio de topologia) y ADR-0024 (`groupId` de `IPrivateEventSender` como mecanismo del `SessionId`); ADR-0026 referencia ademas ADR-0004. **CA-3**: se fija la invariante "queue kebab-case = nombre de la Function que lo consume" y se documenta, en ADR-0006, la desviacion del patron `{Accion}Cuando{Evento}` para Functions de fan-in (no hay estimulo unico; convergen N eventos). Todas las afirmaciones tecnicas (restriccion de auto-forward sobre entidad con sesion, preservacion del `SessionId` a traves del forward, mapeo AMQP `group-id` -> `SessionId`, `IsSessionsEnabled` del trigger de Functions, `maxConcurrentSessions`) se verificaron contra Microsoft Learn. El indice tematico de `CLAUDE.md` suma la fila de ADR-0026. Archivo nuevo: `docs/adr/0026-colas-sesion-fan-in-serializacion-por-clave.md`. Archivos modificados: `docs/adr/0001-service-bus-topics-por-evento.md`, `docs/adr/0006-convenciones-nombramiento-funciones-azure.md`, `docs/adr/0023-bounded-context-topologia-asb.md`, `docs/adr/0024-modelo-eventos-bus-privado-publico.md`, `CLAUDE.md`.

- **La capa de infra aprende a generar y validar queues de Service Bus con sesion + forward, doctrina ADR-0026 (issue #270)**: el modulo `service-bus` de `infra-base-scaffolder` solo generaba topics + subscriptions via `topics_config` -- no existia `azurerm_servicebus_queue`, `requires_session` ni `forward_to`; `infra-writer` e `infra-reviewer` tampoco conocian la primitiva de fan-in que ADR-0026 fijo como doctrina. **CA-1**: el modulo `service-bus` (`agents/infra-base-scaffolder.md`) suma la variable `queues_config` (map de queues con `requires_session` configurable, default `false`) y el objeto de `topics_config.subscriptions` suma `forward_to` (nombre del queue destino, no su ID) -- verificado contra el provider `azurerm` (`azurerm_servicebus_queue`/`azurerm_servicebus_subscription`, Argument Reference). El objeto de subscriptions deja `requires_session` fuera a proposito: como una subscription nunca puede declarar sesion en este modulo, la restriccion de plataforma "la fuente del forward no puede tener sesion" queda satisfecha por construccion. Nuevo output `queue_ids`. **CA-2**: `infra-writer` gana la seccion "Fan-in: queues con sesion (ADR-0026)" con el criterio de cuando usar la primitiva, la convencion de naming (queue kebab-case = nombre de la Function consumidora, excepcion documentada a la tabla de nomenclatura Azure) y un ejemplo minimo de dos topics convergiendo en un queue de fan-in. **CA-3**: `infra-reviewer` suma, a la seccion "Arquitectura", la validacion de la restriccion de Azure: cualquier entidad con `forward_to != null` no puede declarar `requires_session = true` -- solo el queue destino la declara; si la encuentra, la corrige y la reporta como hallazgo. Archivos modificados: `agents/infra-base-scaffolder.md`, `agents/infra-writer.md`, `agents/infra-reviewer.md`.

- **`implementer` y `domain-scaffolder` aprenden a scaffoldear el endpoint consumidor de fan-in: queue en modo sesion + switch por Subject (issue #271, ADR-0026)**: ni `implementer.md` (seccion "Endpoint ServiceBus") ni `domain-scaffolder.md` (patron `ServiceBusEndpointBase<TEvento>`) sabian generar el trigger de queue en modo sesion que ADR-0026 fijo como doctrina de fan-in -- solo cubrian `[ServiceBusTrigger("<topic>","<subscription>", Connection=...)]` (topic+subscription, un unico `TEvento`). **CA-1**: `implementer.md` suma la seccion "Endpoint de fan-in: queue en modo sesion (ADR-0026)" con el trigger de un unico argumento posicional (nombre del queue) + `IsSessionsEnabled = true`, y fija que `Connection` es siempre `SERVICE_BUS_CONNECTION_INTERNO` (el queue de fan-in vive en el namespace interno del BC, ADR-0026 seccion 2/ADR-0023 -- sin variante de alias, a diferencia del topic+subscription). **CA-2**: cuando el queue recibe varios tipos de evento convergentes, el endpoint concreto implementa un `switch` por `message.Subject` (la propiedad AMQP "subject", verificada contra la referencia de `Azure.Messaging.ServiceBus.ServiceBusReceivedMessage.Subject`), usando el `nameof` de cada tipo de evento convergente como discriminante; un `Subject` no reconocido lanza y la clase base lo dead-letterea. Que el productor fije el `Subject` con ese nombre al emitir es una invariante del lado productor (doctrina del productor, issue #272), declarada como dependencia en `implementer.md`. **CA-3**: se aplica la invariante de ADR-0026/ADR-0006 "queue kebab-case = nombre de la Function que lo consume" (ejemplo reusado del propio ADR: queue `consolidar-cierre-turno` -> Function `ConsolidarCierreTurno`), documentada como excepcion al patron `{Accion}Cuando{Evento}` en la tabla de convenciones de nombramiento de `implementer.md`. Nueva clase `ServiceBusSessionEndpointBase` (contraparte de `ServiceBusEndpointBase<TEvento>` para este caso, con complete/lock-lost/dead-letter identico pero despacho delegado al metodo abstracto `DespacharPorSubject`) que `domain-scaffolder` scaffoldea en `Infraestructura/` (Paso 1.11d) junto con sus tests (`ServiceBusSessionEndpointBaseTests.cs`, Paso 2.7, cubriendo camino feliz, lock de sesion perdido, error generico y Subject no reconocido) y refleja en el "Resultado final". El implementer no provisiona el queue ni sus subscriptions de forward en Terraform (issue #270, paralelo e independiente): si falta, lo documenta en su resumen igual que ya hace con topics/subscriptions faltantes. Archivos modificados: `agents/implementer.md`, `agents/domain-scaffolder.md`.

- **`implementer` ensena la invariante dura `groupId` <-> `requires_session` en la guia del productor (issue #272, ADR-0026)**: cierra el epico de 4 issues -- ADR-0026 (#269) fijaba la doctrina y la seccion de fan-in de `implementer.md` (#271) ya diferia la invariante del `groupId` a "issue #272" sin que existiera todavia; los ejemplos de `CommandHandler` del propio agente publicaban con `eventSender.PublishAsync(turno.GetPrivateEvents())`, sin ensenar cuando esa llamada necesita `groupId`. **CA-1**: nueva seccion "`groupId` en `PublishAsync`: invariante dura con `requires_session` (ADR-0026)" en `agents/implementer.md`, dentro de "CommandHandler — orquestador puro": si el topic al que publica el handler alimenta, via `forward_to`, un queue con `requires_session = true` (`queues_config` de `module "service_bus_interno"`, `infra/environments/dev/main.tf`), el productor debe usar la sobrecarga de `IPrivateEventSender.PublishAsync` con `groupId` = clave del aggregate destino; documenta que un mensaje sin `SessionId` que llega via auto-forward a un destino con sesion se dead-lettera en la entidad **fuente**, no en el destino (misma referencia que ya citaba ADR-0026: Microsoft Learn, "Chaining Service Bus entities with autoforwarding"). Las tres referencias a "issue #272" que dejaba la seccion de fan-in (#271) se actualizan para apuntar a la seccion nueva, y se suma un pointer corto en "Infraestructura (topics y subscriptions)". **CA-2**: se documenta, como nota de scope y no como correccion a aplicar, que cerrar un workaround downstream que ya resuelve (o deberia resolver) este invariante de forma ad-hoc desde antes de esta doctrina -- caso real: `CreateAdminUserCommandHandler` en `Cosmos.ControlPlane` -- es tarea del **consumidor**, con su propio tracking en el backlog de ese repo; el implementer documenta el hallazgo en su resumen, no lo refactoriza como scope creep de un issue no relacionado. Se suma ademas una consideracion opcional de handoff en `agents/planner.md` (template "Modelo de eventos"): declarar en "Consumidores" cuando varios eventos convergen sobre el mismo aggregate (criterio de dos condiciones de ADR-0026), sin prescribir la topologia (fan-out vs fan-in) ni el `groupId`, que quedan a criterio del implementer/infra-writer. Archivos modificados: `agents/implementer.md`, `agents/planner.md`.

- **ADR-0027: enrutamiento multi-destinatario de un evento por correlation filter de igualdad (issue #275)**: draft cross-repo del consumidor `Cosmos-SincoERP/Cosmos.ControlPlane`, refinamiento del issue #22 de ese repo (aprovisionamiento de subscriptions al ApplicationPlane por request-reply; contrato relacionado issue #61) -- ADR-0001 razona sobre filtrar por **tipo** de evento (varios tipos en un topic de dominio) y rechaza filtros SQL para ese eje, pero no tenia doctrina para el eje distinto "un unico evento publico, N destinatarios, cada uno con su subconjunto"; sin una primitiva, la alternativa era que cada destinatario se suscriba al topic completo y descarte en el handler, moviendo el filtrado real a codigo de aplicacion invisible desde la infraestructura. Es el issue fundacional de un epico de 3: bloquea a (b) infraestructura y (c) guia del productor. **CA-1/CA-2**: nuevo `docs/adr/0027-enrutamiento-multidestinatario-correlation-filter.md` fija la doctrina -- un topic + N subscriptions, cada una con un **correlation filter de igualdad exacta** sobre una **clave de enrutamiento** (application property nombrada por el flujo, sin hardcodear `bundleId` ni ningun nombre concreto) -- preservando intacto el rechazo de filtros SQL de ADR-0001 (el correlation filter es estrictamente de igualdad; cualquier condicion que exija mas se sigue rechazando sin excepcion). **CA-3**: se aclara que el filtro es construccion exclusiva del lado consumidor -- el productor sigue publicando unicamente a topics (ADR-0001/ADR-0024), con la unica obligacion nueva de estampar la clave de enrutamiento en el sobre del mensaje (los filtros no evaluan el body); se referencia la advertencia de ADR-0026 de no conflacionar esta clave con `SessionId` (`groupId` de `IPrivateEventSender`, reservado para la serializacion de fan-in). **CA-4**: ADR-0001 queda enmendado (parrafo nuevo en "Decision", ajuste de la consecuencia "Sin filtros SQL" y referencia cruzada) para acotar su absoluto sin eliminar la doctrina vigente, y se reconcilia en el cuerpo el escape-hatch `filter_type = "SqlFilter"` que el modulo `service-bus` de `infra-base-scaffolder` ya expone hoy para `sub.filter` -- unico mecanismo de filtro del modulo, cuyo ajuste hacia `CorrelationFilter` queda diferido al issue (b) de infraestructura. Las afirmaciones tecnicas (soporte de `azurerm_servicebus_subscription_rule` para `filter_type = "CorrelationFilter"` + bloque `correlation_filter.properties` desde el provider 2.30.0, con al menos una propiedad requerida; recomendacion de Microsoft Learn de preferir correlation filters por eficiencia; los filtros no evaluan el body del mensaje) se verificaron contra Microsoft Learn y el Terraform Registry. El indice tematico de `CLAUDE.md` suma la fila de ADR-0027. Archivo nuevo: `docs/adr/0027-enrutamiento-multidestinatario-correlation-filter.md`. Archivos modificados: `docs/adr/0001-service-bus-topics-por-evento.md`, `CLAUDE.md`.

- **La capa de infra aprende a generar y validar subscriptions con correlation filter de igualdad, doctrina ADR-0027 (issue #277)**: el modulo `service-bus` de `infra-base-scaffolder` solo generaba `filter_type = "SqlFilter"` via el campo `sub.filter` -- unico uso en el repo, con los ejemplos pasando siempre `filter = null` -- sin conocer `filter_type = "CorrelationFilter"`; `infra-writer` no conocia filtros y `infra-reviewer` solo validaba el SKU del namespace (Standard/Premium). **CA-1**: el modulo `service-bus` (`agents/infra-base-scaffolder.md`) reemplaza el campo `sub.filter` (string, SqlFilter) por `sub.correlation_filter` (`map(string)`, >=1 property exigida por una `validation` nueva de `topics_config`) y suma el recurso `azurerm_servicebus_subscription_rule.correlation_filters` con `filter_type = "CorrelationFilter"` + bloque `correlation_filter { properties = ... }` -- verificado contra el provider `azurerm` (soporte desde la version 2.30.0, >=1 property exigida por el propio provider). **CA-2**: `infra-writer` gana la seccion "Enrutamiento multi-destinatario: correlation filter de igualdad (ADR-0027)" con el criterio de uso (un evento, N destinatarios, cada uno con su subconjunto), la advertencia de no conflacionar la clave de enrutamiento con `SessionId`/`groupId` (ADR-0026), la regla de en que namespace viven las N subscriptions (el modulo solo modela namespaces que el BC posee: el del productor en su namespace de integracion / patron Open Host de ADR-0024; el backbone compartido se provisiona fuera del modulo y cada consumidor agrega su propia subscription, ADR-0001; el namespace interno del BC es privado, ADR-0023) y un ejemplo minimo de un topic con dos subscriptions filtradas por igualdad en el namespace de integracion del productor. **CA-3**: `infra-reviewer` suma, a la seccion "Arquitectura", la validacion de que todo `azurerm_servicebus_subscription_rule` con `filter_type = "CorrelationFilter"` declare >=1 property y senala como hallazgo de arquitectura cualquier `filter_type = "SqlFilter"` residual, corrigiendolo a `CorrelationFilter` si el filtro real era de igualdad o eliminandolo si exigia algo distinto. **CA-4**: se resuelve la tension del escape-hatch `SqlFilter` que ADR-0027 dejaba latente en el modulo -- se **removio** en vez de documentarlo como prohibido, porque ADR-0001 rechaza los filtros SQL sin excepcion y ADR-0027 cubre por completo el unico eje legitimo (enrutamiento por destinatario) con el correlation filter de igualdad, sin dejar ningun caso que justifique conservarlo. Archivos modificados: `agents/infra-base-scaffolder.md`, `agents/infra-writer.md`, `agents/infra-reviewer.md`.

- **`implementer` documenta el estampado de la clave de enrutamiento como application property y el limite de paquete que lo diferia (issue #278, ADR-0027)**: cierra el epico de 3 issues -- ADR-0027 (#275) fijaba la doctrina y la capa de infra ya aprendio a generar y validar subscriptions con correlation filter (#277), pero `agents/implementer.md` no ensenaba al productor cuando ni como estampar la clave de enrutamiento que esos filtros matchean; el wrapper de publicacion (`IPublicEventSender.PublishAsync`/`IPrivateEventSender.PublishAsync`) solo expone `PublishAsync(events)` y `PublishAsync(groupId, events)`, sin sobrecarga para application properties arbitrarias. **CA-1**: nueva seccion "Clave de enrutamiento como application property (ADR-0027)" en `agents/implementer.md`, junto a la seccion de `groupId`/ADR-0026: documenta el CUANDO (un `IPublicEvent` con N destinatarios, cada uno con su propio correlation filter) y el COMO (application property string, nombrada por el flujo, matcheada por igualdad exacta -- sin hardcodear `bundleId` ni ningun nombre concreto), coherente con la subscription que provisiona el consumidor. **CA-2**: se documenta el LIMITE verificado -- el paquete `Cosmos.EventDriven.CritterStack.AzureServiceBus`/`Cosmos.EventDriven.Abstractions` (1.3.0) no expone hoy ninguna sobrecarga para estampar application properties arbitrarias; la capacidad queda diferida como trabajo downstream (cross-repo), mismo patron de limite de paquete que Alt 4 de ADR-0024 (managed identity diferida). **CA-3**: se documenta explicitamente no reusar `groupId`/`SessionId` (reservado a la serializacion de fan-in, ADR-0026) como clave de enrutamiento -- son propiedades ortogonales, con proposito distinto, que un flujo podria necesitar a la vez sin compartir la misma propiedad. Archivo modificado: `agents/implementer.md`.

### Fixed

- **Anadir guard `[ -f ]` al `sed` de `settings.json` en `tdd-pipeline.sh` e `iac-pipeline.sh` (issue #267)**: draft reportado desde un consumidor por Luis Felipe Diaz -- al lanzar `/implement` (o `/infra`) en un consumidor **sin** `.claude/settings.json` (legitimo desde que los hooks de eventos vienen globales via `hooks/hooks.json`), el pipeline abortaba en el setup del worktree, antes de invocar cualquier agente: `sed` fallaba con `No such file or directory` y, bajo `set -euo pipefail`, tumbaba el script entero, dejando ademas un `settings.json` de 0 bytes en el worktree (el redirect `>` se ejecuta aunque `sed` falle al leer el input). `scaffold-pipeline.sh` y `pr-sync.sh` ya envolvian su `sed` equivalente en `if [ -f ... ]; then ... fi`; `tdd-pipeline.sh` (L366-368) e `iac-pipeline.sh` (L260-262) no. **CA-1/CA-2**: se envuelve el `sed` que parchea `settings.json` del worktree con el mismo guard `if [ -f "$REPO_ROOT/.claude/settings.json" ]; then ... fi` en ambos scripts. **CA-3/CA-5**: sin el archivo fuente, el bloque se saltea entero -- no se invoca `sed` ni se crea un `settings.json` vacio, y el pipeline continua hasta invocar test-writer/implementer (o el equivalente de IaC). **CA-4**: con el archivo fuente presente, el comportamiento no cambia -- el worktree sigue recibiendo el `settings.json` parcheado con la ruta absoluta del `events.log`. Se difiere unificar las 4 copias de este patron en un helper de `_pipeline-common.sh` (divergen en la variable de ruta y el destino) para un tercer issue si el patron vuelve a divergir (Rule of Three, ADR-0018). Archivos modificados: `scripts/tdd-pipeline.sh`, `scripts/iac-pipeline.sh`.

## [0.12.0] - 2026-07-10

### Added

- **Skill `/seed-secret` para sembrar secretos nuevos post-greenfield, via un registro declarativo `secrets[]` (issue #256)**: la siembra del Key Vault era correcta (ADR-0025) pero no evolvable -- `infra-cd.yml` nunca se regenera una vez creado (regla 10), y su step de siembra tenia una linea `az keyvault secret set` **hardcodeada por secreto** (tres fijos + un `env`/linea por alias de `serviceBus.external[]`), asi que agregar un secreto arbitrario nuevo exigia editar ese YAML a mano ("Caveat de evolvabilidad" en `README.md` y `agents/infra-base-scaffolder.md`). **CA-1**: `harness.config.json` gana el array opcional `secrets[]` -- cada entrada declara `name` (el secreto en Key Vault) y `source.type`/`source.value` (`output` para un `terraform output` derivable, `github-secret` para un GitHub secret no derivable, o `composite` -- vocabulario cerrado, reservado a la formula fija de `marten-connection`, que solo escribe `infra-base-scaffolder`). `load_harness_config` (`scripts/_pipeline-common.sh`) valida la forma del array (`name` unico, `source.type` en el vocabulario cerrado, `source.value` no vacio) y exporta `HARNESS_SECRETS_NAMES`/`_TYPES`/`_VALUES`; nuevo helper `upsert_harness_secret` inserta/actualiza una entrada de forma idempotente (por `name`), reusado tanto por `infra-base-scaffolder` como por el skill nuevo. **CA-2/CA-3**: el step "Sembrar los secretos del Key Vault" de `infra-cd.yml` (generado por `infra-base-scaffolder`) deja de tener ninguna linea hardcodeada por secreto -- itera `harness.config.json > secrets[]` en runtime y siembra cada entrada segun su `source.type`; como la sintaxis de expresiones de GitHub Actions no permite indexar `secrets` con un nombre dinamico, el job `apply` declara `env: ALL_SECRETS: ${{ toJSON(secrets) }}` y el script hace el lookup por nombre con `jq` (GitHub sigue enmascarando el valor en los logs). Nuevo Paso 2b.0 del agente registra, siempre e idempotentemente (aunque `infra-cd.yml` ya exista), los cuatro secretos fijos del BC en `secrets[]`. El "Caveat de evolvabilidad" se elimina de `README.md` y `agents/infra-base-scaffolder.md`: agregar un secreto nuevo ya no exige editar `infra-cd.yml` a mano. Se enmienda ADR-0025 (decision #6, inventario de la decision #10, y una "Negativa" que quedaba desactualizada) para reflejar el registro declarativo, con el cambio en su Control de cambios. **CA-4/CA-5/CA-6/CA-7/CA-8**: nuevo `commands/seed-secret.md` (ADR-0019, lado publicado) invocable como `/seed-secret <nombre> --domain <Dominio> (--from-output <x> | --from-github-secret <NAME>)`, respaldado por `scripts/seed-secret.sh` (guard cwd != Mefisto; resuelve el dominio contra `infra/environments/<env>/dominio-{kebab}.tf`, la fuente de verdad que ya genero `domain-scaffolder`, aceptando `--domain` en kebab o PascalCase; upsert idempotente en `secrets[]`; si la fuente es `--from-github-secret`, valida con `gh secret list` si ya existe y recuerda crearlo si no). El skill cablea, en el archivo Terraform del dominio, el app setting con la referencia `@Microsoft.KeyVault(...)` versionless y verifica -- sin duplicarlo -- que el rol `Key Vault Secrets User` de `domain-scaffolder` ya cubra ese secreto (nunca toca el `Key Vault Secrets Officer` del SP de CI, ADR-0022 mecanismo M1); ningun valor de secreto viaja en claro. Registrado en el catalogo de skills de `CLAUDE.md`/`README.md` y validado (informativo, nunca bloqueante) por `/onboard`, que ahora reporta cuantas entradas tiene `secrets[]` y si los GitHub secrets que referencian existen. Archivos modificados: `scripts/_pipeline-common.sh`, `scripts/tests/test-harness-config.sh`, `scripts/tests/test-guards.sh`, `agents/infra-base-scaffolder.md`, `docs/adr/0025-custodia-de-secretos.md`, `commands/onboard.md`, `CLAUDE.md`, `README.md`. Archivo nuevo: `scripts/seed-secret.sh`, `commands/seed-secret.md`.

### Fixed

- **Actualizar los paquetes Cosmos a 1.3.0 (resuelve Marten vulnerable) y completar el trio OpenTelemetry del worker en `domain-scaffolder` (issue #263, bug)**: origen -- Cosmos.ControlPlane actualizo de pre-1.0 a `1.3.0` los 5 paquetes Cosmos en sus 5 dominios (PR #47 del consumidor) sin breaking changes de API, resolviendo ademas una vulnerabilidad critica -- 1.3.0 arrastra Marten `9.12.0` (antes `8.23.0`, afectada por [GHSA-vmw2-qwm8-x84c](https://github.com/advisories/GHSA-vmw2-qwm8-x84c)/CVE-2026-45288, inyeccion SQL via `regConfig`). El `domain-scaffolder` seguia fijando las versiones pre-1.0, asi que cada dominio nuevo reintroducia los paquetes desactualizados y la Marten vulnerable. **CA-1/CA-2/CA-3/CA-4/CA-5**: se bumpean a `1.3.0` los 5 paquetes Cosmos en los tres bloques `.csproj` que los fijan (dominio, Contracts, proyecto de tests -- este ultimo reemplazando el wildcard `0.1.*` de `Cosmos.EventSourcing.Testing.Utilities`) y en la tabla de "Decision" de ADR-0003, que ahora documenta la Marten resultante y el CVE resuelto; verificado con `grep` que no queda ningun paquete Cosmos pineado en version pre-1.0. De paso se corrige un comentario impreciso sobre las transitivas de `Cosmos.EventSourcing.Testing.Utilities` (no trae `Cosmos.EventSourcing.Abstractions`/`Cosmos.EventDriven.Abstractions`, verificado contra el nuspec real -- esos dos llegan via el `ProjectReference` al proyecto de dominio). **CA-6/CA-7/CA-8/CA-9/CA-10**: el fix del issue #259 (mas arriba) ya habia reemplazado `Azure.Monitor.OpenTelemetry.AspNetCore` por `Microsoft.Azure.Functions.Worker.OpenTelemetry` + `Azure.Monitor.OpenTelemetry.Exporter`, pero sin el tercer paquete del trio oficial (Microsoft Learn, "Use OpenTelemetry with Azure Functions") -- `OpenTelemetry.Extensions.Hosting` -- y con `Microsoft.Azure.Functions.Worker.OpenTelemetry` fijado en `1.4.0`, una version que nunca existio en NuGet. Se agrega el paquete faltante, se corrige la version a `1.2.0` (la real, verificada contra el nuspec), y se fija explicitamente el metapaquete `Microsoft.Azure.Functions.Worker` en `2.52.0` porque `Worker.OpenTelemetry` 1.2.0 exige `Worker.Core >= 2.52.0`: sin ese pin, un `func init` con plantilla desactualizada deja `Worker.Core` y `Worker.Grpc` desalineados, lo que dispara `MissingMethodException` en `DefaultTraceContext..ctor` al arrancar el host -- HTTP 500 en toda funcion del dominio (verificado por el consumidor, PR #46). Como `func init` (Azure Functions Core Tools 4.6.0) ya emite `Microsoft.Azure.Functions.Worker` en `2.51.0`, el agente **actualiza en su lugar** esa referencia a `2.52.0` en vez de agregar una segunda linea: un `PackageReference` duplicado al mismo paquete solo advierte `NU1504` (no rompe el build) y NuGet resuelve a la version **mas baja** (`2.51.0`), reintroduciendo en silencio el mismo desalineamiento Core/Grpc -- verificado con `dotnet restore` durante la revision. El `Program.cs` y el `host.json` generados ya seguian el patron correcto (`.UseFunctionsWorkerDefaults()` + `.UseAzureMonitorExporter()`, `telemetryMode: "OpenTelemetry"`) desde el #259 y no requirieron cambios. ADR-0003 (seccion "Observabilidad" y tabla de paquetes) se enmienda para reflejar el trio completo y la nota de compatibilidad de versiones, registrado en su "Control de cambios". Todas las versiones se verificaron contra el nuspec real de cada paquete en api.nuget.org. Archivos modificados: `agents/domain-scaffolder.md`, `docs/adr/0003-event-sourcing-marten-wolverine.md`.

- **Corregir la telemetria de App Insights en dominios scaffoldeados: exporter OTel de Functions ausente y sin integracion de plataforma (issue #259)**: origen -- draft cross-repo del consumidor Cosmos.ControlPlane (Luis Felipe Diaz), sintoma verificado en campo: App Insights solo recibia los `requests` del host de Functions, nunca las ejecuciones de las Functions de Service Bus ni las dependencias (SDKs externos), porque OpenTelemetry recolectaba traces/metrics/logs pero no tenia exporter -- recolectaba y descartaba. **CA-1/CA-2**: `domain-scaffolder` reemplaza el paquete `Azure.Monitor.OpenTelemetry.AspNetCore` (la distro de ASP.NET Core, no soportada para Azure Functions isolated worker) por `Microsoft.Azure.Functions.Worker.OpenTelemetry` + `Azure.Monitor.OpenTelemetry.Exporter` (camino oficial, Microsoft Learn "Use OpenTelemetry with Azure Functions"), y el `Program.cs` generado encadena `.UseFunctionsWorkerDefaults()` y `.UseAzureMonitorExporter()` sobre `AddOpenTelemetry()`, conservando el `.WithTracing(...).AddSource("Wolverine"/"Marten"/namespace)` existente. **CA-3**: el `host.json` generado (Paso 1.8) suma `"telemetryMode": "OpenTelemetry"` en la raiz, para que el host tambien emita OpenTelemetry y se correlacione con el worker. **CA-4**: el modulo Terraform `function-app` de `infra-base-scaffolder` deja de declarar `APPLICATIONINSIGHTS_CONNECTION_STRING` como app setting manual y en su lugar setea `site_config.application_insights_connection_string = var.app_insights_connection_string` -- `azurerm` gestiona ese app setting por su cuenta a partir del argumento, sin setting duplicado. **CA-5**: se corrige la seccion "Observabilidad" de ADR-0003 (describia una ingesta automatica del host sin exporter que no existe para Functions isolated worker) y la tabla de paquetes de su "Decision", con el contenido superado eliminado del cuerpo y el cambio registrado en su "Control de cambios" (doctrina no-obsolete). El fix que proponia el draft original (`.UseAzureMonitor()` de `Azure.Monitor.OpenTelemetry.AspNetCore`) no era el camino soportado por Microsoft para Functions isolated worker; este issue prescribio y aplico el camino oficial. Archivos modificados: `agents/domain-scaffolder.md`, `agents/infra-base-scaffolder.md`, `docs/adr/0003-event-sourcing-marten-wolverine.md`.

- **Corregir el `run:` de `smoke-tests-dominio.yml` generado por `domain-scaffolder` para .NET 10 Microsoft Testing Platform (issue #253, ADR-0013)**: reporte de campo verificado (consumidor Cosmos.ControlPlane, Luis Felipe Diaz) -- el `run:` del reutilizable (Paso 6.1) invocaba `dotnet test "${{ inputs.test_project }}" --configuration Release` pasando un **directorio** como argumento posicional; con el SDK de .NET 10 y `global.json` fijando `test.runner = Microsoft.Testing.Platform` (default del scaffold), `dotnet test` ya no acepta un directorio posicional y aborta con exit code 1 antes de correr ningun test ("Specifying a directory for 'dotnet test' should be via '--project' or '--solution'"), rompiendo tanto el job `smoke-tests` post-deploy de cada `deploy-<dominio>.yml` como el workflow global `smoke-tests.yml`, y marcando en rojo la corrida de deploy aunque el deploy en si fuera exitoso. El resto del harness ya usaba la forma correcta (`agents/smoke-test-writer.md`, ADR-0013, y el propio `domain-scaffolder.md` en otras dos lineas) -- solo este `run:` habia quedado con la forma vieja. **CA-1**: se corrige a `dotnet test --project "${{ inputs.test_project }}" --configuration Release`. **CA-2**: no cambia el contrato de entrada -- `test_project` sigue siendo un directorio, que `--project` resuelve al unico `.csproj` que contiene; no se toca `.github/smoke-tests/*.json` ni el `test_project` que pasan los `deploy-<dominio>.yml`. **CA-3**: se documenta junto al Paso 6 que la idempotencia (el scaffolder nunca reescribe un `smoke-tests-dominio.yml` ya existente) implica que los repos ya scaffoldeados antes de este fix deben aplicar el mismo parche a mano. Archivo modificado: unicamente `agents/domain-scaffolder.md`.

## [0.11.0] - 2026-07-09

### Added

- **`infra-base-scaffolder` emite el `.gitignore` raiz del repo consumidor de forma deterministica (issue #241, ADR-0021, ADR-0025)**: en greenfield ningun componente del harness generaba el `.gitignore` **raiz** -- solo el del entorno Terraform (Paso 2.5). Aparecia por improvisacion del LLM en cada corrida de `/scaffold`, con contenido divergente entre corridas: en el scaffold en paralelo de 5 dominios (Cosmos.ControlPlane, SincoERP, investigacion #238) dos ramas generaron el raiz con contenido distinto y un add/add que deberia mergear limpio se volvio conflicto real -- y, mas grave (ADR-0025), si el raiz improvisado no ignoraba `local.settings.json`, el `Password=postgres` que `domain-scaffolder` escribe ahi (Paso 9) podia commitearse al historial de git. **CA-1/CA-2**: nuevo **Paso 2c** en `infra-base-scaffolder` que emite `.gitignore` en la raiz del repo consumidor con contenido **byte-fijo** (build output .NET, `local.settings.json`, artefactos de IDE, logs, resultados de test) **solo si no existe** (idempotencia, mismo patron que `infra-cd.yml`) -- el determinismo viene de ser una corrida unica en greenfield, no del guard; se stagea en el Paso 4 (`git add .gitignore` condicional, junto a `git add infra/`) y se reporta en el Paso 5. Nueva regla absoluta 12. **CA-3/CA-4**: `domain-scaffolder` deja de borrar el `.gitignore` per-proyecto que `func init` genera en `src/<RootNamespace>.{PascalCase}/` (ya ignora `local.settings.json`, `bin/`, `obj/` por defecto) -- queda como blindaje local que evita el leak del secreto de desarrollo aunque el raiz todavia no exista o el orden de invocacion se rompa; se documenta ademas, junto al Paso 1, la dependencia de orden con `infra-base-scaffolder`. **CA-5**: se refuerza en el Paso 6 la instruccion de transcribir byte-a-byte `smoke-tests-dominio.yml` y `smoke-tests.yml`, para que dos scaffolds en paralelo produzcan archivos identicos. Archivos modificados: `agents/infra-base-scaffolder.md`, `agents/domain-scaffolder.md`.

### Changed

- **`domain-scaffolder` deja de tocar archivos compartidos al dar de alta un dominio: el scaffold de N dominios en paralelo no genera conflictos (issue #234, ADR-0020, ADR-0021)**: verificado en Cosmos.ControlPlane (SincoERP) al scaffoldear 5 dominios en paralelo -- de 5, solo 2 abrieron PR limpio, y esos 2 chocaban entre si en `main.tf` (los marcadores de conflicto partian bloques `resource {...}` a la mitad). **Frente 1 (CA-1.1..CA-1.6)**: el Paso 4 ya no lee ni agrega al final de `infra/environments/dev/main.tf` -- crea un archivo **plano y propio** por dominio, `infra/environments/dev/dominio-{kebab}.tf`, con los mismos cuatro bloques HCL de siempre (`random_string.storage_suffix_*`, `module storage_*`, `module service_plan_*`, `module function_app_*` + los cuatro `azurerm_role_assignment`) y las mismas referencias intactas a `local.*`/`module.*`/`var.environment` del root module. Terraform evalua todos los `.tf` del directorio del entorno como un unico root module sin recorrer subdirectorios (HashiCorp, Terraform Language -- "Files and Directories"), por lo que un archivo plano no exige convertir el dominio en modulo con inputs. Nueva Validacion 3 del Paso 0 detiene el agente sin pisar el archivo si ya existe (mismo patron que la Validacion 2 de `src/`). Paso 8 (commit) y "Resultado final" reflejan el archivo nuevo; `main.tf` no se toca al dar de alta un dominio. **Frente 2 (CA-2.1..CA-2.4)**: el Paso 6b registra el dominio en su propio `.github/smoke-tests/{kebab}.json` (un objeto JSON suelto) en vez de aniadir al array compartido `.github/smoke-tests-dominios.json`; el workflow global `smoke-tests.yml` (Paso 6.2) arma la matrix por glob (`jq -sc . .github/smoke-tests/*.json`, con `shopt -s nullglob` + `if:` para tolerar cero archivos sin fallar) y documenta que el archivo legacy monolitico, si sigue presente en un consumidor existente, queda ignorado sin romper nada (conviven; migracion opcional entrada-por-entrada). **Frente 3 (CA-3.1)**: se documenta junto al Paso 3 que `<SolutionFile>` sigue siendo el unico archivo compartido -- su merge es aditivo trivial (add/add sin cuerpo compartido) y no requiere cambio de codigo. **Frente 5 (CA-5.1, CA-5.2)** en `scripts/scaffold-pipeline.sh`: `LOG_FILE` suma el PID (`$$`) y `SCAFFOLD_LOG` suma `$DOMAIN_NAME` + PID, ademas del `TIMESTAMP` de granularidad de segundos, para que dos pipelines lanzados en el mismo segundo no compartan archivo de log; `EVENTS_LOG` sigue compartido y append-only, sin cambios. El PR body del pipeline se actualiza para referenciar los nuevos paths por dominio; se corrigen ademas dos previews de usuario que quedaban desincronizadas con los paths nuevos (`commands/scaffold.md`, confirmacion previa al lanzar el pipeline; `commands/implement.md`, confirmacion del scaffold inline cuando el dominio del issue aun no existe) y se enmienda ADR-0013 (doctrina de smoke tests) para reflejar el archivo por dominio en vez del array compartido, con el contenido superado eliminado del cuerpo (doctrina no-obsolete) y registrado en su "Control de cambios". Archivos modificados: `agents/domain-scaffolder.md`, `scripts/scaffold-pipeline.sh`, `commands/scaffold.md`, `commands/implement.md`, `docs/adr/0013-smoke-tests-contra-entorno-dev.md`.

- **ADR-0025 y ADR-0022 invierten la custodia de secretos: los siembra CI, no un admin a mano (issue #229)**: reporte de campo de un consumidor greenfield -- siguiendo el paso 5 del README ("Sembrar los secretos de Key Vault"), un admin, **aun siendo Owner de la suscripcion**, chocaba con `ForbiddenByRbac` al correr `az keyvault secret set`, porque el Key Vault se crea con `enable_rbac_authorization = true` y ningun paso del bootstrap le asignaba un rol de datos (el control plane de RBAC no otorga acceso al data plane). **CA-1/CA-2**: ADR-0025 decision #6 ("Propiedad del valor") y decision #10 (tabla de perfiles + inventario de la siembra) se reforman: el valor de **todos** los secretos del BC -- interno de ASB, `marten-connection`, `app-insights-connection` y cada `serviceBus.external[]` compartido -- lo siembra **CI** en un step de workflow posterior al `apply` (`az keyvault secret set`), nunca un admin a mano ni Terraform en el state. El perfil (c) deja de ser un "privilegio de infra/admin recurrente" y pasa a ser una accion automatica de CI; la unica accion manual que le queda al admin es crear los GitHub secrets que alimentan la siembra (`TF_VAR_POSTGRESQL_ADMIN_PASSWORD` + uno por alias de `serviceBus.external[]`). **CA-3**: se documenta que `infra-cd.yml` itera sobre `serviceBus.external[]`, sembrando cada alias desde su propio GitHub secret. **CA-4**: ADR-0022 fija el mecanismo M1 -- nueva seccion "Rol de datos de Key Vault para el propio SP de CI": el `apply`, bajo el `Role Based Access Control Administrator` que ya tiene el SP, se autoasigna `Key Vault Secrets Officer` sobre el vault del BC (mismo patron que ya usan los scaffolders para `Key Vault Secrets User`); la condicion anti-escalacion lo permite por ser un rol de datos, no de administracion de roles; `scripts/setup-github-ci.sh` **no se modifica** (el vault no existe en el bootstrap). **CA-5/CA-6**: ambos ADR registran que ningun humano necesita ya un rol de datos de Key Vault -- el footgun `ForbiddenByRbac` desaparece por construccion --, y el control de cambios de cada ADR cita este issue; nada se marca "obsoleto", el contenido superado se elimina del cuerpo. Este issue es el ancla doctrinal; la implementacion (siembra en `infra-cd.yml` + rol M1, `/onboard`, README paso 5) queda en los hijos #231, #232 y #233. Archivos modificados: `docs/adr/0025-custodia-de-secretos.md`, `docs/adr/0022-autenticacion-ci-azure-oidc.md`.

- **`/onboard` deja de instruir la siembra manual de secretos en Key Vault: refleja que la hace CI (issue #232, ADR-0025)**: materializa en el skill publicado la reforma doctrinal de #229 -- `/onboard` seguia recomendando `az keyvault secret set` a mano con "rol admin/infra" en su recordatorio recurrente de "Proximos pasos", el mismo footgun `ForbiddenByRbac` que origino la reforma (un Owner de suscripcion sin rol de datos sobre un vault con RBAC habilitado). **CA-1**: se elimina del bloque "Proximos pasos" (`N_FALTA == 0`) la instruccion de sembrar a mano; no se reescribe a "hazlo con rol de datos", se elimina por completo porque ya no hay siembra manual. **CA-2**: el recordatorio pasa a documentar que la siembra la hace CI (`infra-cd.yml`) tras el `apply`, y que la unica accion manual del admin es crear/verificar los GitHub secrets que la alimentan (`TF_VAR_POSTGRESQL_ADMIN_PASSWORD` y un `SB_EXTERNAL_<ALIAS>_CONNECTION_STRING` por cada alias de `serviceBus.external[]`) en Settings > Secrets and variables > Actions. **CA-3**: se verifico que ninguna otra parte del skill mencionaba `ForbiddenByRbac`, un rol de datos de Key Vault para el admin, ni "Owner != acceso a datos" como paso a resolver -- el footgun desaparece por construccion. **CA-4**: se agrega un sexto diagnostico, informativo y no bloqueante (reusa la lectura de `gh secret list` ya hecha para los secrets OIDC), que reporta si existen `TF_VAR_POSTGRESQL_ADMIN_PASSWORD` y los `SB_EXTERNAL_<ALIAS>_CONNECTION_STRING` requeridos por `serviceBus.external[]`; nunca verifica el data plane del vault, que ya no aplica. No se toca `scripts/setup-github-ci.sh`: el rol de datos M1 lo crea el `apply` (issue #231), no el bootstrap. Archivo modificado: unicamente `commands/onboard.md`.

- **`infra-base-scaffolder` implementa la siembra de secretos en CI + rol de datos M1 (issue #231, ADR-0025, ADR-0022)**: materializa en el agente la reforma doctrinal de #229 -- deja de instruir la siembra manual del Key Vault y la reemplaza por un mecanismo de CI. **CA-1**: el `main.tf` del esqueleto del entorno (Paso 2.3) suma `azurerm_role_assignment.ci_kv_secrets_officer` (`role_definition_name = "Key Vault Secrets Officer"`, `scope = module.key_vault.id`, `principal_id = data.azurerm_client_config.current.object_id`); sigue sin crear jamas un `azurerm_key_vault_secret` (regla absoluta 8, reencuadrada: "la siembra es un step de CI via `az`"). **CA-2/CA-3**: el job `apply` de `infra-cd.yml` (Paso 2b) gana el step post-`apply` "Sembrar los secretos del Key Vault", que siembra con `az keyvault secret set` los tres secretos fijos del BC (`serviceBus.internal.secretName` y `app-insights-connection` desde `terraform output`; `marten-connection` compuesto desde `postgresql_fqdn`/`postgresql_database_name`/`postgresql_administrator_login` + `TF_VAR_POSTGRESQL_ADMIN_PASSWORD`, ya en el `env`) mas un secreto por cada `serviceBus.external[]` alias, con el valor tomado del nuevo GitHub secret por alias `SB_EXTERNAL_<ALIAS>_CONNECTION_STRING` inyectado al `env` del job. **CA-4**: el step reintenta con backoff (hasta ~3 min) ante `ForbiddenByRbac`, porque el role assignment de CA-1 y su primer uso ocurren en la misma corrida y la propagacion de RBAC de Azure tiene latencia (1-2 min, Microsoft Learn). **CA-5**: la siembra es idempotente (`az keyvault secret set` sobre el mismo nombre no falla) y preserva la idempotencia ya vigente de `infra-cd.yml` (el archivo sigue sin regenerarse una vez creado, regla 10). **CA-6**: el Paso 5 (resumen del agente) deja de instruir `az keyvault secret set` manual -- documenta que CI siembra automaticamente y que la unica accion del admin es crear los GitHub secrets (`TF_VAR_POSTGRESQL_ADMIN_PASSWORD` + uno por alias de `serviceBus.external[]`) -- y suma el caveat de evolvabilidad: un alias `serviceBus.external[]` agregado despues de la primera generacion de `infra-cd.yml` exige anadir a mano su GitHub secret y su linea de siembra, porque el workflow no se sobrescribe. Archivo modificado: unicamente `agents/infra-base-scaffolder.md`.

### Fixed

- **Corregir `REPO_ROOT` en `scaffold-pipeline.sh`: apuntaba al cache del plugin, no al repo consumidor (issue #228)**: tras la migracion de ADR-0019 (el plugin dejo de vivir dentro del repo del consumidor), `REPO_ROOT` se seguia derivando de `$SCRIPT_DIR/..` (la ubicacion del script, en instalaciones marketplace el cache del plugin) en vez del cwd del consumidor -- el cache no es un repo git, asi que la primera operacion que usaba `REPO_ROOT` (`git -C "$REPO_ROOT" remote get-url origin`) abortaba con `fatal: not a git repository` (exit 128) antes de crear worktree, rama o PR, inutilizando `/scaffold` en cualquier instalacion via marketplace. **CA-1**: `REPO_ROOT` ahora se obtiene con `git rev-parse --show-toplevel`, mismo patron ya vigente en los otros cinco pipelines publicados (`tdd-pipeline.sh`, `tooling-pipeline.sh`, `iac-pipeline.sh`, `batch-pipeline.sh`, `parallel-pipeline.sh`). **CA-2**: se elimina la linea `SCRIPT_DIR=...`, que solo alimentaba el calculo anterior de `REPO_ROOT` y queda huerfana. El guard defensivo (lineas 16-28) y el `source` de `_pipeline-common.sh` (que resuelve su propia ruta via `dirname "${BASH_SOURCE[0]}"`, correcto porque busca la libreria comun en el cache del plugin, no el repo consumidor) no se tocan. Archivo modificado: unicamente `scripts/scaffold-pipeline.sh`.

- **`scaffold-pipeline.sh` commitea defensivamente cuando `domain-scaffolder` omite el commit (issue #236)**: el Paso 8 del agente (`git add` + `git commit`) es no determinista por ser un LLM -- reportado por Luis Felipe Diaz scaffoldeando 5 dominios en Cosmos.ControlPlane: Onboarding y UserManagement commitearon y abrieron PR OK, Billing genero el arbol de archivos pero sin commit, y `gh pr create` fallo con "No commits between main and scaffold-billing". El pipeline verificaba solo que existiera `src/<Ns>.<Pascal>/` y saltaba directo al `git push`, sin garantizar el commit. **CA-1**: nuevo bloque entre el fin del scaffold y la creacion del PR que, si el worktree tiene cambios sin commitear tras el agente, los commitea con `git add -A` + `git commit -m "scaffold(<dominio>): nuevo dominio <PascalCase>"`. **CA-2**: en el camino feliz (el agente ya commiteo) el bloque no genera commit extra -- la condicion es `git status --porcelain` sobre el working tree, no un conteo de commits. **CA-3**: antes de evaluar el working tree se restaura `.claude/settings.json` con `git checkout -- .claude/` (mismo patron que ya usa el cleanup final) para que el patch runtime que el pipeline inyecta en el worktree (ruta absoluta de `events.log`) nunca entre al `git add -A` ni viaje en el diff del PR. **CA-4**: red de seguridad final -- si tras el agente + el commit defensivo no hay ningun commit sobre `origin/main` (`git log --oneline origin/main..HEAD` vacio), el pipeline aborta con mensaje claro antes del `push`, en vez de dejar que `gh pr create` falle rio abajo. Archivo modificado: unicamente `scripts/scaffold-pipeline.sh`.

- **Corregir la compuerta de longitud del nombre de Function App en `domain-scaffolder`: limite real 60, no 32 (issue #245)**: reporte de campo de Luis Felipe Diaz (Cosmos.ControlPlane, misma tanda de #228/#234/#236) -- la Validacion 1 del Paso 0 rechazaba dominios validos (`tenant-management`, `tenant-provisioning`) cuyo nombre `func-{prefix_func}-{kebab}` superaba **32** caracteres, y como el agente corre no interactivo (`claude -p ... --permission-mode bypassPermissions` en `scripts/scaffold-pipeline.sh`) el mensaje "elige un nombre mas corto" no llegaba a nadie: no creaba nada y el pipeline abortaba en su post-check. Verificado contra las naming rules de Azure (https://learn.microsoft.com/azure/azure-resource-manager/management/resource-name-rules#microsoftweb): el nombre de recurso `Microsoft.Web/sites` admite **2-60** caracteres; el "32" corresponde al truncado del **host ID** de Azure Functions, cuya colision solo ocurre si dos Function Apps comparten la misma storage account (https://learn.microsoft.com/azure/azure-functions/storage-considerations#host-id-considerations; AZFD0004), y en este marco cada Function App tiene su propia Storage Account y su propio plan dedicado sin slots (ADR-0020) -- esa colision nunca aplica. **CA-1/CA-2**: la Validacion 1 de `agents/domain-scaffolder.md` (Paso 0) ahora valida el nombre completo contra **60**, con mensaje de error que expone el presupuesto real del kebab (`60 - 5 ("func-") - 1 ("-") - len(prefix_func)`); como `func-` es el prefijo mas largo entre los dos recursos que comparten `{prefix_func}-{kebab}`, la misma validacion cubre al App Service Plan (`asp-`, `Microsoft.Web/serverfarms`, 1-60). **CA-3**: la Storage Account (Paso 4, limite real 24 de `Microsoft.Storage/storageAccounts`) sigue siendo el unico recurso que se trunca, y su truncado pasa a ser **determinista**: la instruccion LLM-driven ("avisa al usuario y trunca hasta que quepa") se reemplaza por una regla mecanica fija -- `{kebab-storage}` = primeros 13 caracteres del kebab sin guiones para el entorno `dev` (`24 - 2 "st" - 3 "dev" - 6 sufijo`), con el sufijo aleatorio de 6 chars (`random_string`) garantizando unicidad global aun si dos dominios largos comparten los primeros 13 -- eliminando la misma clase de no-determinismo investigada en #238 (dos corridas del scaffolder producen el mismo archivo byte a byte). **CA-4**: se agrega nota del presupuesto de nombres en `commands/onboard.md` (junto al diagnostico de labels por `domainLabels`) y en `docs/adr/0006-convenciones-nombramiento-funciones-azure.md` (nota nueva, sin reformar el ADR). **Coherencia**: `agents/infra-base-scaffolder.md` (Paso 0) justificaba la brevedad de `project_short` citando "Function App <= 32 chars, ver ADR-0006" -- doblemente incorrecto (32 no es el limite del Function App, y el binding constraint real que consume `project_short` es el Key Vault, `kv-{project_short}-{sufijo}`, 3-24 chars, ya documentado en la nota **Limites de Azure (CA-2)** del Paso 2.3) y ademas autocontradictorio tras enmendar ADR-0006; se corrige la justificacion para que apunte al Key Vault y no perpetue el "32" que este issue refuta. Archivos modificados: `agents/domain-scaffolder.md`, `commands/onboard.md`, `docs/adr/0006-convenciones-nombramiento-funciones-azure.md`, `agents/infra-base-scaffolder.md`.

- **Renombrar `enable_rbac_authorization` a `rbac_authorization_enabled` en el modulo `key-vault` de `infra-base-scaffolder` (issue #242, ADR-0021, ADR-0025)**: reporte de campo de un consumidor greenfield (Luis Felipe Diaz) -- el `terraform validate/plan/apply` del entorno generado emitia en cada corrida el warning "Argument is deprecated ... `enable_rbac_authorization` ... ha sido renombrado a `rbac_authorization_enabled` y sera removido en v5.0 del provider", repetido varias veces por plan porque `module.key_vault.uri` se referencia desde varios app settings. Con el pin `~> 4.0` del provider `azurerm` que fija el scaffold es solo ruido hoy; al subir a v5.0 el argumento viejo desaparece y `validate`/`plan` fallarian. **CA-1**: el bloque `resource "azurerm_key_vault" "this"` (Paso 1.8) reemplaza `enable_rbac_authorization = true` por `rbac_authorization_enabled = true` -- mismo comportamiento (RBAC habilitado, sin access policies, ADR-0025 decision #5), solo cambia el nombre del argumento. **CA-2**: las tres referencias en prosa/comentarios que nombraban el argumento viejo (descripcion del modulo y comentarios del esqueleto del entorno) se actualizan al nombre nuevo. **CA-3**: auditoria de cara a v5.0 sobre el resto de los modulos (`resource-group`, `monitoring`, `postgresql`, `service-bus`, `service-plan`, `storage`, `function-app`), verificada contra el schema fuente del provider (`hashicorp/terraform-provider-azurerm`, rama `main`): ademas de `enable_rbac_authorization` aparecio un segundo argumento deprecado no cubierto por el issue -- `daily_data_cap_notifications_disabled` en `azurerm_application_insights` (modulo `monitoring`), deprecado en favor de `daily_data_cap_notifications_enabled` con la misma ventana de remocion (v5.0) -- se corrige junto con el rename principal por ser la misma clase de deuda tecnica: `daily_data_cap_notifications_disabled = false` pasa a `daily_data_cap_notifications_enabled = true` (semantica identica; el codigo del provider fija ambos booleans sobre `StopSendNotificationWhenHitCap` con polaridad invertida). El resto de los modulos no arrastra argumentos deprecados de cara a v5.0. **Coherencia doctrinal**: ADR-0022 nombraba el argumento viejo `enable_rbac_authorization = true` en el cuerpo (seccion "Rol de datos de Key Vault para el propio SP de CI") como descriptor presente de un Key Vault con RBAC; se renombra a `rbac_authorization_enabled = true` para que la doctrina no perpetue el nombre deprecado que este issue elimina (cambio sintactico, ninguna decision del ADR se altera). La mencion del nombre viejo en la entrada de change-log de ADR-0025 (2026-07-08, #229) se conserva como registro historico del reporte de campo. Archivos modificados: `agents/infra-base-scaffolder.md`, `docs/adr/0022-autenticacion-ci-azure-oidc.md`.

## [0.10.0] - 2026-07-07

### Fixed

- **Sanear la coherencia de la documentacion de onboarding greenfield (issue #213, ADR-0007, ADR-0022)**: barrido de coherencia sobre el onboarding operativo que dejaba afirmaciones contradictorias y caveats verificados en campo sin documentar. **CA-1**: el conteo de labels default queda unico y preciso -- `README.md` (paso 4a) y el comentario de `scripts/setup-github-labels.sh` decian "elimina los 9 labels default"; se corrigen a la formulacion exacta ya vigente en `commands/onboard.md` (borra 8 de los 9 default y recrea `bug`). **CA-2**: `setup-github-ci.sh` se describia dos veces con distinto nivel de detalle en `README.md` (paso 4b y paso 5 sub-paso 2); el paso 4b se recorta a lo esencial (proposito, prerequisitos, dependencia del backend) y remite al paso 5 como descripcion canonica unica. **CA-3**: se verifico que el orden `/onboard` <-> backend ya estaba bien anclado (paso 2 y paso 4 del README ya remiten la provision de CI al paso 5, tras el backend) -- no requirio cambios. **CA-4**: se documenta en el paso 1 del bootstrap de infraestructura (`README.md`) el caveat de que la primera corrida de `bootstrap-backend.sh` con `--auth-mode login` exige el rol `Storage Blob Data Contributor` sobre la Storage del tfstate (ya advertido por el script pero ausente del README). **CA-5**: se documenta, en `README.md` (paso 5 sub-paso 2) y `commands/onboard.md` (provision opt-in del CI), la idempotencia parcial de la condicion ABAC de `setup-github-ci.sh`: `az role assignment create` no actualiza la condicion de una asignacion de `Role Based Access Control Administrator` que ya exista para el mismo principal/rol/alcance (la introdujo el issue #195, asi que en un onboarding limpio se crea de una vez con la condicion; el caso a vigilar es una asignacion sin condicion creada fuera del script -- p. ej. a mano -- o un cambio futuro de la expresion de la condicion), por lo que re-correrlo no le aplica ni actualiza la condicion anti-escalacion; se documenta el diagnostico y la remediacion manual, y el arreglo del script queda diferido a un issue aparte. Archivos modificados: `README.md`, `commands/onboard.md`, `scripts/setup-github-labels.sh`.

- **Corregir la fila `function-app` de la tabla de los 8 modulos de ADR-0021 (issue #210, ADR-0021, ADR-0025)**: la tabla seguia listando los inputs `storage_account_connection_string` y `storage_account_access_key` para el modulo `function-app`, aunque ADR-0025 decision #3 (storage por identidad) y la regla absoluta #9 de `agents/infra-base-scaffolder.md` ya los prohiben y el HCL real del modulo (`agents/infra-base-scaffolder.md:675`) usa `storage_uses_managed_identity = true` sin esos inputs desde el issue #182. Se corrige la fila para reflejar el contrato real (`name`, `resource_group_name`, `location`, `service_plan_id`, `storage_account_name`, `app_insights_connection_string`, `app_settings`, `tags`) y se registra el cambio en "Control de cambios" de ADR-0021. Archivo modificado: unicamente `docs/adr/0021-infraestructura-base.md`.

- **`infra-cd.yml` alimenta las variables Terraform requeridas por `TF_VAR_*` y se blinda `terraform.tfvars` (issue #208, ADR-0022, ADR-0025)**: el `env` del workflow que emite `infra-base-scaffolder` solo declaraba `ARM_CLIENT_ID`/`ARM_TENANT_ID`/`ARM_SUBSCRIPTION_ID`/`ARM_USE_OIDC`, sin fuente para `alert_email` ni `postgresql_admin_password` (ambas requeridas, sin default en `variables.tf`); con `-input=false`, `plan`/`apply` fallaban con "No value for required variable" en cualquier greenfield, admin incluido. (1) **CA-1**: el `env` global de `infra-cd.yml` (Paso 2b) suma `TF_VAR_alert_email: ${{ vars.ALERT_EMAIL }}` y `TF_VAR_postgresql_admin_password: ${{ secrets.TF_VAR_POSTGRESQL_ADMIN_PASSWORD }}`, heredados por los jobs `plan` y `apply`. (2) **CA-2**: `subscription_id` deja de ser una variable Terraform del entorno -- se elimina `variable "subscription_id"` de `variables.tf` (Paso 2.2) y `subscription_id = var.subscription_id` del `provider "azurerm"` (Paso 2.1); el provider la resuelve nativamente de `ARM_SUBSCRIPTION_ID` (ya presente en el `env`, HashiCorp docs del provider `azurerm`). Verificado con `terraform init -backend=false` + `validate` real sobre el HCL emitido (sin credenciales): sigue pasando. (3) **CA-3**: nuevo **Paso 2.5** genera `infra/environments/<env>/.gitignore` (patron estandar de Terraform: `.terraform/`, `*.tfstate*`, `terraform.tfvars`/`*.auto.tfvars`, etc.) para que un `terraform.tfvars` local nunca se commitee con el password en claro (ADR-0025 decision #1); nueva regla absoluta 11. (4) **CA-4**: se documenta (Paso 5 del agente, `commands/infra-base.md` y `README.md`) que la GitHub **variable** `ALERT_EMAIL` y el GitHub **secret** `TF_VAR_POSTGRESQL_ADMIN_PASSWORD` los crea **manualmente el admin del repo** -- `scripts/setup-github-ci.sh` no los toca (Opcion R: generar la credencial de la base de datos es un concern distinto de provisionar la identidad de CI) -- y que el mismo valor del password sirve luego para sembrar `marten-connection` en el Key Vault. (5) **CA-5**: el Paso 5 del agente, `commands/infra-base.md` y `README.md` dejan de instruir pasar `alert_email`/`postgresql_admin_password`/`subscription_id` por `terraform.tfvars` y documentan el wiring por `TF_VAR_*`/GitHub. (6) **CA-6**: ADR-0022 suma la seccion "Fuente de las variables Terraform requeridas por `infra-cd.yml`" y corrige la afirmacion "los unicos secrets de GitHub son tres" (acotandola a los que autentican `azure/login`); ADR-0025 suma la decision #9 ("Fuente del `postgresql_admin_password` para el `apply` de CI") con la doctrina no-obsolete (nada que eliminar del cuerpo: el vacio nunca habia quedado documentado). Archivos modificados: `agents/infra-base-scaffolder.md`, `commands/infra-base.md`, `README.md`, `docs/adr/0022-autenticacion-ci-azure-oidc.md`, `docs/adr/0025-custodia-de-secretos.md`.

- **`infra-bootstrap` completa la orquestacion greenfield: labels, CI y base entre el bootstrap del backend y el pipeline IaC (issue #209, ADR-0021, ADR-0022, ADR-0025)**: el agente saltaba del paso 3 (`bootstrap-backend.sh`) directo al paso 4 (`iac-pipeline.sh`), sin invocar `setup-github-labels.sh` ni `setup-github-ci.sh` ni generar la infraestructura base -- contradiciendo la cadena greenfield que ADR-0021 ya fijaba (`bootstrap-backend.sh` -> `infra-base-scaffolder` -> `/infra`). El propio agente prometia que el `apply` corre en CI, pero sin `setup-github-ci.sh` no existe el Service Principal para autenticarse, y sin la base el `/infra` del paso 4 no tenia modulos sobre los que escribir/validar. (1) **CA-1**: se insertan tres pasos nuevos, en orden, entre el bootstrap del backend (paso 3) y el pipeline IaC (ahora paso 7): paso 4 (`setup-github-labels.sh`, esquema de labels), paso 5 (`setup-github-ci.sh`, **despues** del backend porque resuelve el nombre real de su Storage Account) y paso 6 (generacion de la infraestructura base). (2) **CA-2**: el paso 2 (verificacion de prerequisitos) suma una advertencia explicita de los privilegios de admin que exige el bootstrap -- `Owner` (o `Role Based Access Control Administrator` + `User Access Administrator`) a nivel suscripcion y `Application Administrator` de Entra -- citando ADR-0022 (`docs/adr/0022-autenticacion-ci-azure-oidc.md:47` y `:137`) y ADR-0025 (`docs/adr/0025-custodia-de-secretos.md:52`). (3) **CA-3**: los bloques nuevos reutilizan integramente el patron `$PLUGIN_ROOT`/`$PLUGIN_SCRIPTS` ya vigente en el agente; ninguna invocacion relativa. (4) **CA-4**: cada eslabon nuevo documenta su propia idempotencia (labels con `--force`, CI con "ya existe; se reutiliza") y el flujo continua sin abortar si el script reporta que el recurso ya existe; la seccion "Manejo de errores" se actualiza para permitir reintentar solo el script que fallo, sin repetir el bootstrap del backend. Para que la idempotencia del paso 4 sea real (y no solo documentada), se corrige `scripts/setup-github-labels.sh`: los labels `tipo:*`, `estado:*` y `bloqueado` se creaban **sin** `--force`, asi que una re-ejecucion abortaba (`set -e` + exit≠0 de `gh label create` sobre un label existente) -- contradiciendo tanto la nota del paso 4 como la seccion "Manejo de errores" que prometen reintentar el script. Ahora los 9 `gh label create` usan `--force` (consistente con `dom:*`/`bug`, que ya lo tenian), de modo que el script es plenamente re-ejecutable. El eslabon de la base (agente `infra-base-scaffolder` / skill `/infra-base`) no se invoca por Bash -- el propio agente no puede lanzar otro agente ni correr un slash command--: el paso 6 lo documenta como **accion guiada** que le indica al usuario ejecutar `/infra-base <ambiente>` y espera su confirmacion antes de continuar. Archivos modificados: `agents/infra-bootstrap.md` y `scripts/setup-github-labels.sh`.

### Removed

- **Se elimina el apply local del pipeline IaC: `iac-pipeline.sh` y `infra-reviewer` dejan de tocar Azure (issue #199, ADR-0021, ADR-0022)**: materializa el mecanismo concreto que ambos ADRs remitian a este issue -- en el flujo *ongoing*, el desarrollador que usa Mefisto tiene **cero permisos de Azure**. Se elimina `agents/infra-applier.md` y todo su rastro: la entrada de la tabla de agentes de `CLAUDE.md`, la lista/conteo de `README.md` (17 -> 16 agentes) y su fila del dashboard en `commands/work-status.md` -- CA-4. En `scripts/iac-pipeline.sh` se elimina el Stage 3 (apply) completo junto con los flags `--auto-apply`/`--skip-apply`, `--from-stage 3`, la variable `IAC_AUTO_APPLY`, el marcador `.preview-pr` y el tracking `AGENT_AP_*` (incluida su entrada en el status JSON) -- CA-2. El Stage 2 (`infra-reviewer`) pasa de ejecutar `terraform plan -out=tfplan` a una **revision estatica** (`terraform fmt -check` + `terraform init -backend=false` + `terraform validate`, mismo patron que ya usa `infra-base-scaffolder`); el gate de la etapa verifica `validate`, no la existencia de un `tfplan` -- CA-1. La copia+commit de `backend.tf` al worktree se conserva (CI lo necesita para aplicar con el backend remoto). El PR que crea el pipeline **ya no lleva `Closes #N`** bajo ninguna condicion: el pipeline comenta el issue indicando que la infra queda pendiente de aplicar por CI, y es el workflow `Infra CD` (job `apply`, #197) quien cierra el issue tras un apply exitoso -- CA-3. El pipeline corre sin `az login` ni sesion de Azure; `terraform` se usa solo offline para `fmt`/`validate` -- CA-6. `commands/infra.md` documenta el flujo nuevo (escribir/revisar HCL local sin plan ni apply; plan-en-PR y apply-en-merge los hace CI) y la distincion bootstrap (privilegiado, una vez) vs flujo ongoing (cero permisos), reemplazando la seccion "Flujo preview -> apply" -- CA-5. `agents/infra-bootstrap.md` y el paso 4 del greenfield en `README.md` se actualizan para no invocar los flags eliminados y describir el nuevo flujo (no forman parte del alcance de archivos del issue -- que acotaba `README.md` al conteo de agentes --, pero ambos invocaban directamente los flags retirados `--skip-apply`/`--from-stage 3`; dejarlos intactos habria dejado instrucciones que ahora abortan). Archivos modificados: `scripts/iac-pipeline.sh`, `agents/infra-reviewer.md`, `commands/infra.md`, `commands/work-status.md`, `agents/infra-bootstrap.md`, `CLAUDE.md`, `README.md`. Archivo eliminado: `agents/infra-applier.md`.

### Added

- **`/onboard` cierra el reporte con un bloque "Proximos pasos" contextual y un puntero al quickstart greenfield (issue #221, #222, ADR-0007, ADR-0021, ADR-0022, ADR-0025)**: el checklist terminaba en el resumen OK/FALTA/NO VERIFICADO y las "Acciones sugeridas" por item, sin una guia de que hacer a continuacion ni una via para descubrir la narrativa de arranque greenfield sin salir de la sesion de Claude Code. **CA-1**: nuevo bloque **"Proximos pasos"** al cierre del diagnostico que, segun que reporto cada seccion (config, tokens, labels, CI, estructura/infra-base), imprime el comando exacto a correr a continuacion -- labels: `setup-github-labels.sh` o el paso opt-in; CI: `setup-github-ci.sh`; sin infra-base: `/mefisto:infra-base dev` --, derivado de los mismos flags que ya fija el diagnostico (`PA_CONFIG_FALTA`, `PA_TOKENS_FALTA`, `PA_LABELS_FALTA`, `PA_CI_FALTA`, `PA_INFRA_BASE_MISSING`) sin re-diagnosticar nada. **CA-2**: el bloque cierra con un puntero descubrible a `docs/greenfield-quickstart.md` (issue #221) -- resuelto contra la URL estable de GitHub (`homepage` de `plugin.json` + `/blob/main/...`, con fallback hardcodeado si `jq`/`plugin.json` no estan disponibles) para que funcione aunque `docs/` no viaje en el cache del plugin. **CA-3**: el bloque es puramente informativo -- solo `echo`, ninguna escritura ni invocacion de `gh`/`az`; las provisiones opt-in (pasos 3 y 4) siguen intactas. **CA-4**: cuando el diagnostico sale LISTO, sugiere `/mefisto:scaffold <dominio>` (o `/mefisto:infra-base dev` si la infraestructura base aun no existe) en vez de un cierre seco, mas un recordatorio recurrente de la siembra de secretos en Key Vault (ADR-0025). **CA-5**: se preserva el guard `cwd != Mefisto`, las secciones 1-5 del diagnostico y las provisiones opt-in; el paso 2 del skill ("Presentar el resultado") deja de recalcular su propia sugerencia de siguiente paso y solo presenta el bloque que ya imprimio el diagnostico, para no duplicar el mensaje. Verificado con corridas simuladas de un consumidor greenfield en tres estados (todo NO VERIFICADO, tokens FALTA, labels+CI FALTA) y uno LISTO con `gh`/`az` mockeados. Archivo modificado: unicamente `commands/onboard.md`.

- **`docs/greenfield-quickstart.md`: narrativa corta del arranque greenfield (issue #221, ADR-0021, ADR-0022, ADR-0025)**: el flujo greenfield ya existía exhaustivo en `README.md` (sección "Primeros pasos con el harness (greenfield)", ~150 líneas), pero un consumidor nuevo no tenía una vía corta para entender el arranque sin leerse el README entero. **CA-1**: nuevo `docs/greenfield-quickstart.md`, formato narrativo ("explicáselo a un compañero"), no una copia ni un resumen línea-a-línea de la sección del README. **CA-2**: explica el modelo de dos roles operativos -- admin/infra (bootstrap privilegiado + siembra de secretos) vs dev ongoing (cero credenciales de Azure) -- enlazando a ADR-0025 decisión #10 en vez de re-enunciar la regla de los tres perfiles de acceso. **CA-3**: cubre los 10 pasos de la secuencia de arranque en orden (scope `user` -> config + tokens -> labels -> backend del tfstate -> CI-OIDC -> secrets manuales de GitHub -> infra-base -> primer `/infra` -> siembra de Key Vault -> scaffold + primer ciclo TDD) en una tabla que marca, por fila, si lo corre el rol admin/infra o cualquier dev. **CA-4**: no re-enuncia reglas de ADR-0021/0022/0025 ni duplica la sección del README -- cada regla se referencia por enlace. **CA-5**: enlaces bidireccionales -- el README suma un aviso al inicio de la sección greenfield apuntando al quickstart, y el quickstart enlaza de vuelta a la sección del README y a los tres ADRs. Archivos modificados: `README.md` (solo el enlace). Archivo creado: `docs/greenfield-quickstart.md`.

- **`/onboard` diagnostica los tokens de `CLAUDE.md` y la estructura de carpetas esperada (issue #212)**: el checklist cubria solo config, labels y CI, dejando fuera dos prerequisitos obligatorios del contrato con el consumidor (`CLAUDE.md` del harness, seccion "Contrato con el proyecto consumidor", puntos 2 y 3) -- un consumidor podia pasar `/onboard` en verde y aun asi fallar en el primer skill por falta de tokens o de estructura. (1) **CA-1**: nuevo bloque que grepea las 5 etiquetas de la seccion "Tokens del harness" del `CLAUDE.md` raiz del consumidor (`RootNamespace`, `SolutionFile`, `ProjectDisplayName`, `BoundedContext`, `BoundedContextDomains`) y reporta `OK`/`FALTA`/`NO VERIFICADO` (este ultimo si no hay `CLAUDE.md` legible); es un artefacto separado de `harness.config.json` que hoy nadie verificaba. (2) **CA-2**: nuevo bloque informativo (no bloqueante) que reporta la presencia de `src/`, `tests/` e `infra/environments/` sin marcarlas `FALTA` en un greenfield que aun no hizo su primer `/scaffold` -- evita el falso negativo; las carpetas ausentes caen en `NO VERIFICADO` para no inflar el contador de `FALTA`, verificado con una corrida real en un repo greenfield simulado. (3) **CA-3**: ambos bloques reusan el helper `row()` y los contadores `N_OK`/`N_FALTA`/`N_NV` existentes, sin escribir ni modificar ningun archivo. (4) **CA-4**: la seccion "Que verifica" pasa de 3 a 5 diagnosticos. Se agrega ademas una regla explicita en "Reglas" documentando el caracter informativo de la estructura de carpetas. Archivo modificado: unicamente `commands/onboard.md`.

- **`infra-base-scaffolder` emite el workflow de CI de Terraform `infra-cd.yml` de forma idempotente (issue #197, ADR-0021, ADR-0022)**: materializa el mecanismo concreto que ADR-0021 (CA-3) y ADR-0022 dejaban remitido a este issue. Nuevo **Paso 2b** entre el esqueleto del entorno y `terraform fmt`/`validate`: genera `.github/workflows/infra-cd.yml` **solo si no existe** (mismo patron de idempotencia que los workflows de smoke-tests del `domain-scaffolder`, Paso 6 -- CA-1), con dos jobs siguiendo el modelo plan-en-PR/apply-en-merge-a-main de HashiCorp: `plan` (`pull_request` sobre `infra/**`, `terraform plan` publicado como comentario del PR via `actions/github-script`, nunca aplica -- CA-2) y `apply` (`push` a `main` sobre `infra/**`, `terraform apply -auto-approve` -- CA-3). Ambos jobs se autentican por OIDC (`permissions: id-token: write` + `contents: read`, `azure/login@v3` con `client-id`/`tenant-id`/`subscription-id`, `ARM_USE_OIDC=true` y los tres secrets `AZURE_CLIENT_ID`/`AZURE_TENANT_ID`/`AZURE_SUBSCRIPTION_ID`, sin `AZURE_CREDENTIALS` ni access keys) y heredan el backend keyless por AAD (`use_azuread_auth`, #198) via las mismas variables `ARM_*` -- CA-4. Tras un `apply` exitoso, el job cierra el issue de infra correspondiente (`gh issue close` con comentario) derivando el numero de la rama `infra-issue-<num>-*` (convencion ya vigente en `scripts/iac-pipeline.sh`) del PR mergeado, resuelto via `GET /repos/{owner}/{repo}/commits/{sha}/pulls` -- robusto a la estrategia de merge (squash/merge/rebase), a diferencia de parsear el mensaje del commit; el PR del pipeline IaC no lleva `Closes #N` (ADR-0022, "infra aplicada" != "infra mergeada") -- CA-5. Se agrega la regla absoluta 10 (nunca sobrescribir `infra-cd.yml`) y se actualizan Paso 4 (commit condicional del workflow, primera corrida) y Paso 5 (reporte: archivo creado/omitido, secrets requeridos, siguiente paso via PR+CI en vez de apply local). Archivo modificado: unicamente `agents/infra-base-scaffolder.md`.

### Changed

- **ADR-0025 nombra la siembra/custodia de secretos de Key Vault como un tercer perfil de acceso, distinto del bootstrap (issue #211, ADR-0022, ADR-0024)**: ADR-0022 (seccion "El apply de infraestructura ocurre en CI, nunca localmente") y ADR-0025 (decision #7) enmarcaban el "cero permisos"/"cero credenciales" del desarrollador ongoing con **una unica excepcion**, el bootstrap de una sola vez -- sin nombrar el privilegio de infra/admin **recurrente** que ya describian en prosa suelta (decision #6, "el valor lo coloca infra/admin de forma administrativa"; decision #9, la fuente del password de Postgres) sin ubicarlo como un perfil de acceso propio. **CA-1**: se agrega la **decision #10** en ADR-0025 ("Tres perfiles de acceso a Azure del marco"), con una tabla que distingue (a) desarrollador ongoing (cero credenciales, decision #7), (b) bootstrap (admin, una sola vez) y (c) siembra/custodia de secretos de Key Vault (infra/admin, **recurrente** -- se repite tras cada `apply` que crea un secreto y con cada alias nuevo en `serviceBus.external`). Se ajusta la decision #7 y la seccion equivalente de ADR-0022 para remitir a la #10 en vez de enmarcar el bootstrap como la unica excepcion (doctrina no-obsolete: nada nuevo que marcar "obsoleto" en el cuerpo, solo se deja de implicar exhaustividad donde no la habia). **CA-2**: la decision #10 suma el inventario de la siembra -- que secreto (`serviceBus.internal.secretName`, `marten-connection`, `app-insights-connection`, cada `serviceBus.external[].secretName` con `alcance: "compartido"`), quien (infra/admin), cuando (tras el primer `apply` que lo crea + cada alias external nuevo) y por que `marten-connection` y el `serviceBus.external[]` compartido no son derivables de `terraform output` (el primero porque el password de Postgres es un **input** del admin, ADR-0025 decision #9, nunca un output del state; el segundo porque el ASB del backbone compartido lo administra otro equipo, ADR-0024 decision #4, fuera de este state). `README.md` suma el **paso 5** ("Sembrar los secretos de Key Vault") dentro de "Bootstrap de infraestructura", con la misma tabla y el mismo criterio de derivabilidad, y corrige la "regla mnemonica" final de la seccion "Que corre donde" (que tambien enmarcaba el bootstrap como la unica excepcion). **CA-3**: ambos ADRs registran el cambio solo en "Control de cambios", con fecha y motivo; no se elimino ninguna decision existente, solo se dejo de implicar que el bootstrap agotaba las excepciones. Alcance deliberadamente **no arquitectural** (opcion A del issue): documenta y enmarca, sin mecanizar la siembra -- la mecanizacion parcial de lo derivable queda como decision futura separada, pendiente de sign-off por el trade-off de RBAC de escritura sobre Key Vault que exigiria. Archivos modificados: `docs/adr/0022-autenticacion-ci-azure-oidc.md`, `docs/adr/0025-custodia-de-secretos.md`, `README.md`.

- **Documentacion y onboarding reflejan el modelo de apply de infraestructura en CI (issue #200, ADR-0021, ADR-0022, ADR-0025)**: barrido final de la oleada "el apply de infra ocurre en CI" (#196..#199, #195) -- parte de la prosa publicada aun describia (o dejaba ambiguo) que `/infra` ejecutaba el `apply` localmente. En `README.md` se refuerza la nota del paso "Primer `/infra`" con la doctrina explicita (el dev no aplica ni planifica local, no necesita `az login` con permisos elevados ni acceso al tfstate) y se agrega una fila a la tabla "Que corre donde": `terraform plan` (en cada PR) y `terraform apply` (al mergear a `main`) corren en el runner de GitHub Actions via `infra-cd.yml`, nunca en la maquina del desarrollador -- salvo el bootstrap inicial, que sigue siendo la excepcion privilegiada de una sola vez. `commands/infra-base.md` deja de decir que "el primer `/infra` aplica": ahora documenta que `/infra-base` tambien genera `.github/workflows/infra-cd.yml` y que el `apply` real lo ejecuta CI al mergear el PR. Se corrige la misma frase residual en la descripcion introductoria de `agents/infra-base-scaffolder.md` (no listado en el alcance de archivos del issue, pero con el mismo residuo textual que #199 dejo sin tocar). `commands/health-check.md` suma un paso que consulta `gh run list --workflow=infra-cd.yml` sobre `main` y reporta el estado del ultimo apply en el dashboard (semaforo verde/amarillo/rojo, tolerante a "sin runs" en un proyecto greenfield). `commands/onboard.md` (roles ampliados de `setup-github-ci.sh`, issue #195) y `agents/infra-bootstrap.md` (sin los flags retirados por #199) ya reflejaban el modelo nuevo -- se verificaron sin requerir cambios adicionales. En la revision del barrido se detectaron tres residuales mas del modelo local (CA-5) que ninguna oleada previa habia tocado: (1) en `agents/domain-scaffolder.md` los "Proximos pasos" que imprime tras el scaffold instruian `terraform apply` local (contradiciendo la propia nota del archivo sobre `infra-cd.yml`/`workflow_run`) -- ahora indican abrir un PR para que CI aplique al mergear a `main`; (2) `scripts/scaffold-pipeline.sh` imprimia el mismo paso de `terraform apply` local tras crear el PR del scaffold -- ahora reordena los proximos pasos (secrets OIDC primero, porque CI los necesita para aplicar) y aclara que el apply/deploy los hace CI al mergear; y (3) el template de issues `tipo:infra` del `agents/planner.md` listaba `terraform plan`/`terraform apply` como criterios de aceptacion sin ubicarlos en CI -- ahora los CA-2/CA-3 apuntan al plan-en-PR y apply-en-merge del workflow Infra CD. Archivos modificados: `README.md`, `commands/infra-base.md`, `commands/health-check.md`, `agents/infra-base-scaffolder.md`, `agents/domain-scaffolder.md`, `agents/planner.md`, `scripts/scaffold-pipeline.sh`.

- **`scripts/setup-github-ci.sh` eleva el principal de CI para que pueda aplicar infraestructura (issue #195, ADR-0022, ADR-0025)**: cierra los tres gaps que dejaba el modelo de solo-deploy-de-codigo, ahora que el `terraform apply` migra a CI (issue #196). (1) Se agrega el rol integrado `Role Based Access Control Administrator` a nivel **suscripcion** (mismo scope que `Contributor`), con una **condicion ABAC (version 2.0)** que excluye que el SP pueda asignar los roles privilegiados `Owner`, `User Access Administrator` o el propio `Role Based Access Control Administrator` -- plantilla integrada de Azure "Allow all except specific roles" ([Delegate Azure role assignment management with conditions, Microsoft Learn](https://learn.microsoft.com/azure/role-based-access-control/delegate-role-assignments-examples#example-allow-most-roles-but-dont-allow-others-to-assign-roles)); sin este rol, `Contributor` excluye explicitamente `Microsoft.Authorization/roleAssignments/write` ([Azure built-in roles, Microsoft Learn](https://learn.microsoft.com/azure/role-based-access-control/built-in-roles/general#contributor)) y el `apply` de CI fallaria con `AuthorizationFailed` al crear los role assignments que emiten los scaffolders (Key Vault Secrets User, roles de datos de Storage; ADR-0025). Los IDs de los tres roles se resuelven por nombre (`az role definition list`), no se hardcodean. (2) El rol sobre la Storage Account del tfstate pasa de `Storage Blob Data Reader` a `Storage Blob Data Contributor` (sobre la cuenta REAL resuelta con el sufijo de unicidad, issue #92): con el backend keyless por AAD (`use_azuread_auth`, issue #198) el `apply` necesita escribir el state y tomar su lease/lock, no solo leerlo. (3) Se agrega un segundo federated credential con subject `repo:<owner/repo>:pull_request` (ademas del existente `ref:refs/heads/main`): el `terraform plan` que corre en cada PR (modelo plan-en-PR/apply-en-merge-a-main) recibe un claim OIDC distinto que no matchea el subject de push a `main`. Todas las asignaciones y credenciales son idempotentes (mismo patron ya vigente en el script). La salida final del script se actualiza para reportar los tres roles con su scope/condicion, ambos subjects de federated credential y por que el SP necesita cada permiso. Archivo modificado: unicamente `scripts/setup-github-ci.sh`.

- **`scripts/bootstrap-backend.sh` genera el backend de Terraform keyless por AAD (issue #198, ADR-0025, ADR-0022)**: el bloque `backend "azurerm"` que el script escribe en `infra/environments/<env>/backend.tf` y el que imprime al final (bloque de confirmacion) no llevaban `use_azuread_auth`, con lo que Terraform accedia al blob del tfstate via access key (`listKeys`) -- contrario a la doctrina de ADR-0025 (ningun secreto/key en texto plano) fijada por el issue #196. Se agrega `use_azuread_auth = true` a ambos bloques, que quedan identicos entre si (CA-1, CA-2), de modo que tanto el `plan` local como el `apply` de CI acceden al tfstate por AAD/RBAC. La creacion idempotente de Resource Group/Storage Account/container no cambia y sigue sin emitir ni persistir ningun access key (`--auth-mode login`, CA-3). La salida final del script suma un aviso: el backend es keyless y el principal que aplica en CI requiere `Storage Blob Data Contributor` (lectura+escritura, no solo lectura) sobre la Storage Account del tfstate -- ese cambio de rol lo asigna `scripts/setup-github-ci.sh` en el issue #195, acoplado pero no incluido aqui (CA-4). Archivo modificado: unicamente `scripts/bootstrap-backend.sh`.

- **`domain-scaffolder` encadena `deploy-{kebab}.yml` tras `infra-cd.yml` para garantizar el orden infra->deploy (issue #197, ADR-0022, CA-6)**: con el `apply` de infra migrado a CI (issue #196), `infra-cd.yml` y `deploy-{kebab}.yml` pueden correr ambos en un `push` a `main`, sin orden garantizado entre ellos. Se elimina `infra/environments/dev/**` de los `paths` del trigger `push` del deploy (que conserva el disparo directo por `src/<RootNamespace>.{PascalCase}/**` y `Contracts/**` para cambios de solo codigo) y se agrega un trigger `workflow_run` que encadena tras el workflow `Infra CD`. Un `workflow_run` sin mas se dispararia tras **cada** `apply` de infra y redesplegaria **todos** los dominios (seguro por idempotencia, pero costoso); se agrega el job `determinar-alcance`, que solo continua si la corrida de `Infra CD` fue el job `apply` (rama `main`, no el `plan` de una PR de infra sin mergear) y concluyo en `success`, y ademas el PR mergeado toco `src/<RootNamespace>.{PascalCase}/**` de ese dominio -- resuelto via la misma API `commits/{sha}/pulls` + `pulls/{num}/files` que usa `infra-cd.yml` para el cierre de issue, robusta a la estrategia de merge. Los jobs `build-and-test` y `deploy` quedan condicionados a `needs.determinar-alcance.outputs.debe_desplegar == 'true'`, y su `actions/checkout` fija el `ref` a `github.event.workflow_run.head_sha` (con fallback a `github.sha`) para desplegar exactamente el commit que disparo el `apply`, no la punta de `main` en el momento del `workflow_run`. Se documenta el caso limite de un `apply` sin PR asociado (push directo a `main` fuera de `scripts/iac-pipeline.sh`): el redeploy se omite por diseno y requiere `workflow_dispatch` manual. Archivo modificado: unicamente `agents/domain-scaffolder.md`.

- **ADR-0021, ADR-0022 y ADR-0025 fijan que el `apply` de infraestructura ocurre en CI bajo identidad federada, nunca localmente (issue #196)**: reforma doctrinal ancla de la oleada que invierte el modelo actual -- `scripts/iac-pipeline.sh` Stage 3 corria `terraform apply` con las credenciales de Azure del desarrollador, exigiendole permisos elevados (Contributor + `roleAssignments/write` + acceso a secretos) en su propia maquina. **ADR-0022** (CA-1, CA-2) fija el modelo plan-en-PR / apply-en-merge-a-main (guia de HashiCorp), amplia los roles del Service Principal de CI (`Role Based Access Control Administrator` con condicion anti-escalacion a nivel suscripcion -- justificada por el problema de huevo-y-gallina de acotar al resource group del BC, que aun no existe en el bootstrap -- y `Storage Blob Data Contributor` en vez de `Storage Blob Data Reader` sobre el tfstate) y agrega el federated credential de subject `pull_request` (distinto del de `ref:refs/heads/main`) para el job de `plan` en pull requests; se elimina del cuerpo la afirmacion de que ese subject "queda fuera del workflow que emite el marco". **ADR-0021** (CA-3) documenta que el `infra-base-scaffolder` tambien emite el workflow de CI de Terraform (`infra-cd.yml`), analogo a como el `domain-scaffolder` emite los workflows de smoke-tests (ADR-0013), y elimina del cuerpo la afirmacion de que `/infra aplica` localmente. **ADR-0025** (CA-4) refleja que el desarrollador no custodia secretos de Azure en local (consecuencia de que el `apply` migra a CI) y que el backend del tfstate es keyless (`use_azuread_auth`), coherente con el principio de "ningun secreto ni key en texto plano". Ambos ADRs fijan (CA-5) que el issue de infra se cierra al completar el `apply` de CI exitosamente, no al mergear el PR -- continuidad de la doctrina de cierre del flujo preview-apply (issue #96), solo cambia quien ejecuta el cierre -- y (CA-6) el principio de que el deploy de codigo de un dominio no debe correr antes que el `apply` de infra que crea su Function App, remitiendo el mecanismo concreto al issue #197. Es el issue ancla de la oleada: #197 (workflow de CI), #198 (backend keyless), #195 (RBAC ampliado del SP), #199 (rediseno de `iac-pipeline.sh`/`infra-applier`) y #200 (barrido de docs) materializan lo que aqui queda fijado como doctrina. Archivos modificados: `docs/adr/0022-autenticacion-ci-azure-oidc.md`, `docs/adr/0021-infraestructura-base.md`, `docs/adr/0025-custodia-de-secretos.md`.

## [0.9.0] - 2026-07-01

### Added

- **`infra-base-scaffolder` custodia Postgres y App Insights por Key Vault y Storage por identidad (issue #182, ADR-0025)**: cierra la brecha que la auditoria posterior a ADR-0024 encontro -- el password de PostgreSQL (embebido en `MartenConnectionString`), la access key de la Storage Account del host (`AzureWebJobsStorage`) y la connection string de Application Insights (con su instrumentation key) seguian en texto plano. Es el **issue ancla** de la oleada de ADR-0025: fija los nombres de secreto y los roles de Storage que el `domain-scaffolder` (issue derivado) debe consumir sin reinventarlos. Cambios en `agents/infra-base-scaffolder.md`: (1) el modulo `function-app` (Paso 1.7) elimina las variables `storage_account_access_key` y `storage_account_connection_string` (la segunda, muerta -- no se usaba en el recurso -- y con un access key embebido, contrario al principio de la decision #1) y activa `storage_uses_managed_identity = true` en `azurerm_linux_function_app`, validado contra el schema real del provider `azurerm` (`terraform validate` con el provider instalado, sin warnings nuevos) -- CA-1. (2) `var.app_insights_connection_string` del mismo modulo deja de recibir el valor literal: su descripcion documenta que ahora es la referencia `@Microsoft.KeyVault(SecretUri=...)` versionless al secreto `app-insights-connection` -- CA-2. (3) el modulo `key-vault` (Paso 1.8) se reencuadra de "custodia de cadenas de ASB" a **almacen general de secretos del BC** (ADR-0025 decision #5); sigue sin crear secretos -- CA-4. (4) se agrega la seccion **"Convencion anclada: secretos de Key Vault y roles de Storage"** tras el Paso 1.8: fija los nombres fijos `marten-connection` y `app-insights-connection` (uno por BC, fuera de `harness.config.json` -- a diferencia del registro `serviceBus`, no hay eleccion que delegar al consumidor) y los tres roles de datos de Storage (`Storage Blob Data Owner`, `Storage Queue Data Contributor`, `Storage Table Data Contributor`) que el `domain-scaffolder` asigna via `azurerm_role_assignment` a la managed identity de cada Function App -- CA-6. (5) el esqueleto del entorno (Paso 2.3) suma los locals `marten_connection_kv_ref` y `app_insights_connection_kv_ref`, analogos a `service_bus_connection_interno_kv_ref`, apuntando a `${module.key_vault.uri}secrets/marten-connection` y `.../app-insights-connection` -- CA-3. (6) `outputs.tf` (Paso 2.4) suma `postgresql_database_name`, `postgresql_administrator_login` y `app_insights_connection_string` (sensitive), todos de **uso administrativo**: los datos crudos que un admin necesita para construir y sembrar (`az keyvault secret set`) los secretos `marten-connection` y `app-insights-connection` tras el primer `apply` -- Terraform nunca escribe su valor -- CA-5. (7) el Paso 5 (reporte) y las Reglas absolutas (nueva regla 9) documentan la accion administrativa pendiente y prohiben pasar de vuelta una access key de Storage al modulo `function-app`. Las cadenas de ASB (`SERVICE_BUS_CONNECTION_*`, ADR-0024) no se tocan -- CA-7. Archivo modificado: unicamente `agents/infra-base-scaffolder.md`.

- **Modulo Key Vault en la infraestructura base y enrutamiento de la cadena de ASB interna por referencia `@Microsoft.KeyVault(...)`** (issue #170): materializa ADR-0024 decision #6 (custodia de cadenas de conexion) en `agents/infra-base-scaffolder.md`. Hasta ahora la infra base no provisionaba ningun Key Vault: las cadenas de Azure Service Bus quedaban en texto plano vueltas app setting por Terraform. Se agrega el **octavo modulo compartido** `infra/modules/key-vault/main.tf` (CA-1): `azurerm_key_vault` con `enable_rbac_authorization = true` (modelo de permisos por rol, nunca access policies) y `prevent_destroy = true`; el modulo **no crea secretos** — el valor de cada cadena lo coloca infra/admin de forma administrativa (`az keyvault secret set`), nunca Terraform (CA-4), para que el valor nunca quede materializado en el state del Key Vault. El esqueleto del entorno (Paso 2.3) instancia `module "key_vault"` con nombre `kv-${var.project_short}-${sufijo}` — el patron omite `environment` porque el limite de nombre de Key Vault (3-24 chars) es el mas estrecho de los cuatro recursos con sufijo de unicidad global (Postgres, dos namespaces ASB, Key Vault) y no le alcanza el prefijo completo — y define, a partir de `serviceBus.internal.secretName` (contrato #163) resuelto en el Paso 0, el local `service_bus_connection_interno_kv_ref` con la referencia **versionless** `@Microsoft.KeyVault(SecretUri=<vault_uri>secrets/<secretName>)` (sin sufijo de version, para tomar la ultima al rotar) y el mapa `service_bus_connection_external_kv_refs` (una entrada por alias de `serviceBus.external[]`) (CA-2, CA-5). Se documenta, con el mismo patron de comentario ya usado para el RBAC Data Sender de ASB, que el `domain-scaffolder` agrega por cada Function App el `azurerm_role_assignment` **Key Vault Secrets User** sobre `module.key_vault.id` y consume esos locals (en vez del valor en claro de `module.service_bus_interno.default_primary_connection_string`) como app setting `SERVICE_BUS_CONNECTION_<ALIAS>` (CA-3): el Key Vault en si (RBAC habilitado) no otorga permisos por si solo sin ese role assignment. Se agregan los outputs raiz `key_vault_name`/`key_vault_uri` y se actualiza la descripcion de `service_bus_interno_connection_string` para dejar claro su uso administrativo (sembrar el secreto) en vez de valor de app setting directo. El conteo de modulos base pasa de 7 a 8 en `agents/infra-base-scaffolder.md`, `commands/infra-base.md`, `CLAUDE.md`, `README.md` y `agents/infra-writer.md` (la lista de modulos base reutilizables que lee el `infra-writer`). Secuenciado antes de #165 (ambos editan el mismo archivo): #165 elimina el namespace de integracion `sbext-*`; #166 (domain-scaffolder) consumira los locals y anadira el role assignment concreto por dominio, referenciando esta base sin duplicarla. Queda pendiente para esos sucesores actualizar el conteo/tabla de contrato en `docs/adr/0021-infraestructura-base.md` (aun en 7 modulos, sin fila de `key-vault`) y la nota de infraestructura base en `agents/domain-scaffolder.md` (co-editada por #165 y #166). Archivos modificados: `agents/infra-base-scaffolder.md`, `commands/infra-base.md`, `CLAUDE.md`, `README.md`, `agents/infra-writer.md`.

- **Registro `serviceBus` con alcance de ASB (propio/compartido/externo) en el contrato de `harness.config.json`** (issue #163): fija el contrato ancla de la oleada de ADR-0024 (#164/#165/#166/#170/#158) para declarar los Azure Service Bus que un Bounded Context toca. ADR-0024 clasifico los ASB por alcance (propio del BC, compartido del producto, verdaderamente externo diferido) pero el harness no tenia donde nombrarlos declarativamente. Se agrega el registro `serviceBus` (opcional, ADR-0024 decisiones #1 y #6) con `internal.secretName` (nombre del secreto de Key Vault de la cadena del ASB propio del BC, alias reservado `INTERNO`, obligatorio si se declara el registro) y `external` (lista opcional de entradas `{ alias, alcance, secretName }`, con `alcance` en `{compartido, externo}`); la cadena de conexion **nunca** aparece en el JSON, solo el nombre del secreto (CA-1, CA-5). `load_harness_config` en `scripts/_pipeline-common.sh` lee el registro con `jq`, valida su forma (secreto interno no vacio; cada entrada externa con alias/alcance/secretName no vacios y alcance en el enum; aliases unicos; `INTERNO` reservado y no reutilizable en `external`, comparacion case-insensitive) y aborta temprano con mensaje accionable si es invalido, o exporta `HARNESS_SB_INTERNAL_SECRET` y tres listas paralelas separadas por espacios en el mismo orden posicional (`HARNESS_SB_EXTERNAL_ALIASES`, `HARNESS_SB_EXTERNAL_ALCANCES`, `HARNESS_SB_EXTERNAL_SECRETS`); el registro completo (y por tanto `external`) es opcional, por lo que un consumidor que aun no lo declara sigue cargando su config sin error (CA-2, CA-3). Se fija y documenta en `CLAUDE.md` el patron oficial `SERVICE_BUS_CONNECTION_<ALIAS>` (con `INTERNO` como alias reservado y la clave de broker de Wolverine igual al alias), que reemplaza al viejo `SERVICE_BUS_CONNECTION_INTEGRACION` y que consumen `implementer`, `infra-base-scaffolder`, `domain-scaffolder` y el issue de Key Vault sin reinventarlo (CA-4). El alcance verdaderamente externo se soporta declarativamente pero su wiring/provision queda diferido y default-off (ADR-0024 decision #5): el contrato solo lo nombra (CA-6). La suite `scripts/tests/test-harness-config.sh` se extiende con la cobertura SB-1..SB-9 (ausencia opcional, secreto interno obligatorio, forma valida con exports paralelos, y las cinco formas de malformacion que abortan). De paso se corrige el indice tematico de ADRs en `CLAUDE.md`: se agrega la fila de ADR-0024 (faltaba desde su creacion) y se actualiza la de ADR-0023 al titulo vigente ("namespace interno de ASB y frontera publico/privado", tras la enmienda de issue #161), evitando que el indice contradiga al registro `serviceBus` recien documentado. Archivos modificados: `scripts/_pipeline-common.sh`, `CLAUDE.md`, `scripts/tests/test-harness-config.sh`.

- **Bounded Context en el contrato de config del consumidor** (issue #131): introduce el concepto de BC (ADR-0023) en `harness.config.json` y en la logica de parseo del harness. El campo `boundedContext: { name, domains }` es ahora **obligatorio**; `load_harness_config` en `scripts/_pipeline-common.sh` lo lee, valida y exporta `HARNESS_BC_NAME` y `HARNESS_BC_DOMAINS`. Validaciones: `name` de 1-63 chars alfanumericos y guiones; `domains` array no vacio donde cada elemento debe estar en `domainLabels`. Guia de migracion accionable para consumidores legacy: cuando el campo esta ausente, `load_harness_config` aborta con el shape exacto a anadir y un ejemplo concreto con los `domainLabels` existentes; `/onboard` detecta la ausencia y lo reporta en su checklist con instrucciones de correccion; el README incorpora la seccion "Migracion para consumidores existentes". Se documenta la relacion BC-resource-group (`infraResourceGroupPrefix`+`-`+`name`) en `CLAUDE.md` y `README.md`. El context map (registro de BCs externos) queda explicitamente diferido a sucesores. La suite `scripts/tests/test-harness-config.sh` se extiende con cobertura de la validacion de `boundedContext` (ausencia, name invalido, domains vacio o fuera de `domainLabels`, exports). Archivos modificados: `scripts/_pipeline-common.sh`, `CLAUDE.md`, `commands/onboard.md`, `README.md`, `scripts/tests/test-harness-config.sh`.

- **ADR-0023: Bounded Context, namespace interno de Azure Service Bus y frontera publico/privado** (issues #121, #161): crea la raiz estrategica de mensajeria que unifica las decisiones satelite del harness (ADR-0001/0003/0005/0012/0020/0021/0022). Establece los puntos canonicos vigentes: (1) **BC = grupo de dominios + resource group; dominio = Function App** (ADR-0020); el BC es la unidad de agrupacion superior al dominio. (2) **Un namespace interno de Azure Service Bus por BC** para los eventos privados intra-BC (`IPrivateEventSender`); lo publico viaja por el backbone compartido del producto (caso comun) o por integracion externa (caso diferido), segun ADR-0024. (3) **Todo lo que cruza un bus es plano y portable**: el modelo rico (value objects, `ConfigurarSerializacion`) vive exclusivamente en el event store de Marten y se traduce a una forma plana al publicar, sea cual sea el destino (ADR-0012). (4) **Publico vs privado = enrutamiento por frontera de BC, no forma del payload**: privado si lo consume un dominio del mismo BC, publico si lo consume un dominio de otro BC. (5) **Frontera de seguridad por topologia**: el namespace interno no es alcanzable desde fuera del BC; el RBAC least-privilege por entidad queda como norte diferido, junto con el Context Map y la integracion externa entrante (Open Host Service + Published Language, Evans, *DDD* 2003, cap. 14). Se indexa ADR-0023 en la tabla de ADRs de `CLAUDE.md`.

### Changed

- **`agents/domain-scaffolder.md` consume las referencias de Key Vault y el storage por identidad de la infra base (issue #183, ADR-0025)**: cierra la oleada de ADR-0025 consumiendo, sin reinventarla, la convencion anclada por el issue #182 (`infra-base-scaffolder`). El scaffolder emitia el `module function_app_{snake_case}` (Paso 4) con tres secretos en texto plano: el password de PostgreSQL embebido en `MartenConnectionString`, la access key de Storage (`storage_account_access_key`) y la connection string de Application Insights en claro. Cambios: (1) `MartenConnectionString` deja de interpolar `Password=${var.postgresql_admin_password}` y pasa a `local.marten_connection_kv_ref` -- la referencia `@Microsoft.KeyVault(...)` versionless del secreto `marten-connection` que provisiona la infra base -- CA-1. (2) `app_insights_connection_string` deja de recibir `module.monitoring.connection_string` y pasa a `local.app_insights_connection_kv_ref` (secreto `app-insights-connection`) -- CA-2. (3) se eliminan los inputs `storage_account_access_key` y `storage_account_connection_string` del `module function_app_{snake_case}` (el modulo `function-app` de #182 ya no los acepta; resuelve `AzureWebJobsStorage` internamente via `storage_uses_managed_identity = true`) -- CA-3. (4) se agregan los tres `azurerm_role_assignment` de datos de Storage (`Storage Blob Data Owner`, `Storage Queue Data Contributor`, `Storage Table Data Contributor`) sobre `module.storage_{snake_case}.id` con `principal_id` = la managed identity de la Function App, mismo patron que ya usaba para "Key Vault Secrets User" -- CA-4. (5) el role assignment `Key Vault Secrets User` se conserva sin cambios de mecanica; su comentario deja de nombrar solo `SERVICE_BUS_CONNECTION_*` y ahora cubre tambien `MartenConnectionString` y `APPLICATIONINSIGHTS_CONNECTION_STRING` -- CA-5. (6) el bloque `app_settings` final queda sin ningun secreto literal: solo referencias KV y `DOMINIO`; las cadenas de ASB conservan intacto su wiring de ADR-0024 -- CA-6. Se agrega ademas una nota "modulo function-app" (paralela a la ya existente de "modulo service-plan") que documenta el contrato nuevo del modulo y su ruta de fallback si el consumidor tiene una version heredada, y se actualizan la nota de infraestructura base y el resumen final del scaffold para reflejar los nuevos locals y role assignments. El `MartenConnectionString` de `local.settings.json` (desarrollo local) no se toca: sigue siendo un placeholder de dev, no un app setting de Azure. Archivo modificado: unicamente `agents/domain-scaffolder.md`.

- **`agents/implementer.md` enuncia la doctrina general de custodia de secretos nuevos (issue #185, ADR-0025)**: el implementer ya documentaba la custodia de las cadenas de ASB por Key Vault (seccion "Infraestructura", ~linea 280 y 704-709) pero no generalizaba esa doctrina a un secreto arbitrario nuevo (API key de terceros, token, credencial) ni mencionaba la identidad administrada como mecanismo alterno. Se agrega la seccion **"Custodia de secretos nuevos (ADR-0025)"** entre "Infraestructura (topics y subscriptions)" y "Proceso": cualquier secreto nuevo va por referencia de Key Vault (por defecto) o por identidad administrada (cuando el runtime lo exige antes de resolver referencias, ej. `AzureWebJobsStorage`); nunca en texto plano en un app setting ni en el state de Terraform -- CA-1. Se reafirma el reparto de responsabilidades ya vigente para ASB: el implementer no emite referencias `@Microsoft.KeyVault(...)` ni role assignments, eso lo provisionan `infra-base-scaffolder`/`domain-scaffolder`; si la custodia de un secreto nuevo aun no existe, se documenta en "Infraestructura modificada" del resumen para seguimiento administrativo, igual que ya se hace con topics/subscriptions faltantes del backbone -- CA-2. Se agrega la regla absoluta 12 remitiendo a la seccion nueva. No se toca la guia de enrutamiento publico/privado de ASB ni el wiring ya documentado (ADR-0024) -- CA-3. Archivo modificado: unicamente `agents/implementer.md`.

- **Doctrina de custodia de secretos generalizada en ADR-0024, ADR-0021 y `CLAUDE.md` (issue #184, mandato de ADR-0025)**: ADR-0024 decision #6 dejaba la custodia en Key Vault como si fuera exclusiva de las cadenas de ASB y omitia la cadena del propio ASB interno del BC al nombrar solo "backbone y externo"; se generaliza remitiendo a ADR-0025 como doctrina general (Key Vault por defecto, identidad administrada donde el runtime lo exige) y se incluye explicitamente la cadena interna en la custodia, junto con las correcciones equivalentes en Referencias y Control de cambios. ADR-0021 reencuadra el modulo Key Vault -- generado por `infra-base-scaffolder` desde el issue #170 -- como **almacen general de secretos del BC** en vez de "custodia de la cadena del backbone", corrige una atribucion de issue incorrecta en el cuerpo (la custodia Key Vault la introdujo #170, no #165) y completa el conteo/tabla de modulos base de 7 a 8 (fila `key-vault`, esqueleto del entorno, nota de SKU y consecuencias) -- actualizacion que #170 dejo pendiente para sucesores y que quedaba en contradiccion con el cuerpo que ya afirmaba que el scaffolder genera el Key Vault del BC. `CLAUDE.md` corrige la prosa superada "dos namespaces de Azure Service Bus (interno e integracion)" (lineas ~67 y ~96, no cubierta por el barrido #167) por la topologia vigente desde ADR-0024: un namespace interno por BC, con lo publico viajando por el backbone compartido o, en el caso diferido, por integracion externa. Archivos modificados: `docs/adr/0024-modelo-eventos-bus-privado-publico.md`, `docs/adr/0021-infraestructura-base.md`, `CLAUDE.md`.

- **`agents/implementer.md` instrumenta la verificacion de la subscription de consumo sobre el backbone compartido (issue #158)**: cierra el ultimo eslabon de la oleada de ADR-0024 (#163/#164/#166) materializando el flujo de consumo real de un evento publico de otro BC. El paso "Verificar infraestructura" del proceso solo disparaba verificacion cuando el handler **publica** un evento (`IPrivateEventSender`/`IPublicEventSender`); no habia ningun paso accionable equivalente para cuando el handler **consume** un evento publico via `[ServiceBusTrigger]`, pese a que la guia declarativa de la seccion "Infraestructura (topics y subscriptions)" (issue #164) ya documentaba que el consumidor crea su propia subscription (naming ADR-0005, `{consumidor}-escucha-{productor}`) sobre el backbone. Se agrega al paso 5 la verificacion explicita del lado consumidor: si el `[ServiceBusTrigger]` apunta a `SERVICE_BUS_CONNECTION_<ALIAS>` del backbone compartido (ADR-0024 decision #4), el implementer verifica que su subscription con ese naming existe alli y, si no, la documenta en "Infraestructura modificada" de su resumen para seguimiento administrativo — no la agrega a ningun archivo de este repo, igual que el topic del productor (CA-1, CA-2). No se redefine ningun patron de app setting, custodia ni RBAC (CA-3): se reutilizan integramente los fijados por #163/#164/#166. El criterio publico/privado BC-aware (ADR-0023 decision #4) y la exigencia de payload plano/portable (ADR-0012) no se tocan (CA-4). El consumo intra-BC de un evento privado no requiere este paso nuevo: su subscription ya queda cubierta por la verificacion existente del bloque `topics_config` del productor. Archivo modificado: unicamente `agents/implementer.md`.

- **`agents/domain-scaffolder.md` wirea el backbone compartido por cadena custodiada en Key Vault (issue #166)**: cierra la oleada de ADR-0024 (#162/#163/#170) actualizando el ultimo agente que aun asumia un namespace de integracion propio del BC. Cambios en el scaffold generado: (1) el registro de brokers en `Program.cs` mantiene el broker default (interno, `SERVICE_BUS_CONNECTION_INTERNO`) y reemplaza el named broker fijo `"integracion"` por un broker nombrado **dinamico por alias** — una variable + una linea `AgregarAzureServiceBusNombradoServerless` por cada alias de `serviceBus.external` con `alcance == "compartido"` (resuelto de `harness.config.json` en un nuevo paso del Paso 0), sin brokers si el BC no declara ninguno todavia y sin wirear jamas `alcance == "externo"` (diferido, ADR-0024 decision #5); el registro sigue siendo por tipo (CA-1, CA-7). (2) los app settings de la Function App (Terraform, Paso 4) inyectan tanto `SERVICE_BUS_CONNECTION_INTERNO` como cada `SERVICE_BUS_CONNECTION_<ALIAS>` como referencia `@Microsoft.KeyVault(...)` leyendo los locals `service_bus_connection_interno_kv_ref` / `service_bus_connection_external_kv_refs` que provisiona `infra-base-scaffolder` (#170), en vez del valor en claro de `module.service_bus_integracion.default_primary_connection_string`; se elimina esa linea del cuerpo (CA-2). (3) el scaffolder referencia el modulo `key-vault` y sus locals como preexistentes (provistos por la infra base, sin duplicar su definicion) y emite siempre, por cada dominio, el `azurerm_role_assignment` **Key Vault Secrets User** de la managed identity de la Function App sobre `module.key_vault.id` (CA-3). (4) se elimina el `azurerm_role_assignment` **Azure Service Bus Data Sender** sobre `module.service_bus_integracion` — RBAC de un namespace de integracion que ya no se provisiona; el modelo vigente es permisos baseline de infra sobre el backbone mas naming de subscription `{consumidor}-escucha-{productor}` (ADR-0005), documentado en `implementer.md` (CA-4). (5) `local.settings.json` sustituye el placeholder `SERVICE_BUS_CONNECTION_INTEGRACION` por `SERVICE_BUS_CONNECTION_<ALIAS>` (placeholder de desarrollo local; en Azure se resuelve via Key Vault), sin dejar referencia a un namespace de integracion propio (CA-5). (6) se actualizan a lenguaje ADR-0024 los comentarios de `Program.cs`, el XML doc de `ServiceBusEndpointBase` (namespace de origen del `[ServiceBusTrigger]`), la nota de infraestructura base (que ahora referencia los 8 modulos, incluido `key-vault`, en vez de 7) y el resumen de archivos del Paso final; no queda descripcion del modelo de dos namespaces por BC (CA-6). El aviso sobre el helper bulk `PublicarEventosServerless` (registro por tipo obligatorio) permanece intacto (CA-7). Archivo modificado: unicamente `agents/domain-scaffolder.md`.

- **`agents/implementer.md` realinea el enrutamiento de eventos publicos al backbone compartido (issue #164)**: materializa ADR-0024 decisiones #4 (publico comun = backbone compartido), #6 (custodia de cadenas en Key Vault) y #7 (wiring de brokers) en el agente que escribe la fase verde. El agente todavia enrutaba `IPublicEvent` a un `module "service_bus_integracion"` propio del BC y al app setting `SERVICE_BUS_CONNECTION_INTEGRACION` — topologia superada por ADR-0024. Cambios: (1) la tabla de `Connection` del `[ServiceBusTrigger]` reemplaza la fila del namespace de integracion propio por el backbone compartido (`SERVICE_BUS_CONNECTION_<ALIAS>`, patron fijado en #163) y reencuadra la fila diferida como integracion verdaderamente externa (ADR-0024 decision #5), no como "namespace de integracion de otro BC"; se agrega un ejemplo de `[ServiceBusTrigger]` consumiendo del backbone; (2) la tabla de enrutamiento topic → broker en la seccion "Infraestructura" reemplaza `module "service_bus_integracion"` por "ninguno — lo administra infra"; se elimina el ejemplo HCL de `topics_config` para eventos publicos (no existe modulo local para el backbone) y se reemplaza por la guia conceptual: el productor crea su topic, el consumidor su subscription (naming ADR-0005), acceso por cadena custodiada en Key Vault, sin RBAC de Azure Service Bus sobre el namespace; (3) la nota de "RBAC del productor" se reescribe como "Acceso al backbone" (referencia Key Vault + managed identity, no `Data Sender`); (4) los pasos "Verificar infraestructura" y la regla absoluta #7 restringen la escritura de Terraform de este agente a `module "service_bus_interno"` unicamente, documentando en el resumen cualquier necesidad de topic/subscription en el backbone; (5) las citas a "ADR-0023 decision #2" (topologia de dos namespaces) se reemplazan por ADR-0024; el criterio semantico publico/privado BC-aware (issue #156, ADR-0023 decision #4) queda intacto. Archivo modificado: unicamente `agents/implementer.md`.

- **`infra-base-scaffolder` genera un solo namespace interno por Bounded Context (issue #165)**: materializa en HCL la doctrina de ADR-0024 (decisiones #3 y #4) que ADR-0021 ya fijaba a nivel de ADR (issue #160). El esqueleto del entorno (`infra/environments/<env>/main.tf`) dejaba dos instancias del modulo `service-bus` — `module "service_bus_interno"` (`sbint-*`) y `module "service_bus_integracion"` (`sbext-*`) — con sus dos `random_string` de sufijo, sus cuatro outputs raiz y el comentario de RBAC Data Sender del productor sobre el namespace de integracion. Se elimina del cuerpo de `agents/infra-base-scaffolder.md` la segunda instancia completa (CA-1): el `random_string.sb_integracion_suffix`, el bloque `module "service_bus_integracion"`, los outputs `service_bus_integracion_name`/`service_bus_integracion_connection_string` en `outputs.tf`, y el comentario de asignacion de rol Data Sender sobre `module.service_bus_integracion.id` (reemplazado por una nota que aclara que el acceso al backbone compartido es por cadena de conexion custodiada en Key Vault, no por RBAC sobre un namespace del BC). Se ajustan las secciones de unicidad global, limites de nombre y outputs (CA-2) para describir un unico namespace interno con sufijo propio, y las citas a "ADR-0023 decision #2 / dos namespaces" se reemplazan por ADR-0024 decisiones #3 (namespace interno) y #4 (backbone compartido fuera de alcance de este scaffolder) (CA-3). La custodia Key Vault (modulo `key-vault`, referencias `@Microsoft.KeyVault(...)`, RBAC de la managed identity) ya la introdujo #170 antes de este issue (secuenciado segun lo indicado en el issue) y queda intacta: este issue solo reduce la topologia de namespaces ASB a uno interno (CA-4). Archivo modificado: unicamente `agents/infra-base-scaffolder.md`.

- **ADR-0003: enmienda del wiring de brokers al modelo de broker interno por defecto mas N brokers nombrados (issue #162)**: alinea ADR-0003 con la nueva raiz doctrinal de ADR-0024 (#159, aceptado), que reemplaza el wiring fijo de dos brokers por Bounded Context (decision #7). Cambios estructurales al cuerpo (contenido superado eliminado, no marcado "obsoleto", per convencion del proyecto): (1) el parrafo de **"Modo serverless"** que describia "dos destinos fisicos" (namespace interno como broker default + namespace de integracion como named broker fijo `"integracion"`) se reescribe para describir **dos senders** segun el marcador del evento (`IPrivateEventSender` al ASB propio del BC, `IPublicEventSender` al backbone compartido o a un ASB externo diferido) y un wiring de **broker default** (siempre, `HabilitarAzureServiceBusParaServerLess(serviceBusInterno)`) mas **N brokers nombrados** (uno por ASB — backbone compartido y, si aplica, externos — con `AgregarAzureServiceBusNombradoServerless(<nombre>, <cadena>)`), cada uno leyendo su cadena de conexion custodiada; se mantiene explicito que el wiring sigue siendo **por cadena de conexion**, coherente con el paquete `Cosmos.EventDriven.CritterStack.AzureServiceBus` actual, remitiendo a ADR-0024 decision #6 (custodia en Key Vault) y Alt 4 (managed identity diferida) para el detalle de custodia; (2) la seccion **Referencias** reemplaza la cita a ADR-0023 que describia "dos senders fisicos (namespace interno vs de integracion)" por una cita que solo afirma el ASB propio del BC (`IPrivateEventSender`), y suma una referencia nueva a ADR-0024 como autoridad de a que broker enruta `IPublicEventSender` y del wiring de N brokers nombrados. Se agrega una seccion **Control de cambios** (nueva en este ADR) con la reforma de este issue. `IPrivateEventSender` / `IPublicEventSender` siguen existiendo como interfaces sin cambios; lo que cambia es el broker de destino. Archivo modificado: unicamente `docs/adr/0003-event-sourcing-marten-wolverine.md`.

- **ADR-0021: reforma doctrinal — un solo namespace de Service Bus interno por Bounded Context (issue #160)**: alinea ADR-0021 con la nueva raiz doctrinal de ADR-0024 (#159, aceptado), que reemplaza el modelo de dos namespaces por BC (interno + integracion) fijado por ADR-0023 decision #2. Cambios estructurales al ADR: (1) el **Contexto** reemplaza la cita a ADR-0023 como raiz doctrinal por ADR-0024, que establece que el harness provisiona por defecto un unico namespace interno por BC y que el evento publico comun viaja por el backbone compartido del producto (provisionado por infra), fuera del alcance del `infra-base-scaffolder`; (2) la subseccion de **Decision** "Dos namespaces ASB por Bounded Context" se reescribe como "Un namespace de Service Bus interno por Bounded Context": queda una sola fila en la tabla de namespaces (interno, `IPrivateEventSender`), se elimina el namespace de integracion y se aclara que el wiring del backbone compartido (Key Vault, brokers nombrados) queda diferido a issues derivados (#165); (3) la **tabla de los 7 modulos base** y su nota siguiente eliminan el RBAC Data Sender del productor sobre el namespace de integracion — el modulo `service-bus` se instancia una sola vez; (4) la seccion **"El sufijo de unicidad vive en el ENTORNO"** pasa de "aplica a ambos namespaces" a "aplica al namespace interno", elimina el ejemplo `sbext-*` y el patron de naming de integracion; (5) la seccion **"El esqueleto del entorno y sus outputs"** deja una sola instancia del modulo (`module "service_bus_interno"`) en el `main.tf` de ejemplo y reduce los outputs raiz de cuatro a dos (`service_bus_interno_name`, `service_bus_interno_connection_string`); (6) las **Consecuencias** cambian la positiva "Aislamiento estructural por topologia" (dos namespaces) por "Aislamiento por default" (namespace interno sin RBAC para identidades externas) y eliminan la negativa "Cada BC provisiona dos namespaces de Service Bus... duplicando el recurso"; (7) las **Referencias** reemplazan ADR-0023 como raiz doctrinal por ADR-0024, reencuadran ADR-0023 como origen del concepto de BC y de la regla de portabilidad (sin describir la topologia de dos namespaces, que ADR-0023 decision #2 enmienda por separado en issue #161), y sustituyen la referencia al issue #130 (materializo las dos instancias) por el issue #165 (actualizacion pendiente del `infra-base-scaffolder` a un namespace interno + custodia Key Vault del backbone). Se agrega una seccion **Control de cambios** (nueva en este ADR) con las dos reformas (issue #128 y este issue #160), siguiendo la convencion del proyecto de no dejar lenguaje de obsolescencia en el cuerpo. Archivo modificado: unicamente `docs/adr/0021-infraestructura-base.md`.

- **Consideracion de handoff de bus generalizada a `IPrivateEvent` e `IPublicEvent` en `agents/planner.md`** (issue #127): el template de handoff condicionaba la consideracion de payload plano exclusivamente a eventos `IPublicEvent`, dejando sin cobertura a los eventos privados (`IPrivateEvent`) que cruzan el namespace interno del Bounded Context. Los cambios: (1) la consideracion **"Construccion del evento publico"** pasa a llamarse **"Construccion del evento que cruza un bus"** y su condicion de inclusion cambia de "cuando algun evento es `IPublicEvent`" a "cuando algun evento cruza un bus (`IPrivateEvent` o `IPublicEvent`)"; (2) el cuerpo afirma que el payload plano y portable aplica a ambos canales (el ASB interno del BC via `IPrivateEventSender` y el backbone compartido via `IPublicEventSender`), cita ADR-0023 como doctrina raiz del criterio "¿cruza un bus?" y conserva ADR-0012 "Frontera de serializacion: event store vs bus" como autoridad de la regla; (3) el **punto del checklist explicativo** que describe para que sirve esa consideracion pasa de "cuando hay un `IPublicEvent`" a "cuando hay un evento que cruza un bus (`IPrivateEvent` o `IPublicEvent`)", actualizando ademas la referencia al nombre renombrado de la consideracion y sumando ADR-0023 como segunda cita de autoridad. La referencia a que el test-writer escribe el round-trip con serializador por defecto (seccion 6e generalizada en #125) y el implementer aplana el modelo rico antes de publicar/enviar queda intacta. Archivo modificado: unicamente `agents/planner.md`.

- **Restriccion de forma del payload generalizada a todo evento con marker de bus en `agents/implementer.md` y `agents/reviewer.md`** (issue #126): ambos agentes ataban la regla de portabilidad unicamente a `IPublicEvent`, dejando sin cobertura a los eventos privados (`IPrivateEvent`) que cruzan el namespace interno del Bounded Context -- exactamente el defecto que ADR-0012 (reformado en #122) y ADR-0023 describieron como cierre pendiente. En **`agents/implementer.md`**: (1) la tabla "Donde vive cada tipo de evento" gana una columna "Forma del payload" que marca las dos filas con marker (`IPublicEvent`, `IPrivateEvent`) como **plano y portable** y la fila de event sourcing sin marker como "modelo rico permitido", dejando claro que solo la tercera fila admite `ConfigurarSerializacion`; (2) el bloque "Restriccion de forma del payload publico" pasa a llamarse **"Restriccion de forma del payload (criterio: ¿cruza un bus?)"** y cubre explicitamente ambos markers: `IPublicEvent` via `IPublicEventSender` al backbone compartido, `IPrivateEvent` via `IPrivateEventSender` al ASB interno del BC; la autoridad sigue siendo ADR-0012 "Frontera de serializacion: event store vs bus" con el criterio "¿cruza un bus?", sumando ADR-0023 como doctrina raiz. En **`agents/reviewer.md`**: (3) el **antipatron item 7** pasa de "`IPublicEvent` cuyo payload carga modelo de dominio rico" a "**Evento con marker de bus (`IPrivateEvent` o `IPublicEvent`) cuyo payload carga modelo de dominio rico**", conservando la pregunta diagnostica (¿se reconstruye con `JsonSerializerOptions` por defecto?) y citando la seccion 6e del test-writer como "regla generalizada a todo evento con marker de bus"; (4) el **bloque de vigilancia activa** pasa de "`IPublicEvent` — portabilidad por el bus" a "**Eventos con marker de bus (`IPrivateEvent` e `IPublicEvent`) — portabilidad por el bus (vigilancia activa)**", ampliando los dos items de verificacion (payload plano + guardrail 6e) a cualquier evento con marker; (5) el parrafo previo que explicaba por que el round-trip de Marten no detecta el defecto se actualiza para nombrar explicitamente el canal adicional de `IPrivateEvent` (ASB interno del BC) junto al de `IPublicEvent` (backbone compartido); (6) el parrafo "No confundas este guardrail con el sin-registro-falla de 6d" conserva la distincion de expectativa invertida y la extiende a ambos markers. La referencia a test-writer.md seccion 6e queda consistente con la 6e generalizada de #125 (mismo contrato: round-trip por defecto para todo evento con marker de bus). Archivos modificados: unicamente `agents/implementer.md` y `agents/reviewer.md` (CA-8). No se tocan ADRs (ya reformados en #122/#123) ni `agents/test-writer.md` (#125).

- **Guardrail de portabilidad por el bus generalizado a `IPrivateEvent` e `IPublicEvent` en `agents/test-writer.md`** (issue #125): la seccion 6e del agente estaba atada solo a `IPublicEvent`, lo que dejaba sin guardrail a los eventos privados (`IPrivateEvent`) que cruzan el namespace interno del Bounded Context — exactamente el defecto que ADR-0012 (reformado en #122) y ADR-0023 describieron como cierre pendiente. Los cambios: (1) el encabezado de **6e** pasa de "para eventos publicos (`IPublicEvent`)" a "para eventos con marker de bus (`IPrivateEvent` e `IPublicEvent`)"; (2) el cuerpo de 6e establece que ambos markers (`IPrivateEvent` via `IPrivateEventSender` al ASB interno del BC; `IPublicEvent` via `IPublicEventSender` al backbone compartido) requieren el mismo round-trip con `JsonSerializerOptions` por defecto, cita ADR-0023 como doctrina raiz del criterio "¿cruza un bus?" y aclara que el test de un `IPublicEvent` vive en `Contracts.Tests/` mientras que el de un `IPrivateEvent` vive en el proyecto de tests del dominio (`{Dominio}.Tests/`), nunca en `Contracts.Tests/`; (3) la nota **"Cuando usar helper inline"** de 6d aclara que aplica solo al event store de Marten (6d) y que los tests de portabilidad de bus (6e) usan `JsonSerializerOptions` por defecto y viven junto a los eventos con marker; (4) el parrafo **"Distincion critica"** generaliza la expectativa invertida a todo evento con marker de bus (antes solo nombraba `IPublicEvent`), y suma ADR-0023 como autoridad de la extension; (5) la **tabla de tres filas** actualiza la fila de 6e: de "bus / `IPublicEvent`" a "bus / evento con marker (`IPrivateEvent` o `IPublicEvent`)"; (6) la **regla 21** pasa de "Todo `IPublicEvent` DEBE..." a "Todo evento con marker de bus (`IPrivateEvent` o `IPublicEvent`) DEBE...", y cita ADR-0023 junto a ADR-0012 como autoridad. Archivo unico modificado: `agents/test-writer.md`. No se tocan ADRs (ya reformados en #122), ni `implementer.md`/`reviewer.md` (#126), ni la filosofia Given/When/Then (ADR-0002).

- **ADR-0012: regla unica "todo tipo que cruza un bus es plano y portable"** (issue #122): reforma in-place de la seccion "Frontera de serializacion: event store vs bus" de `docs/adr/0012-estilo-modelado-objetos-dominio.md` para eliminar la ambiguedad que ADR-0023 (issue #121) volvio contradictoria. Antes la seccion ataba la regla de portabilidad al marcador `IPublicEvent` ("el payload de un `IPublicEvent` contiene solo tipos serializables por defecto"), lo que deja de ser correcto tras ADR-0023: tambien un evento privado (`IPrivateEvent`) cruza un bus (el namespace interno del BC) y debe ser plano. Se reescribe el criterio de "¿es `IPublicEvent`?" a **"¿cruza un bus?"**: (1) la regla pasa a ser "todo tipo que cruza un bus -- el ASB interno del BC (via `IPrivateEventSender`) o el backbone compartido del producto (via `IPublicEventSender`) -- contiene solo tipos serializables con el serializador por defecto"; (2) se afirma explicitamente que el modelo rico + `ConfigurarSerializacion` es **exclusivo del event store de Marten** y que ningun tipo que cruce un bus puede depender de un resolver custom para reconstruirse; (3) los ejemplos INCORRECTO/CORRECTO se reescriben para ilustrar "tipo que cruza un bus" con comentarios que aclaran que la regla aplica por igual a `IPublicEvent` e `IPrivateEvent`; (4) el corolario de construccion y la descripcion del guardrail hablan de "tipo que cruza un bus" en vez de `IPublicEvent`; la referencia a la seccion 6e de `test-writer.md` se describe como regla generalizada (no acota a `IPublicEvent`); (5) se anade una nota de version que cita ADR-0023 (issue #121) como origen del cambio de doctrina y apunta los issues #125/#126 como propagacion aguas abajo a `test-writer` e `implementer`. La seccion "Referencias" suma ADR-0023 y actualiza las descripciones de ADR-0001, ADR-0003 y ADR-0005 para reflejar la topologia de mensajeria (los topics viven en el ASB interno del BC o en el backbone compartido; el contrato blindado aplica a ambos). Las secciones de ADR-0012 fuera de "Frontera de serializacion" permanecen sin cambios.

- **Enmienda ADR-0005, ADR-0020 y ADR-0022 para anclar el framing de Bounded Context (ADR-0023)** (issue #124): enmiendas minimas de framing y referencias reciprocas a ADR-0023 en tres ADRs satelite. ADR-0005 (naming y versionado de eventos) gana una nueva subseccion "Eventos publicos como Published Language del Bounded Context" que declara que los eventos publicos son el Published Language del BC (Evans, *DDD* 2003, cap. 14) y se publican al backbone compartido del producto (ADR-0024), y una seccion "Referencias" nueva que apunta a ADR-0001 y ADR-0023. ADR-0020 (hosting, un App Service Plan por dominio) gana: (Contexto) el parrafo de framing "El marco organiza dominios en Bounded Contexts: grupos de dominios relacionados con su propio resource group (ADR-0023). Dentro de un BC, cada dominio es una Function App independiente"; (Decision) la aclaracion de que la directiva aplica a cada dominio dentro del BC y que el resource group del BC es responsabilidad de ADR-0021; (Referencias) referencia a ADR-0023. ADR-0022 (OIDC) gana: (Decision) la subseccion "Frontera de alcance: autenticacion runtime cross-BC" que delimita el alcance de este ADR a CI/deploy, declara la autenticacion runtime entre BCs como trabajo diferido, afirma que la seguridad inter-BC se logra por aislamiento del namespace interno (ADR-0023) y que el RBAC least-privilege por entidad es una recomendacion diferida; (Referencias) referencia a ADR-0023. Ninguna enmienda cambia las decisiones tecnicas sustantivas de los tres ADRs.

### Fixed

- **`agents/implementer.md`: criterio publico/privado corregido a BC-aware (issue #156)**: la seccion "Donde vive cada tipo de evento" definia un evento como publico si "otro dominio lo consume (via ServiceBus)" — criterio pre-Bounded Context que contradecia ADR-0023 decisiones #2 y #4 ("publico vs privado = enrutamiento, no forma del payload") y que era inconsistente con el propio lado de consumo del agente (seccion "El Connection del trigger", que ya acertaba: "un dominio reacciona a un evento privado publicado por otro dominio del mismo BC"). Un implementer que siguiera el criterio viejo clasificaria como publicos eventos que cruzan entre dominios del mismo BC, cuando ADR-0023 los considera privados (cruzan el ASB interno del BC, no un canal publico inter-BC). Cambios: (1) la tabla relabela "Publico (entre dominios)" → "Publico (inter-BC)" y "Privado (dentro del dominio)" → "Privado (intra-BC)"; (2) el parrafo de criterio se reescribe como "un evento es publico (inter-BC) si lo consume un dominio de otro Bounded Context; es privado (intra-BC) si lo consume un dominio del mismo BC, aunque sea un dominio distinto al productor", ancla en ADR-0023 sin duplicar su doctrina; (3) se agrega un cross-reference entre esta seccion y "El Connection del trigger" para evitar futuras desincronias entre el lado publish y el lado consumo. No hay cambio de comportamiento de codigo; es correccion de guia — un consumidor legado que reclasifique eventos intra-BC de "publico" a "privado" estara alineandose con la arquitectura vigente, no introduciendo un cambio. Archivo modificado: unicamente `agents/implementer.md`.

- **El writer del pipeline de tooling coloca la entrada en la seccion equivocada del CHANGELOG, y el gate emite un diagnostico enganoso** (issue #133): detectado durante la implementacion de #122 via `/mefisto-sequential`. El writer colocaba la entrada bajo `## [0.8.0]` (version publicada) en vez de bajo `## [Unreleased]`; el gate abortaba correctamente (issue #70), pero el mensaje "cambio notable sin entrada en [Unreleased]" era identico al de "writer no escribio nada" -- costando investigacion extra. Dos defectos corregidos: (1) el `STAGE1_PROMPT` del writer en `.claude/scripts/mefisto-tooling-pipeline.sh` aclara ahora que `## [Unreleased]` es SIEMPRE la PRIMERA seccion del archivo (antes de cualquier `## [x.y.z]`), que la entrada va ahi y nunca bajo una version publicada, y que si falta la subseccion de categoria (`### Added`/`### Changed`/etc.) dentro de `## [Unreleased]` debe crearla ahi; (2) el gate anade un `elif` que llama a la nueva funcion `detect_misplaced_changelog_entry` de `_mefisto-common.sh`: compara el cuerpo de cada seccion `## [x.y.z]` entre el base commit y el HEAD actual y, si alguna crecio, emite un mensaje especifico que nombra la version afectada ("la entrada esta bajo `## [0.8.0]`, debe ir bajo `## [Unreleased]`") y la recuperacion exacta. El bloque "no hay ninguna entrada" del gate anterior se preserva intacto como `else`, de modo que ningun camino se relaja (CA-4). Nuevo test `test-changelog-gate.sh`: CA-7 (entrada mal colocada detectada, gate aborta con mensaje especifico) y CA-8 (entrada en `[Unreleased]` no detectada como mal colocada, gate pasa); `detect_misplaced_changelog_entry` anadida al bloque [pre]. El pipeline publicado (`scripts/tooling-pipeline.sh`) no comparte el patron: no incluye instruccion de CHANGELOG en el writer (los consumidores no mantienen necesariamente Keep a Changelog) ni tiene gate de CHANGELOG (queda como follow-up opt-in de #70, ADR-0019).

## [0.8.0] - 2026-06-26

### Added

- **`/onboard` gana la provision opt-in del CI hacia Azure (seguimiento 79c)** (issue #82): cierra el ultimo gap de solo-lectura del doctor de onboarding. `/onboard` (creado por #79) ya diagnosticaba el CI hacia Azure bajo OIDC (ADR-0022) -- existencia de la app de Entra y de los 3 secrets OIDC del repo -- pero cuando faltaban **solo lo sugeria**: imprimia `setup-github-ci.sh <subscription-id>` como accion y no lo ejecutaba; el consumidor tenia que salir del skill y correrlo a mano. Este issue **es** ese seguimiento 79c que #79 anticipaba: convierte la sugerencia en una **accion ejecutable opt-in** dejando el diagnostico de solo lectura como comportamiento por defecto. Se anade a `commands/onboard.md` un **paso 4 de provision** que: (CA-1) reusa la misma resolucion de `PLUGIN_SCRIPTS` que el skill ya hace (lee `.claude/pipeline/.plugin-root` con fallback al glob del cache) e invoca `"$PLUGIN_SCRIPTS/setup-github-ci.sh" <subscription-id> [<owner/repo>]` **plugin-relative**, sin reescribir ni redisenar el script; (CA-2) **nunca** ejecuta nada sin confirmacion explicita del usuario -- el skill **advierte que crea recursos reales en Azure** (app de Entra + Service Principal sin secret, dos role assignments -- `Contributor` a nivel suscripcion y `Storage Blob Data Reader` sobre el tfstate -- y un federated credential OIDC para `refs/heads/main`) **antes** de preguntar "¿Quieres que lo configure ahora? [si/no]", y solo corre el bloque tras un "si"; (CA-3) **valida prerequisitos antes de invocar y reporta claro en vez de fallar opaco** -- `az` instalado y con sesion activa (`az account show`), el `<subscription-id>` que aporta el usuario (no esta en el config) y la advertencia de que debe correr **despues** de `bootstrap-backend.sh` (que resuelve el nombre real del tfstate con el sufijo de unicidad de #92); el `<owner/repo>` lo auto-resuelve el script (via `gh`/remote `origin`) y el skill ofrece pasarlo solo si la resolucion falla; (CA-4) el **comportamiento por defecto sigue siendo solo diagnostico** -- el paso 4 se aplica *solo si* la seccion "CI hacia Azure" reporto `FALTA` (si salio `OK` se omite; si `NO VERIFICADO` por falta de `az`/sesion/`githubServicePrincipalName`, pide resolverlo en vez de provisionar a ciegas), de modo que una corrida sin confirmar no crea ningun recurso en Azure ni copia secrets; (CA-5) **tras una provision exitosa el skill recuerda explicitamente** que el script imprime los 3 secrets OIDC (`AZURE_CLIENT_ID`, `AZURE_TENANT_ID`, `AZURE_SUBSCRIPTION_ID`) que el usuario debe **pegar a mano** en GitHub (Settings > Secrets and variables > Actions), porque ni el script ni `/onboard` los suben y no hay client secret que expire (OIDC, ADR-0022); (CA-6) el skill **mantiene el guard "cwd != Mefisto"** (top-level y embebido en cada bloque `bash`, incluido el nuevo de provision como cinturon + tirantes sobre el guard propio del script -- ADR-0019), **no introduce terminologia obsoleta** (`az ad sp create-for-rbac`, `AZURE_CREDENTIALS`, "secret que expira"; verificado: `grep` no los encuentra) y la regla de "solo lectura" se **matiza** -- deja de nombrar la provision de labels como la *unica* escritura permitida y pasa a "las unicas escrituras permitidas son las provisiones opt-in de labels (paso 3) y CI (paso 4), cada una bajo confirmacion". El paso 4 aprovecha la **idempotencia** del script (reutiliza app/SP/federated credential existentes y re-imprime los 3 secrets), asi que tambien sirve cuando el diagnostico reporto la app `OK` pero los secrets OIDC `FALTA`. Se actualizan en consecuencia la descripcion del skill (suma el CI como segunda excepcion opt-in), la nota de "Que verifica" (la provision de CI ya la ofrece `/onboard`, ya no es "el seguimiento 79c pendiente") y los mensajes de "Acciones sugeridas" para CI/secrets (de "lo automatizara el seguimiento 79c" a "lo puede provisionar el paso opt-in"). Como toque de coherencia se actualiza el `README.md` (seccion onboarding greenfield, pasos 2 y 4): donde decia que la configuracion del CI "sigue siendo manual con `setup-github-ci.sh`" ahora refleja que `/onboard` puede configurarlo bajo confirmacion, despues del bootstrap del backend. Depende de #79 (ya CLOSED, Oleada 3); se construye sobre el contrato OIDC de #97 / ADR-0022 (ya CLOSED): el refinamiento corrigio el dato desactualizado del borrador original (decia `az ad sp create-for-rbac` con secret; la realidad post-#97 es app sin secret + 3 secrets OIDC + federated credential). Comparte el archivo `commands/onboard.md` con #81 (provision de labels, ya en `[Unreleased]`) por areas distintas (labels vs CI) -- a secuenciar, no paralelizar; este issue rebasa sobre la version de #81 y reusa su patron de confirmacion. Mefisto es un harness y no consume su propio onboarding: `/onboard` opera solo sobre el consumidor (ADR-0019) y no tiene gemelo interno. El guard de `onboard.md` sigue cubierto por `scripts/tests/test-guards.sh` (58/0).
- **`/onboard` gana la provision opt-in de los labels faltantes (seguimiento 79b)** (issue #81): el doctor `/onboard` (creado por #79) hoy solo **sugiere** crear los labels del esquema (`tipo:*`/`estado:*`/`dom:*`/`bug`/`bloqueado`, ADR-0007) imprimiendo la ruta plugin-relative de `setup-github-labels.sh` y declarando explicitamente "Solo lectura. Nunca ejecutes `gh label create`"; el consumidor tiene que salir del skill, recordar la ruta y correr el script a mano. Este issue **es** ese seguimiento 79b que #79 ya anticipaba: convierte la sugerencia en una **accion ejecutable opt-in** manteniendo el diagnostico de solo lectura como comportamiento por defecto. Se anade al skill (`commands/onboard.md`) un **paso 3 de provision** que: (CA-1) reusa la misma resolucion de `PLUGIN_SCRIPTS` que el skill ya hace (lee `.claude/pipeline/.plugin-root` con fallback al glob del cache) e invoca `"$PLUGIN_SCRIPTS/setup-github-labels.sh"` **plugin-relative**, sin reescribir ni redisenar el script; (CA-2) **nunca** ejecuta nada sin confirmacion explicita del usuario -- el skill **advierte que el script es destructivo** (borra los 8 labels default de GitHub: documentation, duplicate, enhancement, good first issue, help wanted, invalid, question, wontfix, y re-crea `bug` con el esquema del harness) **antes** de preguntar "¿Quieres que los provisione ahora? [si/no]", y solo corre el bloque tras un "si"; (CA-3) el **comportamiento por defecto sigue siendo solo diagnostico** -- el paso 3 se aplica *solo si* la seccion de labels reporto `FALTA` (si salieron `OK` se omite; si `NO VERIFICADO` por `gh` sin autenticar, pide `gh auth login` en vez de provisionar a ciegas), de modo que una corrida sin confirmar no crea ni borra ningun label y se preserva la naturaleza de "doctor"; (CA-4) el guard "cwd != Mefisto" (top-level y embebido en cada bloque `bash`, incluido el nuevo de provision como cinturon + tirantes sobre el guard propio del script) y las demas secciones del diagnostico (config via `load_harness_config`, CI hacia Azure) quedan intactas; (CA-5) el bloque de provision corre bajo `set +e`, captura el exit code de `setup-github-labels.sh` y, si falla, **reporta el fallo de forma clara sin dejar el onboarding en estado ambiguo** y sin abortar el resto del flujo -- enumera las dos causas tipicas (`gh` no autenticado; o un label `tipo:*`/`estado:*`/`bloqueado` ya existia y el script aborta por `set -e`, ya que solo usa `--force` en `bug`/`dom:*`) con la accion para cada una, y recuerda re-correr `/onboard` para ver el estado real; (CA-6) la regla de "solo lectura" del skill se **matiza**: deja de afirmar "Nunca ejecutes `gh label create`" de forma absoluta y pasa a "diagnostico de solo lectura por defecto; la unica escritura permitida es la provision opt-in de labels del paso 3, y solo tras confirmacion explicita". Se actualizan en consecuencia la descripcion del skill (de "doctor de solo lectura" a "doctor: por defecto solo diagnostica; excepcion opt-in de labels"), la nota de "Que verifica" (la provision de labels ya la ofrece `/onboard`; la de CI sigue siendo el seguimiento 79c) y el mensaje de "Acciones sugeridas" para labels. **Decision de idempotencia** (nota tecnica del issue): se invoca solo cuando hay `FALTA` *y* se tolera el exit code, sin tocar el script -- `setup-github-labels.sh` no es completamente idempotente (re-correrlo sobre un repo que ya tiene `tipo:*`/`estado:*` aborta por `set -e`); un `setup-github-labels.sh` con `--force` en todos los labels queda como posible issue de seguimiento. Depende de #79 (ya CLOSED, Oleada 3); comparte el archivo `commands/onboard.md` con #82 (CI en `/onboard`) -- areas distintas (labels vs CI), a secuenciar, no paralelizar. Mefisto es un harness y no consume su propio onboarding: `/onboard` opera solo sobre el consumidor (ADR-0019) y no tiene gemelo interno. Cubierto por `scripts/tests/test-guards.sh` (el guard de `onboard.md` sigue presente; 58/0).
- **Skill publicado `/onboard`: doctor diagnostico del onboarding del consumidor** (issue #79): primer corte del onboarding automatizado. Hoy el onboarding del consumidor es manual y disperso (crear `.claude/harness.config.json`, provisionar labels con `setup-github-labels.sh`, configurar CI con `setup-github-ci.sh`) y no habia forma de saber de un vistazo que falta o si el config esta bien formado; el primer fallo tipico aparecia tarde (un `/draft` que fallaba por labels inexistentes, o un `/infra` que abortaba en el `apply` por un nombre de tfstate invalido). Se crea `commands/onboard.md` (lado publicado, con el guard defensivo "cwd != Mefisto" de ADR-0019 -- Mefisto no es consumidor del harness: no tiene `harness.config.json`, labels de dominio ni CI hacia Azure que diagnosticar; sin equivalente interno, mismo grupo que `/health-check`/`/infra`/`/scaffold`) que reporta un **checklist de solo lectura** -- NO crea ni modifica nada (ni labels, ni archivos, ni recursos de Azure, CA-6): (1) **Configuracion** -- reusa `load_harness_config` (issue #78) plugin-relative, asi que la regla de formato de `terraformStateStorage` (`^[a-z0-9]{3,24}$`) y los campos requeridos (`projectName`/`namespacePrefix`/`solutionFile`) quedan con #78 como **unica fuente de verdad**; reporta si falta el archivo, si `jq` falla, si faltan campos o si el tfstate no cumple, re-emitiendo el mensaje exacto de `load_harness_config` (CA-2); (2) **Labels** (ADR-0007) -- lista `gh label list` y reporta cuales del esquema (`tipo:*`, `estado:borrador`, `estado:listo`, `bug`, `bloqueado`, y `dom:<x>` por cada `domainLabels` del config) estan ausentes, sin crearlos (CA-3); (3) **CI hacia Azure** (ADR-0022) -- verifica de solo lectura que existe la aplicacion de Entra/Service Principal (`az ad app list --display-name`) y los secrets OIDC del repo (`gh secret list`), sin crearlos (CA-4). El diagnostico es **tolerante**: cada seccion es independiente y, si `gh`/`az` no estan disponibles o no hay sesion, reporta `NO VERIFICADO` en vez de fallar (nota tecnica del issue: el CI no verificable sin `az login` se reporta, no aborta). Imprime un **checklist consolidado** con estado por item (OK / FALTA / NO VERIFICADO), un resumen con contadores y estado global (LISTO / LISTO con salvedades / INCOMPLETO), y para cada FALTA la accion o comando que lo resuelve -- apuntando a `setup-github-labels.sh`/`setup-github-ci.sh` y anotando que la provision automatizada llega en los seguimientos del onboarding (79b labels, 79c CI), que este issue **bloquea** (CA-5). El diagnostico corre bajo `bash` garantizado (heredoc) para que `load_harness_config` (arrays) y el word-splitting de `domainLabels` se comporten igual sin depender del shell interactivo del consumidor (zsh no hace word-splitting de variables sin comillas). Se registra `/onboard` en el catalogo de `CLAUDE.md` (conteo 15 -> 16 skills, tabla de skills, "skills sin equivalente interno") y de `README.md` (lista de skills, "Verificar instalacion" y paso 2 del flujo greenfield). Entra en la cobertura de `scripts/tests/test-guards.sh` (array `PUBLISHED_SKILLS`; guard verificado). Depende de #78 (ya en `[Unreleased]`: la validacion del tfstate vive en `load_harness_config`); relacionado con #72 (que documenta el bootstrap manual). Mefisto es un harness y no tiene `harness.config.json` propio: el skill opera solo sobre archivos del consumidor (ADR-0019), sin gemelo interno.
- **El `domain-scaffolder` genera, la primera vez que corre en un repo, el workflow reutilizable `smoke-tests-dominio.yml` y el global `smoke-tests.yml`** (issue #98): cierra el vacio greenfield donde el primer deploy fallaba al resolver un `uses:` a un archivo inexistente. El workflow de deploy que emite el scaffolder (Paso 5) referenciaba `./.github/workflows/smoke-tests-dominio.yml` (job `smoke-tests`, lineas 1428-1434) y la nota del Paso 5 prometia que ese reutilizable aceptaba `SERVICEBUS_CONNECTION_STRING`/`POSTGRES_CONNECTION_STRING` como secrets opcionales (`required: false`), pero **ningun paso lo generaba**; el Paso 6b ademas registraba el dominio en `.github/smoke-tests-dominios.json` "para el workflow global de smoke tests", workflow que tampoco se creaba (verificado: el unico match de `smoke-tests-dominio.yml`/`workflow_call`/reusable en `agents/`+`commands/`+`scripts/` era la **referencia** de la linea 1427, nunca la **generacion**; gap ya constatado en el field note del #90, 2026-06-25-1045). Se anade un **Paso 6** al agente `agents/domain-scaffolder.md` que genera ambos archivos de forma **idempotente** (misma logica "si existe / si no existe" del Paso 6b: solo crea lo faltante y **nunca sobreescribe**, asi el segundo dominio en adelante respeta lo existente -CA-3-): (1) el reutilizable `.github/workflows/smoke-tests-dominio.yml` con `on: workflow_call`, inputs `base_url`/`test_project` y los secrets opcionales `SERVICEBUS_CONNECTION_STRING`/`POSTGRES_CONNECTION_STRING` (`required: false`) exactamente como el deploy del Paso 5 los pasa -CA-1-, que corre `dotnet test "${test_project}" --configuration Release` mapeando `base_url`->`Api__BaseUrl` y los secrets->`ServiceBus__ConnectionString`/`Postgres__ConnectionString` (las claves de config que lee el `appsettings.json` del proyecto SmokeTests), coherente con `tests/<RootNamespace>.{PascalCase}.SmokeTests/` y ADR-0013 -CA-2-; declara solo `permissions: contents: read` (no `id-token: write`: los smoke tests son black-box por HTTP y connection string, no se autentican contra Azure por OIDC); y (2) el workflow **global** `.github/workflows/smoke-tests.yml` (`workflow_dispatch` + `schedule` diario) que lee `.github/smoke-tests-dominios.json` (el JSON que mantiene el Paso 6b) y expande una entrada de matrix por dominio, reusando el mismo `smoke-tests-dominio.yml` con `secrets: inherit` para no duplicar la logica de ejecucion -CA-4-. El **Paso 8 (commit)** agrega los nuevos workflows (condicionalmente, solo si se generaron en esta corrida) y -por primera vez- el `.github/smoke-tests-dominios.json` que el Paso 6b venia escribiendo sin commitear; el **resumen final** lista los archivos generados, distinguiendo los de cada dominio de los "solo la primera vez" -CA-5-. Se actualizan tambien los resumenes de `commands/scaffold.md` y el cuerpo del PR de `scripts/scaffold-pipeline.sh`, y el **ADR-0013** (`docs/adr/0013-smoke-tests-contra-entorno-dev.md`): la seccion "Integracion en el proceso de desarrollo" suma la responsabilidad del scaffolder de generar ambos workflows la primera vez, y la seccion CI/CD se corrige para fijar la nomenclatura (reutilizable = `smoke-tests-dominio.yml`; global = `smoke-tests.yml`), antes descrita de forma imprecisa. Comparte territorio con #97 (secrets de Azure en CI) en `agents/domain-scaffolder.md` pero no choca: #97 toco el job `deploy` (OIDC) y este toca los workflows de smoke tests. Mefisto es un harness y no tiene `.github/workflows/` propio: el cambio vive como YAML inline en el prompt del agente publicado (patron "agente emisor" de ADR-0019, sin equivalente interno -el `/scaffold` no tiene version interna-).
- **Agente `infra-base-scaffolder` + skill `/infra-base` + ADR-0021: el harness genera la infraestructura base del consumidor** (issue #93): cierra el vacio greenfield donde el `domain-scaffolder` (Paso 4) y el `infra-writer` asumian preexistentes unos modulos Terraform base que el harness no proveia de ninguna forma (ni plantilla, ni generador, ni agente; verificado: no existia `templates/`). En el primer greenfield real (`Bitakora.ControlAsistencia`) hubo que escribir `infra/modules/*` y `infra/environments/dev/*` a mano, el `service-plan` de campo incumplia el contrato de ADR-0020 (solo aceptaba `name`/`resource_group_name`/`location`/`sku_name`/`tags`, no `os_type`/`worker_count`/`always_on`) y el `terraform output` salia vacio por falta del esqueleto del entorno. Se crea el agente publicado `agents/infra-base-scaffolder.md` (lado consumidor, guard "cwd != Mefisto" -- ADR-0019; sin equivalente interno porque Mefisto no tiene `infra/`), que **escribe inline** (no copia plantillas, coherente con el patron del `domain-scaffolder`) los **7 modulos base** bajo `infra/modules/` (`resource-group`, `monitoring`, `postgresql`, `service-bus`, `service-plan`, `storage`, `function-app`) con los inputs/outputs documentados, y el **esqueleto del entorno** bajo `infra/environments/<env>/` (`main.tf` con las 4 instancias compartidas, `variables.tf` con locals `prefix`/`prefix_func`, `providers.tf` sin `backend.tf` -- lo escribe `bootstrap-backend.sh` --, y `outputs.tf` exponiendo `resource_group_name`/`service_bus_name`/`postgresql_fqdn` a nivel raiz). El modulo `service-plan` generado **acepta los 4 inputs del contrato de ADR-0020** (`os_type`/`sku_name`/`worker_count` al recurso; `always_on` reexpuesto como output para la Function App), de modo que el `terraform validate` del Paso 4 pasa sin advertencias. El agente es **idempotente**: no sobrescribe archivos `.tf` existentes (respeta personalizaciones), solo crea lo faltante, y solo corre `fmt`/`init -backend=false`/`validate` (nunca `plan`/`apply`). Tokens hardcodeados de Bitakora generalizados a variables (`alert_email` requerido, `postgresql_database_name`, `project`/`project_short` derivados de `harness.config.json`). Se anade el skill `commands/infra-base.md` (entrada al agente, mismo patron que `/bug`) y el ADR-0021 (`docs/adr/0021-infraestructura-base.md`), indexado en `CLAUDE.md` (tabla de ADRs, catalogo de skills, tabla de agentes, "skills sin equivalente interno") y en `README.md` (conteos 15 skills/17 agentes/21 ADRs y nuevo paso 3 del flujo greenfield, entre `bootstrap-backend.sh` y el primer `/infra`). Se actualizan las Notas del Paso 4 del `domain-scaffolder` y la asuncion de `infra-writer` (lineas 29/56): en vez de "debe existir / advertencia pasiva", apuntan a generar la base con `/infra-base`. `infra-base.md` entra en la cobertura de `scripts/tests/test-guards.sh`. Bloquea a #94 (el sufijo de unicidad se aplicara en este esqueleto del entorno); relacionado con #99 (region de PostgreSQL, parametrizada via `postgresql_location`).
- **`load_harness_config` valida el formato de `terraformStateStorage` al cargar `harness.config.json`** (issue #78): la funcion en `scripts/_pipeline-common.sh` exportaba `HARNESS_TFSTATE_STORAGE` (linea 58) sin validar su forma, asi que un nombre que viola las reglas de Azure Storage Account pasaba en silencio y fallaba tarde, en el `apply` de `/infra` (caso real detectado en el bootstrap greenfield de `MiControlPlane`: `stmicontrolplanetfstatedev`, 26 chars). Se agrega, dentro del bloque de validaciones de `load_harness_config` (tras el chequeo de campos obligatorios), una verificacion que aborta con `return 1` y mensaje claro cuando el campo **tiene valor** y no cumple `^[a-z0-9]{3,24}$` (3-24 caracteres, solo minusculas y digitos; reglas de Microsoft Learn -- "Storage account overview"). El mensaje indica el limite y sugiere abreviar el prefijo del proyecto (`micontrolplane -> mcp -> stmcptfstatedev`). La validacion solo aplica cuando el campo tiene valor: `terraformStateStorage` es opcional en la carga (consumidores sin IaC lo dejan vacio o ausente) y esos casos NO se bloquean. El manejo es consistente con el de los campos obligatorios faltantes (mismo patron `echo ... >&2` + `return 1`), error duro que aborta antes de que cualquier pipeline IaC use un nombre invalido. `grep -Eq` es portable en bash 3.2 (macOS). Complemento ejecutable de la seccion D de #72 (que solo documenta el limite). Cubierto por el test nuevo `scripts/tests/test-harness-config.sh` (11 casos: >24 chars, mayusculas, guiones y <3 chars abortan; vacio/ausente y nombres validos en los limites 3 y 24 pasan; el mensaje menciona el limite y sugiere abreviar). No hay ADR de naming de recursos Azure; esta validacion implementa una restriccion de la plataforma.

### Changed

- **El scaffold de dominio emite los GitHub Actions en su version mayor estable mas reciente** (issue #90): el bloque YAML del workflow de deploy del consumidor (`.github/workflows/deploy-{kebab}.yml`) que emite el agente `domain-scaffolder` en el Paso 5 referenciaba actions atrasados, de modo que todo dominio nuevo nacia con avisos de deprecacion (runtimes de Node antiguos) y obligaba a actualizarlos a mano. Verificado contra la API de GitHub el 2026-06-25, se suben las versiones mayores flotantes: `actions/checkout@v4` -> `@v7` (jobs `build-and-test` y `deploy`), `actions/setup-dotnet@v4` -> `@v5` (ambos jobs; el input `dotnet-version: '10.0.x'` se conserva) y `azure/login@v2` -> `@v3` (el input `creds: ${{ secrets.AZURE_CREDENTIALS }}` se conserva). `Azure/functions-action@v1` queda sin cambio (v1 sigue siendo la mayor vigente y el tag flotante ya recibe parches) y se anota con un comentario aclaratorio. Los bumps son seguros bajo `ubuntu-latest`: los breaking changes de `setup-dotnet@v5` (runner Node 24+) y `azure/login@v3` (Node 20 -> 24) solo afectan al runtime del runner hosteado por GitHub, que siempre esta actualizado, y no tocan ningun input usado por el template. Se mantiene el patron del harness de fijar por tag mayor flotante (no por SHA), coherente con lo que ya emitia y con la guia de GitHub para actions de primera parte; el pin por SHA queda como decision de hardening del consumidor, fuera de alcance. El resto del bloque (estructura de jobs, `needs`, paths del trigger, pasos de publish/validacion, llamada a `smoke-tests-dominio.yml`) queda intacto. Punto unico de cambio: `agents/domain-scaffolder.md`; Mefisto es un harness y no tiene `.github/workflows/` propio que actualizar.
- **Modelo explicito por rol para los agentes y skills (publicados e internos)** (ajuste de tooling): hasta ahora los 24 skills (`commands/` y `.claude/commands/`) no declaraban `model` y heredaban el modelo de la sesion, y dos agentes (`domain-scaffolder`, `infra-base-scaffolder`) tampoco lo declaraban. Se fija el modelo por rol siguiendo la convencion del harness (opus = razonamiento, sonnet = escritura/generacion, haiku = mecanico). En **agentes**: los dos scaffolders sin declarar pasan a `sonnet`, `pr-sync` sube de `haiku` a `sonnet` (resolver conflictos de merge no es puramente mecanico), e `implementer` baja de `opus` a `sonnet` (decision de costo: corre en cada issue en la fase verde, con `reviewer`=opus como red de seguridad); el resto conserva su modelo (en particular `reviewer`, `planner` y los investigadores siguen en opus, y `test-writer` se mantiene en sonnet). En **skills** se introduce `model` explicito: `haiku` para los lanzadores delgados que validan, lanzan el agente/pipeline y reportan (`bug`, `draft`, `implement`, `infra`, `infra-base`, `merge`, `onboard`, `parallel`, `scaffold`, `sequential`, `tooling`, `work-status` y los gemelos internos `mefisto-bug`/`mefisto-merge`/`mefisto-plan`/`mefisto-sequential`/`mefisto-tooling`/`mefisto-work-status`); `sonnet` para los que generan o interpretan contenido (`eraser-diagram`, `show-flow`, `health-check`, `mefisto-release`); y `opus` para los que editan codigo real en sesion via plan + Edit/Write (`fix-review`, `mefisto-fix-review`). El frontmatter de los skills se introduce con **solo** el campo `model:` (sin `description:`): Claude Code sigue derivando la descripcion del primer parrafo del cuerpo, por lo que las descripciones visibles no cambian (verificado contra la doc oficial de skills, code.claude.com/docs/en/skills). Como los alias `opus`/`sonnet`/`haiku` apuntan al modelo mas reciente de cada tier, el cambio fija el tier por rol y se auto-actualiza con cada release de modelo.
- **El writer y el reviewer del pipeline de tooling (interno y publicado) corren con modelo explicito por etapa** (ajuste de tooling, complementa la entrada anterior): a diferencia de los pipelines TDD/IaC/scaffold -- que invocan `claude -p --agent <nombre>` y por tanto respetan el `model:` del frontmatter del agente --, los dos pipelines de tooling (`.claude/scripts/mefisto-tooling-pipeline.sh` interno y `scripts/tooling-pipeline.sh` publicado) construyen un prompt inline y lo corren con `claude -p` **sin `--agent` ni `--model`**, de modo que sus etapas writer/reviewer (y la etapa merge del interno) usaban el **modelo por defecto del CLI** (Opus 4.8) -- el rebalanceo de frontmatter (#115) no las cubria porque no hay agentes `tooling-writer`/`tooling-reviewer`. Ahora la funcion `run_agent` deriva un `AGENT_MODEL` por etiqueta de etapa (`reviewer` -> `opus`; resto, es decir `writer` y la etapa `merge` etiquetada como writer -> `sonnet`) y lo pasa como `--model "$AGENT_MODEL"` en todas las invocaciones `claude -p` (incluidos los reintentos 5xx y por permisos del publicado), alineado con la convencion del harness (escritura en sonnet, revision en opus) y con la decision de que `pr-sync` -- que tambien resuelve conflictos -- es sonnet. Con esto **ningun pipeline queda corriendo en el modelo por defecto implicito**: o usan `--agent` (frontmatter) o `--model` explicito. Los alias `sonnet`/`opus` siguen apuntando al modelo mas reciente de cada tier. Cambio simetrico interno/publicado (ADR-0019), verificado con `bash -n`.

### Fixed

- **Documenta la restriccion de region de PostgreSQL Flexible Server y anade `azureLocation` al snippet de `harness.config.json`** (issue #99): dos roces de documentacion del primer greenfield real (`Bitakora.ControlAsistencia`), agrupados por ser ediciones de doc de bajo riesgo en archivos contiguos. (H) El snippet JSON literal de `.claude/harness.config.json` que el lector copia (README, seccion "Configurar el consumidor") **no incluia `azureLocation`**, aunque `bootstrap-backend.sh` lo exige (`--location` o el campo) y la prosa ya lo documentaba en tres puntos; quien copia-pegaba el ejemplo se topaba con el abort del bootstrap y lo agregaba a mano. Ahora el snippet incluye `"azureLocation": "eastus2"` (valor de ejemplo valido para el backend del tfstate), coherente con la prosa existente que lo describe como opcional, de modo que copiar-pegar no dispara el abort (CA-1). (C) Ni el README ni ningun ADR documentaban que algunas regiones devuelven `LocationIsOfferRestricted` al crear el PostgreSQL Flexible Server: `eastus2` lo hizo en el greenfield y se resolvio con `centralus` (quedo solo como comentario en el HCL de campo), asi que el siguiente greenfield reincidiria. Se documenta la restriccion en **dos lugares enlazados, sin duplicar la fuente**: el ADR-0021 (`docs/adr/0021-infraestructura-base.md`) gana la seccion **autoritativa** "Region de PostgreSQL Flexible Server: la restriccion de oferta por suscripcion", que aclara que `LocationIsOfferRestricted` es una restriccion de **oferta a nivel de suscripcion** (no una indisponibilidad global -- la region figura como soportada en el overview oficial de Postgres) y que por eso no hay region "apta" universal; y el README enlaza a ella desde una nota operativa en el campo `azureLocation` y desde un recordatorio en el paso 5.3 del flujo greenfield (CA-2). Ambos lugares aclaran que la region del backend del tfstate (`azureLocation`) y la de Postgres (`postgresql_location` en `terraform.tfvars`) **pueden y a veces deben diferir**, y que conviene **validar la de Postgres antes del primer `terraform apply`** con `az postgres flexible-server list-skus --location <region> -o table` (debe listar `Standard_B1ms`, la SKU que usa el modulo `postgresql`), eligiendo otra (p. ej. `centralus`) si sale vacio o falla (CA-3). Fuentes verificables citadas en el ADR y el README: la referencia de Azure CLI de `az postgres flexible-server list-skus` ("Lists available sku's in the given region") y la lista oficial de regiones del servicio. Encaja de forma natural en ADR-0021 (que ya posee el modulo `postgresql` y referenciaba #99 como seguimiento, tras priorizarse #93). Mefisto es un harness y no consume su propio onboarding ni tiene `infra/` propia: los cambios son documentacion de gobierno (README) mas la seccion nueva del ADR-0021, sin equivalente interno (ADR-0019).
- **Completa la guia de onboarding greenfield del consumidor (README, esquema de `harness.config.json` y ADR-0019)** (issue #72): consolida cuatro huecos verificados del onboarding que dejaban a un usuario greenfield con un setup no reproducible, prerequisitos sin provisionar, campos sin documentar y riesgo de nombres de recursos invalidos (drafts #72/#73/#74/#76). (A) El `.claude/settings.json` del README (paso 1 de Instalacion) ahora incluye, junto a `extraKnownMarketplaces`, el bloque **`enabledPlugins`** con `"mefisto@augusto-romero-arango-harness": true` (clave `<plugin.name>@<marketplace.name>`, verificada contra `.claude-plugin/plugin.json` y `marketplace.json`) -que hace la habilitacion **reproducible y commiteable**, ya que el `/plugin install` interactivo no deja artefacto en el repo- y un bloque **`permissions`** recomendado (sintaxis `Bash(<cmd>:*)` de Claude Code, citada a la doc oficial de settings) con `allow` para `dotnet`/`git`/`gh`/`terraform`/`az` y `deny` contra `terraform destroy`/`az group delete`/`git push --force`, marcado como punto de partida ajustable. (B) Se documenta el campo opcional **`repoSlug`** (redirige los drafts cross-repo del `planner`/`tooling-investigator` a un fork; default `augusto-romero-arango/eda-evsourcing-azure-harness`; se lee inline con `jq`, no se exporta como `HARNESS_*`) en el esquema del README (paso 3) y en la lista de campos de `CLAUDE.md` ("Contrato con el proyecto consumidor"); y se **corrige ADR-0019** (lineas 60 y 111), que mencionaba un inexistente `harnessRepoSlug` en `.claude/harness.config.json` y un `repoSlug` en `plugin.json` -ninguno existe en el codigo, que solo usa `repoSlug` en `.claude/harness.config.json` (desalineacion ya senalada en el log del reviewer del #38)-; `grep -rn 'harnessRepoSlug' docs/ README.md CLAUDE.md` ya no devuelve resultados. (C) El README anade un paso "Bootstrap del repo del consumidor (labels y CI)" en el flujo greenfield que indica ejecutar `setup-github-labels.sh` (labels `tipo:*`/`dom:*`/`estado:*`, prerequisito operativo de ADR-0007; elimina los 9 labels default de GitHub y requiere `domainLabels` + `gh auth`) y `setup-github-ci.sh <subscription-id>` (Service Principal OIDC; requiere `gh auth` + `az login` + `<subscription-id>` y corre despues del backend del tfstate), documentando la **invocacion plugin-relative** (`$PLUGIN_SCRIPTS`, no `./scripts/` del consumidor) y que ambos abortan dentro del repo de Mefisto (ADR-0019). (D) Junto al campo **`terraformStateStorage`** (README y CLAUDE.md) se documenta la restriccion de Azure -**3-24 caracteres, solo minusculas y digitos**, citada a Microsoft Learn (reglas de nombres de recursos, `Microsoft.Storage`)- y se sugiere abreviar el prefijo para nombres largos con el ejemplo que excede el limite (`stmicontrolplanetfstatedev` = 26 chars invalido) y su correccion (`stmcptfstatedev` = 15). Dependia de #75 (esquema `source.source` del marketplace, ya cerrado y mergeado en PR #102) y complementa a #78 (validacion ejecutable del formato del tfstate en `load_harness_config`, ya en `[Unreleased]`). Mefisto es un harness y no consume su propio onboarding: los cambios son documentacion de gobierno (README/CLAUDE.md) mas la correccion del ADR-0019, sin equivalente interno.
- **`extract_test_count` suma todos los proyectos de test en el gate de refactoring, en vez de contar solo el primero** (issue #80): durante un refactor real (issue #185 de `Bitakora.ControlAsistencia`) el gate "no perder tests" del pipeline TDD reporto un falso "El refactoring perdio tests" aunque la suite completa seguia verde (425/425); el refactor movia tests de `Contracts.Tests` a `ControlHoras.Tests` y, aunque el total global no cambiaba, el gate abortaba antes de la fase de PR y dejaba trabajo verde sin entregar. Es un bug **generico del harness**: cualquier refactor que reubique tests fuera del primer proyecto alfabetico disparaba el abort. Causa: `extract_test_count()` (`scripts/tdd-pipeline.sh`) usaba `head -1` sobre el output combinado de `run_tests_projects`, que emite una linea de resumen (`correcto: N` en MTP, `Superado: N`/`Passed: N` en VSTest) por **cada** proyecto -- asi solo contaba el conteo parcial del primer proyecto. El gate comparaba ese parcial entre baseline (`:665`) y post-refactor (`:888`, condicion `post < baseline` en `:891`), de modo que mover tests entre proyectos cambiaba el parcial aunque el total fuera estable -> falso positivo. Ahora la funcion **suma** los `N` de **todas** las lineas de resumen del output combinado (`grep ... | awk '{ s += $1 } END { ... }'`), abarcando las cuatro variantes multi-idioma/runner ya soportadas (`correcto`/`correctas`/`passed`/`superado`), de modo que baseline y post-count miden la suite completa y son comparables (CA-1, CA-2). Se preserva el **sentinela `?`**: si no hay ninguna linea parseable, awk lo imprime en su bloque `END` (`NR==0`) -- antes el `head -1` mas `set -euo pipefail` hacia que el caso "sin match" abortara el script (`grep` retorna != 0 y `pipefail` lo propagaba), asi que el sentinela era codigo muerto; ahora la asignacion lleva `|| true` y el sentinela funciona de verdad, para que el gate (`:890`) lo trate como "no comparable" en vez de como `0` (CA-3). La salida sigue siendo un unico entero limpio, valido para la comparacion `-lt` de bash (CA-4), y la implementacion es portable en bash 3.2/macOS (solo `grep`/`awk`, ya usados en el repo -- CA-6). Se anaden a `scripts/tests/test-refactor-gate.sh` los escenarios F (suma multi-proyecto `100+325=>425`, sentinela `?`, salida entera, multi-runner mezclado) y G (reubicacion `100/325 -> 60/365` con baseline==post==425, sin disparar el abort), mas dos verificaciones de coherencia (E4/E5) que aseguran que la funcion reproducida en el test no diverja del script real (CA-5). El gate es parte del control de calidad del pipeline TDD (ADR-0014); esta correccion evita que produzca falsos positivos. Mefisto es un harness y no tiene src/tests propios; `tdd-pipeline.sh` es un script publicado consumido por el pipeline TDD del consumidor, sin equivalente interno (ADR-0019).
- **El pipeline IaC soporta el flujo preview (`--skip-apply`) -> merge -> apply sin que el cierre del issue lo bloquee** (issue #96): en el primer greenfield real el flujo "previsualizo, mergeo, aplico" quedaba roto: `iac-pipeline.sh <issue> --skip-apply` creaba un PR cuyo cuerpo incluia `Closes #N` (el bloque de creacion del PR corria igual con o sin `--skip-apply`; la rama `--skip-apply` solo omitia el Stage 3), asi que al **mergear** ese PR de preview GitHub **cerraba el issue**, y el apply posterior (`--from-stage 3` o corrida normal) **abortaba** en el gate "solo se procesan issues abiertos" -- en campo se termino aplicando con `terraform` directo. Se adopta la **via (a)** del issue (la recomendada) reforzada para que el flujo funcione de extremo a extremo (CA-1): (1) el `Closes #N` del cuerpo del PR ahora es **condicional** -- solo se emite cuando el apply realmente ocurrio en esa corrida (`SKIP_APPLY = false`); en `--skip-apply` el PR lleva en su lugar una nota de preview que explica que no cierra el issue y como aplicar despues (CA-2: el flujo normal write+review+apply de una corrida mantiene `Closes #N` y el cierre al mergear, intactos); (2) la corrida `--skip-apply` **conserva el worktree** (con el `tfplan` revisado) en vez de eliminarlo en el cleanup, y deja un marcador `.claude/pipeline/.preview-pr` con la URL del PR de preview, de modo que la **fase de apply** (`--from-stage 3`) reanuda sobre ese mismo worktree/`tfplan` (atendiendo el "Cuidado" del issue sobre las lineas 232-237) sin reescribir ni re-planear, aplica la infra y **cierra el issue** directamente (`gh issue close`) sin crear un PR duplicado del HCL ya mergeado; (3) como cinturon adicional (via (b)), el gate de estado del issue se relaja para **continuar** cuando el issue esta cerrado **y** se esta reanudando una etapa de apply (`--from-stage 3` sin `--skip-apply`) -- cubre PRs de preview legados que llevaban `Closes` y ya se mergearon -- y su mensaje de abort, antes un rechazo seco, ahora **explica el flujo correcto** (reanudar con `--from-stage 3` o reabrir el issue) en vez de solo rechazar (CA-3). El marcador solo existe tras un preview, asi que una corrida normal nunca lo ve y su ruta de codigo queda byte-identica (CA-2 verificado por construccion: `PREVIEW_PR_URL=""` -> rama normal). El comentario que el pipeline deja en el issue tambien se vuelve consciente del modo (preview vs aplicado). Se documenta el flujo soportado (CA-4): encabezado de uso de `scripts/iac-pipeline.sh`, seccion nueva del agente `agents/infra-bootstrap.md` (pasos 4 y 5), nota en el skill `commands/infra.md` y nota en el paso 4 de "Primeros pasos" del `README.md`. `iac-pipeline.sh` sigue cubierto por `scripts/tests/test-guards.sh` (guard + sintaxis bash, ambos verificados; 57/0). Mefisto es un harness y no tiene `infra/` propia: el pipeline IaC es un script publicado sin equivalente interno (ADR-0019), por lo que no hay gemelo `mefisto-` que tocar.
- **Alinea los secrets de Azure entre el workflow de deploy del scaffolder y `setup-github-ci.sh` adoptando OIDC, y crea ADR-0022** (issue #97): en el primer greenfield real el deploy a Azure abortaba en el paso de autenticacion porque las dos mitades del contrato de secrets no coincidian: el workflow de deploy que emite `domain-scaffolder` (Paso 5) esperaba **un** secret JSON `AZURE_CREDENTIALS` (`azure/login` con `creds:`), pero `scripts/setup-github-ci.sh` emitia **cuatro** secrets separados (`AZURE_CLIENT_ID`/`AZURE_CLIENT_SECRET`/`AZURE_TENANT_ID`/`AZURE_SUBSCRIPTION_ID`) via `az ad sp create-for-rbac`; ninguno cuadraba con el otro y `azure/login` no recibia credenciales validas. Como no habia ADR que fijara el modo de autenticacion de CI (el issue lo anticipaba), se elige la **via (b) con OIDC** (Workload Identity Federation) -CA-1- y se registra en el nuevo **ADR-0022** (`docs/adr/0022-autenticacion-ci-azure-oidc.md`): es la guia vigente de Azure/GitHub (el flujo Service Principal+secret y `--sdk-auth` estan **deprecados** por Azure; fuentes citadas en el ADR) y elimina el footgun operativo del client secret que expira en 1 ano -un harness greenfield cuyo CI moriria en silencio al ano-. `setup-github-ci.sh` ahora crea la app de Entra + Service Principal **sin secret** (idempotente: reutiliza la app si ya existe por displayName); fija antes la suscripcion activa con `az account set --subscription` (mismo patron que `bootstrap-backend.sh`) para que la creacion de la app, el `AZURE_TENANT_ID` resuelto via `az account show` y los role assignments apunten todos a la suscripcion pasada como argumento y no a la que el operador tuviera activa -de otro modo, con multiples suscripciones en distintos tenants, la app naceria en el tenant equivocado y el role assignment cross-tenant fallaria-; le asigna **`Contributor` a nivel suscripcion** (alcance del deploy de Functions, no solo lectura del tfstate -nota tecnica del issue-) y `Storage Blob Data Reader` sobre la Storage del tfstate (resolviendo el nombre final con sufijo de #92), y anade un **federated credential** cuyo subject (`repo:<owner/repo>:ref:refs/heads/main`) coincide exacto con el claim que GitHub pone en el OIDC token para el trigger `push: branches: [main]` + `workflow_dispatch` del workflow generado; el slug `owner/repo` se resuelve via `gh repo view` con fallback al remote `origin` y override por 2do argumento. Las asignaciones de rol usan `--assignee-object-id` + `--assignee-principal-type ServicePrincipal` para evitar el race de replicacion del SP recien creado en Graph. El workflow de deploy del scaffolder cambia el paso `Azure Authentication` a `azure/login@v3` con los inputs separados `client-id`/`tenant-id`/`subscription-id` (sin `creds:`) y el job `deploy` declara `permissions: id-token: write` + `contents: read` -CA-2 rama OIDC; ya no hay nota de expiracion a 1 ano porque no hay secret-. Se sincronizan los nombres de secret en todo el contrato (CA-3): el resumen "Proximos pasos" del scaffolder y la nota nueva del Paso 5 listan los **tres** secrets OIDC en vez de `AZURE_CREDENTIALS`, y `scripts/scaffold-pipeline.sh` actualiza su recordatorio post-scaffold. La doc de onboarding del README (paso "Configurar la autenticacion de CI") refleja la via OIDC, los tres secrets, la resolucion del slug y que no hay secret que expire (CA-4). Se indexa ADR-0022 en `CLAUDE.md` (tabla de ADRs) y se sube el conteo a **22 ADRs** en `CLAUDE.md` y `README.md`. `setup-github-ci.sh` sigue cubierto por `scripts/tests/test-guards.sh` (guard + sintaxis bash, ambos verificados). Mefisto es un harness y no tiene `.github/workflows/` propio: el cambio del workflow vive como YAML inline en el prompt del agente publicado (patron "agente emisor" de ADR-0019, sin equivalente interno). Relacionado con #82 (tambien toca `setup-github-ci.sh`); no diverge: deja el script en su forma OIDC final.
- **El esqueleto del entorno que genera `infra-base-scaffolder` anade sufijo de unicidad global a PostgreSQL y Service Bus** (issue #94): segunda mitad del patron de bug de #92 (Storage del tfstate). En el primer greenfield real (`Bitakora.ControlAsistencia`) el `terraform apply` abortaba con `ServerNameAlreadyExists` para `psql-mcp-dev` porque el nombre de un PostgreSQL Flexible Server es un endpoint DNS publico (`*.postgres.database.azure.com`) y por tanto **unico en TODO Azure**, no solo en el resource group; lo mismo aplica al namespace de Service Bus (`*.servicebus.windows.net`). El esqueleto generado por `agents/infra-base-scaffolder.md` (Paso 2.3) pasaba `name = "psql-${local.prefix_func}"` y `name = "sb-${local.prefix}"` **sin** sufijo, mientras las Storage del mismo entorno SI lo llevaban (`random_string`, agregado por el `domain-scaffolder`). Ahora el `main.tf` del esqueleto declara un `random_string` (length 6, `special = false`, `upper = false` -- identico al patron de las Storage) para PostgreSQL y otro para Service Bus, e incorpora su `.result` al `name` de cada modulo (`psql-${local.prefix_func}-${sufijo}`, `sb-${local.prefix}-${sufijo}`), evitando la colision global en el primer apply (CA-1, CA-2). Los nombres resultantes respetan los limites de Azure (CA-3): PostgreSQL Flexible Server 3-63 chars (minusculas/numeros/guiones, ~19-24 chars para los prefijos tipicos) y Service Bus namespace 6-50 chars (empieza con letra, termina en el sufijo alfanumerico); si el `project` es muy largo el agente indica acortarlo en `variables.tf`. Los outputs raiz `postgresql_fqdn` y `service_bus_name` (CA-4) no cambian: ya leen el output del modulo (`module.postgresql.server_fqdn`, `module.service_bus.name`), que refleja el nombre real con el sufijo resuelto por el recurso -- el agente advierte explicitamente no referenciar el nombre "construido". `random_string` es **idempotente sin `keepers`** (Terraform persiste su valor en el state en el primer apply y lo mantiene estable de por vida del recurso); se documenta la **limitacion de migracion**: anadir el sufijo a un recurso YA desplegado cambia su `name` (atributo `ForceNew`) y choca con el `prevent_destroy = true` de ambos modulos, por lo que el sufijo aplica solo a provisiones nuevas (greenfield) y migrar un recurso existente exige `terraform state mv`/`import` manual. Se actualiza el ADR-0021 (`docs/adr/0021-infraestructura-base.md`, seccion "El sufijo de unicidad vive en el ENTORNO") con la regla de unicidad global para los tres recursos con DNS publico (Storage, PostgreSQL, Service Bus), los limites de nombre y la limitacion de migracion; y el comentario de `providers.tf` (el provider `random` lo usan ahora tanto el esqueleto como el `domain-scaffolder`). Cumple CA-5 rama 2: como #93 entrego el esqueleto sin sufijo, este PR lo edita. Mefisto es un harness y no tiene `infra/` propia; el cambio vive como prosa/HCL inline en el prompt del agente publicado (patron "agente emisor" de ADR-0019, sin equivalente interno).
- **`scripts/bootstrap-backend.sh` resuelve un nombre globalmente unico para la Storage Account del tfstate en vez de hardcodear el del config** (issue #92): el primer greenfield real (`Bitakora.ControlAsistencia`) abortaba de forma irrecuperable con `StorageAccountAlreadyTaken` porque el nombre de una Storage Account es un endpoint DNS publico (`*.blob.core.windows.net`) y por tanto **unico en TODO Azure**, pero el script lo tomaba tal cual del campo `terraformStateStorage` (`STORAGE="${HARNESS_TFSTATE_STORAGE}"`) sin garantizar unicidad global; si otro tenant ya habia reservado ese nombre, `az storage account create` fallaba sin recuperacion. El `domain-scaffolder` (Paso 4) ya resolvia esto para las Storage de dominio con `random_string` de 6 chars (`stmarcacionesdev{suffix}`); el bootstrap usaba el patron viejo. Ahora el campo `terraformStateStorage` se trata como nombre **BASE** y el nombre **FINAL** lleva un **sufijo de unicidad global** de 6 chars `[a-z0-9]`, validado con `az storage account check-name` (hasta 10 reintentos con sufijo distinto si el nombre esta tomado). La **idempotencia** (CA-3) no se ancla en un archivo de estado local fragil sino en dos fuentes durables, en este orden de precedencia: (1) el `storage_account_name` ya escrito en un `backend "azurerm"` del ambiente -registro versionado y canonico que viaja en el PR via #86, es lo que usara `terraform init`-, y (2) la cuenta ya creada en el RG dedicado del tfstate (`rg-<proyecto>-tfstate`) cuyo nombre arranca con la base -cubre una corrida previa interrumpida antes de escribir `backend.tf`, p. ej. el fallo de RBAC del paso 3-; solo si ninguna existe se genera un nombre nuevo. El nombre final resuelto se usa de forma uniforme en el resto del script -creacion de la cuenta, blob-service-properties, container, el `backend.tf` que se escribe (CA-2) y el bloque que se imprime-, de modo que `terraform init` usa exactamente la cuenta creada y no queda mismatch. El limite de 24 chars de Azure (CA-4) se respeta truncando la base a 18 chars (mismo calculo que el scaffolder en Paso 4: `base + 6 <= 24`) con aviso si hace falta; el sufijo nunca produce un nombre que viole `^[a-z0-9]{3,24}$`, asi que no rompe la validacion de #78. Para resolver el nombre antes de imprimir el resumen, `az account set` se mueve por encima de la resolucion. Los tres helpers **puros** (sin `az`) -`truncate_storage_base`, `gen_storage_suffix` (openssl con fallback a `$RANDOM`, portable en bash 3.2/macOS) y `read_backend_storage_account_name`- se extraen a `scripts/_pipeline-common.sh` (mismo patron que la validacion de #78) y quedan cubiertos por el test nuevo `scripts/tests/test-storage-naming.sh` (17 casos: truncado en los limites 18/19/24, formato y aleatoriedad del sufijo, y parseo de `backend.tf` incluyendo valores no literales y bloques en otros `.tf`). La orquestacion `az` (`resolve_storage_account_name`) vive en el script. Como el sufijo cambia el nombre real de la cuenta, `scripts/setup-github-ci.sh` -que corre DESPUES del bootstrap (README "Primeros pasos", paso 2) y asigna `Storage Blob Data Reader` al Service Principal de CI sobre el tfstate ya creado- tambien resuelve ahora el nombre FINAL reutilizando los mismos helpers compartidos (backend.tf versionado -> cuenta existente en el RG por prefijo de base -> fallback al nombre base para backends pre-#92) en vez del campo base del config; de otro modo el rol apuntaria a una cuenta inexistente y el `terraform init` de CI quedaria sin acceso al tfstate. `bootstrap-backend.sh` y `setup-github-ci.sh` siguen cubiertos por `scripts/tests/test-guards.sh` (guard + sintaxis). (CA-5) la doc del campo `terraformStateStorage` en `README.md` y el header del script aclaran que el nombre recibe un sufijo de unicidad. Origen: greenfield real con `StorageAccountAlreadyTaken` sobre `stmcptfstatedev`. Complementa #78 (que solo valida el formato del campo, no el choque de unicidad global).

## [0.7.0] - 2026-06-25

### Added

- **`scripts/bootstrap-backend.sh` y campo opcional `azureLocation` para inicializar el backend de Terraform en greenfield** (issue #83): se crea el script publicado `bootstrap-backend.sh` que materializa, de forma **idempotente**, el backend remoto del `tfstate` -- prerequisito del pipeline IaC, que recien despues levanta los App Service Plans/Function Apps por dominio (ADR-0020). El camino documentado se rompia porque ese script no existia: nada creaba el Resource Group `rg-<proyecto>-tfstate`, la Storage Account ni el container `tfstate`, y `setup-github-ci.sh` ya asumia el backend creado (solo asignaba `Storage Blob Data Reader` al Service Principal). El script lleva la cabecera estandar (`set -euo pipefail`, `source _pipeline-common.sh`, guard defensivo `.claude-plugin/plugin.json` que aborta si se invoca dentro de Mefisto -- ADR-0019 --, `load_harness_config`); parsea `--env <dev|staging|prod>` (default `dev`), `--location <region>` y `--subscription <id>` (tambien posicional); resuelve nombres desde el config sin hardcodear (`RG="${HARNESS_RG_PREFIX}-tfstate"`, `STORAGE="${HARNESS_TFSTATE_STORAGE}"`, `CONTAINER="tfstate"`); fija la suscripcion explicitamente (`az account set`) antes de operar; crea con verificacion de existencia previa en cada paso (`az group create`, `az storage account create` con TLS 1.2 minimo, `--https-only true`, `--allow-blob-public-access false`, versioning + soft-delete de blobs, y `az storage container create --auth-mode login`); escribe `infra/environments/<env>/backend.tf` con el bloque `backend "azurerm"` resuelto (`key = "<env>.tfstate"`) de forma idempotente; e imprime el bloque resultante para que cuadre con el `terraform init` del `infra-reviewer`. Se anade el campo **opcional** `azureLocation` al schema de `harness.config.json`: si no se pasa `--location`, el script lo lee inline con `jq` (mismo patron que `repoSlug` en `_pipeline-common.sh`); si tampoco existe, aborta pidiendo el flag o el campo. Al ser opcional (default + override por flag) NO es un cambio incompatible del schema (no es MAJOR). Se documenta una seccion nueva "Primeros pasos con el harness (greenfield)" en `README.md` (modelo de ejecucion `$PLUGIN_SCRIPTS`/hook `SessionStart`, orden de bootstrap `bootstrap-backend.sh` -> `setup-github-ci.sh` -> primer `/infra`, scaffold + primer ciclo TDD, y tabla "que corre donde") y el campo `azureLocation` en el esquema de la seccion 3. El script entra en la cobertura del test `scripts/tests/test-guards.sh`. Origen: greenfield real (consumidor `MiControlPlane` con `mefisto@0.6.0`).

### Changed

- **El check de `[Unreleased]` del pipeline interno `/mefisto-tooling` pasa de warning a GATE, y el writer redacta la entrada por defecto** (issue #70): el patron cinturon + tirantes sube el liston de "recordatorio pasivo" a "trabajo exigido". *Cinturon*: el `STAGE1_PROMPT` del writer (`.claude/scripts/mefisto-tooling-pipeline.sh`) gana una instruccion explicita para anadir una entrada bajo `## [Unreleased]` en `CHANGELOG.md` con la categoria Keep a Changelog correcta (`Added`/`Changed`/`Fixed`/`Removed`), con excepcion de cambios que toquen exclusivamente bitacora o gobierno no notable. *Tirantes*: el bloque que antes solo emitia `warn` (introducido en #36, que en modo no-interactivo se ignoraba y dejaba PRs sin entrada que `/mefisto-release` tenia que backfillear -evidencia: PRs #62/#63/#67-) ahora ABORTA cuando el cambio es notable y `[Unreleased]` no recibio contenido. Se anaden a `.claude/scripts/_mefisto-common.sh` las funciones `is_path_changelog_exempt` (clasifica rutas exentas: `docs/bitacora/**`, `README.md`, `CLAUDE.md`, `.gitignore`) y `changes_require_changelog` (un cambio es notable si toca al menos una ruta no exenta). El gate corre tras el reviewer (Stage 2) y antes de crear el PR; si el writer omitio la entrada, el operador la anade y retoma con `--from-stage 2`. Se mantiene la degradacion benigna sin `python3` (no aborta para evitar falsos positivos). Cubierto por el test interno `.claude/scripts/tests/test-changelog-gate.sh`. Acotado al pipeline INTERNO (ADR-0019); el equivalente para el pipeline publicado queda como follow-up opt-in.

### Fixed

- **El pipeline IaC copia y commitea el `backend.tf` del working tree al worktree** (issue #86): cierre del *seam* que quedo abierto al implementar `scripts/bootstrap-backend.sh` (#85). `bootstrap-backend.sh` escribe `infra/environments/<env>/backend.tf` en el working tree del consumidor, pero `scripts/iac-pipeline.sh` ramifica su worktree **siempre desde `origin/main`** (#66) y corre el `terraform init`/`plan` del `infra-reviewer` dentro de ese worktree. En greenfield el `backend.tf` recien generado no estaba versionado en `origin/main`, asi que no llegaba al worktree y el primer `terraform plan/apply` corria con **estado local** en vez del backend remoto -- justo el fallo que el #83 buscaba eliminar. Hasta ahora solo se mitigaba con avisos manuales en docs (`README.md` paso greenfield, `agents/infra-bootstrap.md`) que pedian al usuario commitear y empujar el `backend.tf` a `main` antes del primer `/infra`. Se adopta la **opcion (a)** reforzada con commit: tras crear el worktree desde `origin/main` (rama `FROM_STAGE -le 1`, antes de resolver `INFRA_ENV_DIR_ABS` y de cualquier `terraform init`), el pipeline copia `$REPO_ROOT/infra/environments/<env>/backend.tf` a `$WORKTREE_PATH/...` (con `mkdir -p` del destino para el caso greenfield donde el ambiente no existe en `origin/main`) y lo **commitea** en la rama del worktree (`infra(<env>): incluir backend.tf generado por bootstrap`, mismo patron que el commit de correcciones del reviewer), de modo que aparece en `git diff main...HEAD -- infra/` y viaja en el PR; se versiona en `main` via merge, sin push directo a `main` desde un agente (que es riesgoso). Si el `backend.tf` ya estaba identico en `origin/main` (no-greenfield) la copia es un no-op en el diff y no genera commit; si **no existe** en el working tree, el pipeline emite un `warn` y continua sin abortar (no rompe a consumidores que aun no corrieron `bootstrap-backend.sh`). En `--from-stage > 1` (worktree existente) no se re-copia ni se re-commitea (la logica vive en la rama `else` del `if [ "$FROM_STAGE" -gt 1 ]`). Se sincronizan los avisos manuales de `README.md` y `agents/infra-bootstrap.md`, que ahora documentan la automatizacion. La verificacion greenfield (CA-5) queda como prueba manual reproducible (`git -C $WORKTREE_PATH log --oneline -- infra/environments/dev/backend.tf` y `git -C $WORKTREE_PATH diff main...HEAD -- infra/`); no hay test funcional del setup del pipeline IaC en `scripts/tests/`.
- **`agents/infra-bootstrap.md` resuelve los scripts del plugin con el patron canonico `$PLUGIN_SCRIPTS` en vez de rutas relativas rotas** (issue #83): el agente invocaba `./infra/scripts/bootstrap-backend.sh` y `./scripts/iac-pipeline.sh` (lineas 38, 48 y 69), que con `cwd = repo consumidor` resolvian contra `<consumer>/...` (inexistente) -- los scripts del harness viven en el cache del plugin (read-only). `infra-bootstrap` habia quedado fuera del patron `$PLUGIN_SCRIPTS` que ya usan `commands/` y los agentes `planner`/`reviewer` (mecanismo `.plugin-root` de los issues #31/#61). Ahora resuelve `PLUGIN_ROOT`/`PLUGIN_SCRIPTS` (lee `.claude/pipeline/.plugin-root` con fallback por glob al cache) e invoca `"$PLUGIN_SCRIPTS/bootstrap-backend.sh"` y `"$PLUGIN_SCRIPTS/iac-pipeline.sh"`, pasando `--subscription` explicitamente. Ademas indica que si `bootstrap-backend.sh` reporta que el backend ya existe (idempotencia, exit 0) debe continuar al pipeline IaC, no abortar.

## [0.6.0] - 2026-06-24

### Added

- **Convencion "el modelo de dominio rico no cruza el bus" en `IPublicEvent`** (issue #65): se amplia ADR-0012 con una seccion ("Frontera de serializacion: event store vs bus") que establece que el payload de un `IPublicEvent` solo contiene tipos serializables con el serializador por defecto (primitivos, enums, `string`, fechas, `record` DTO planos); el modelo de dominio rico (campos privados, factory privado, `ConfigurarSerializacion`) NO cruza el bus y se traduce a una forma plana al publicar. Motivacion: `ConfigurarSerializacion` (resolver STJ custom) solo esta registrado en el event store de Marten del dominio productor; cuando el mismo tipo cruza Azure Service Bus el serializador de destino no tiene ese resolver y el payload se vuelve lossy/no-portable (bug reproducido en un consumidor). La convencion se hace cumplir en el flujo de agentes publicados: `planner` incluye las consideraciones de construccion del evento publico (payload plano y portable) en el handoff de la tarea; `implementer` documenta la restriccion de forma en la tabla de ubicacion de eventos; `reviewer` vigila problemas de serializacion contra Azure Service Bus; y `test-writer` exige un test de round-trip con `JsonSerializerOptions` por defecto (SIN el resolver custom), que distingue "viaja por el bus" de "viaja por Marten" -el guardrail de round-trip anterior corria CON el resolver y no detectaba el defecto-. Indice tematico de ADRs en `CLAUDE.md` actualizado.

### Fixed

- **Los pipelines que crean worktree ramifican siempre desde `origin/main` actualizado** (issue #66): los cinco scripts que crean un worktree nuevo (`scripts/tdd-pipeline.sh`, `scripts/tooling-pipeline.sh`, `scripts/iac-pipeline.sh`, `scripts/scaffold-pipeline.sh` y el interno `.claude/scripts/mefisto-tooling-pipeline.sh`) lo hacian desde el HEAD del directorio donde se lanzaba el pipeline. Si el cwd estaba parado en una rama de feature vieja, el worktree nacia de una base desactualizada y los conflictos con main aparecian tarde (en la sincronizacion final de la fase verde) en vez de evitarse desde el inicio. El antipatron era identico en los cinco: un guard que solo emitia `warn` si el cwd no estaba en `main`/`master` (sin abortar ni corregir), un `git pull origin "$CURRENT_BRANCH"` que actualizaba la rama equivocada, y un `git worktree add ... -b <rama>` sin commit-ish base. Se reemplaza por `git fetch origin main` (con `abort` si falla) + `git worktree add ... -b <rama> origin/main`, replicando el patron ya probado de `.claude/scripts/mefisto-release.sh`. El worktree se ramifica **siempre** desde `origin/main` sea cual sea la rama del cwd (el guard queda degradado a contexto informativo en el log); el `fetch` es seguro desde cualquier rama porque no muta el working tree ni el HEAD del cwd. La sincronizacion final con `origin/main` (`merge origin/main --no-edit`) se mantiene intacta como red de seguridad para PRs mergeados durante la ejecucion, pero en el caso comun `BEHIND_COUNT` sera 0. Cubre lado publicado (`scripts/`) e interno (`.claude/scripts/`); los orquestadores (`parallel-pipeline.sh`, `batch-pipeline.sh`, `mefisto-batch-pipeline.sh`) no crean worktrees y quedan cubiertos transitivamente. Origen: pipeline TDD del consumidor Bitakora.ControlAsistencia cuyo worktree nacio 7 commits detras de `origin/main`.

## [0.5.0] - 2026-06-24

### Added

- **Regla del "oraculo independiente" en `test-writer` y ADR-0002** (issue #59): el agente `test-writer` gana la regla absoluta 20, que exige construir el valor esperado de toda asercion (`Then`, `And<>`, `ThenIsPublished*`) a mano como oraculo independiente -armado con las primitivas y factories del dominio- y **prohibe** derivarlo ejecutando la logica bajo prueba (ni el SUT ni los colaboradores de produccion que esa logica invoca). Un esperado calculado por el mismo codigo que se verifica vuelve el test tautologico: el bug contamina por igual el esperado y el actual, ambos coinciden y la prueba pasa sin detectar la regresion. La seccion "Verificacion del estado del agregado" (paso 4) lo refuerza con ejemplo de antipatron (`var esperado = ConsolidadorDesgloseHoras.Consolidar(...)`) y de patron correcto (esperado armado con `new DesgloseHoras(...)` + `IntervaloTemporal.Crear(...)`). El principio queda registrado como decision arquitectonica en **ADR-0002**, subseccion "Oraculo independiente (no-tautologia)", al mismo nivel normativo que la cobertura obligatoria Then + And ya documentada ahi, y la regla 20 lo referencia como fuente. Origen: review del PR #180 del consumidor Bitakora.ControlAsistencia, donde sin esta prohibicion el test-writer tomo el atajo de calcular el esperado con la misma consolidacion del SUT. Acotado a `test-writer`; `smoke-test-writer` no se toca.

### Fixed

- **Resolucion de los scripts del plugin contra la raiz instalada en vez de ruta relativa al cwd** (issue #31, PR #62): los 9 comandos publicados (`implement`, `tooling`, `scaffold`, `sequential`, `infra`, `parallel`, `merge`, `health-check`, `bug`) invocaban sus scripts como `./scripts/<x>.sh`; con `cwd = repo consumidor` esa ruta resolvia contra `<consumer>/scripts/` (inexistente) en vez del cache del marketplace donde vive el plugin, asi que el pipeline no arrancaba y el script parecia "ausente". Se anade un hook `SessionStart` en `hooks/hooks.json` que persiste `${CLAUDE_PLUGIN_ROOT}` -disponible en los hooks del plugin, no en los bloques bash de un slash command- en `.claude/pipeline/.plugin-root`, y cada comando resuelve `PLUGIN_ROOT` leyendo ese archivo con fallback automatico a un glob ordenado del cache (`~/.claude/plugins/cache/*/mefisto/*/`, `sort -V`) e invoca `"$PLUGIN_SCRIPTS/<x>.sh"`. Los comandos internos `.claude/commands/mefisto-*` no se tocan: corren con `cwd = repo de Mefisto`, donde la ruta relativa es la correcta (ADR-0019).
- **Resolucion de referencias a ADRs contra la raiz del plugin en skills y agentes publicados** (issue #61, PR #63): `commands/implement.md` (fase 1.5, validacion de Definition of Ready) y los agentes `planner`, `reviewer` y `test-writer` referenciaban los ADRs del marco como `docs/adr/NNNN-*.md` relativos al cwd; con `cwd = consumidor` resolvian contra `<consumer>/docs/adr/` y el ADR parecia "ausente" (sintoma observado en campo: ADR-0011 / Definition of Ready ausente al correr `/implement`). Se reutiliza el mecanismo `.plugin-root` de #31: cada artefacto resuelve e imprime `PLUGIN_ROOT` antes de abrir el ADR y referencia `"$PLUGIN_ROOT/docs/adr/<archivo>.md"`. Cubre las 8 referencias en los 4 archivos (incluida la de `test-writer.md` a ADR-0002 que anadio #59). El lado interno `.claude/` no se modifica (ADR-0019).

## [0.4.1] - 2026-06-23

### Fixed

- **`tmux-pipeline.sh` (publicado) arranca el pipeline en el pane correcto con `pane-base-index 1`** (issue #56): las 6 funciones que abren panes (`cmd_single`, `cmd_batch`, `cmd_parallel`, `cmd_tooling`, `cmd_infra`, `cmd_scaffold`) ya no direccionan el pane del sub-script por indice implicito (`"$session:main.1"`) sino por su `pane_id` (`%N`), estable e independiente de `base-index`/`pane-base-index`. El tail captura su id con `tmux list-panes -F '#{pane_id}' | head -n1` y cada `split-window` devuelve el suyo con `-P -F '#{pane_id}'`. Con `setw -g pane-base-index 1` en `~/.tmux.conf` los panes se numeran 1 y 2 (no 0 y 1), por lo que `main.1` apuntaba al pane del `tail` (que ignora stdin) y el pipeline nunca arrancaba: no aparecia el status file y `/work-status` no mostraba progreso. `cmd_parallel` tenia la variante mas grave (todos los `send-keys` apuntaban a `"$session:main"`, el pane del tail, con cualquier `pane-base-index`); ahora cada issue recibe su sub-script en el `pane_id` que devuelve su propio `split-window`. Es la deuda de paridad de ADR-0019: el mismo patron `pane_id` se aplico solo al gemelo interno (`.claude/scripts/mefisto-tmux-pipeline.sh`, PR #12) y nunca se propago al lado publicado, que es el que consume el proyecto.

## [0.4.0] - 2026-06-23

### Added

- **Microconvencion de estilo "condiciones en positivo"** en `implementer.md`: prescribe preferir `if (existe)` sobre `if (!existe)`, ordenar las ramas `if`/`else` para que la guarda quede afirmativa, y documenta la excepcion de las guard clauses / early-return. El `reviewer` la verifica en fase refactor bajo el lente "Legible". Tambien se corrigio el ejemplo canonico de crear-o-actualizar que mostraba la guarda negada.
- **Recordatorio de CHANGELOG en `/mefisto-tooling`**: el pipeline interno verifica al final (warning, no gate) si el PR anadio contenido bajo `## [Unreleased]` en `CHANGELOG.md` y, de no ser asi, emite un recordatorio accionable. El PR se crea igual. Evita que los cambios lleguen a `main` con `[Unreleased]` vacio y rompan la fase *prepare* de `/mefisto-release` (issue #36).
- **ADR-0020** "Hosting de Azure Functions - un App Service Plan por dominio" (issue #44): fija la directiva canonica del marco de **un App Service Plan dedicado por Function App (dominio)**, no compartido. Documenta la restriccion dura de `DurabilityMode.Solo` (un solo nodo -> prohibido escalar horizontal -> solo eje vertical -> aislar por app), el sintoma noisy-neighbor de los agentes de durabilidad always-on, las proscripciones (no Consumption `Y1` con .NET 10+, no Wolverine `Serverless` mode), los defaults (`B1`, `worker_count = 1`, `always_on` OFF en dev / evaluar ON en prod) y el contrato del modulo `modules/service-plan` del consumidor. Se registra en el indice de `CLAUDE.md` (junto con ADR-0019, antes omitido) y se corrige el conteo de ADRs en `README.md` (18 -> 20).

### Changed

- **Scaffolder emite un App Service Plan dedicado por Function App** (issue #43): el agente `domain-scaffolder` (Paso 4) ahora genera un `module service_plan_<dominio>` propio por dominio (`source ../../modules/service-plan`, `name = asp-...-<dominio>`, `sku_name`, `worker_count = 1`, `os_type`, `always_on`) y enlaza la Function App a ese plan (`service_plan_id = module.service_plan_<dominio>.id`), en lugar de apuntar todas a un `module.service_plan` compartido (la causa del noisy-neighbor que origino el #43). El flujo de aprovisionamiento (Parametros de entrada y Paso 0) **pide/acepta los parametros de hosting** (SKU default `B1`, `always_on` OFF en dev) con override del usuario y los muestra en el resumen de confirmacion. Se anade un aviso si el modulo `modules/service-plan` del consumidor no acepta esos inputs (mismo patron que el aviso de `module.postgresql`). El `infra-writer` actualiza el naming de Service Plans a `asp-<proyecto>-<env>-<dominio>`, el `infra-reviewer` agrega al checklist de Arquitectura "cada Function App tiene su Service Plan dedicado" y lo refleja en el resumen del plan, y `/scaffold` incluye el Service Plan dedicado en el resumen de "lo que se va a crear". Alinea el harness con ADR-0020 (#44). Aplica a dominios nuevos; no migra infra existente.
- **Fase 5 de `/fix-review` (publicado) adaptada al modelo plugin** (issue #38): las mejoras a agentes/skills del harness ya no se editan en la rama del PR del consumidor (esos archivos viven read-only en el cache del plugin), sino que se enrutan como **draft** (`estado:borrador`) al repo de Mefisto via `gh -R`, reutilizando el routing cross-repo del `planner` y el `tooling-investigator`. La edicion en-rama queda reservada a lo que realmente vive en el consumidor (ADR local, convenciones de su `CLAUDE.md`). Se documenta la deteccion de modelo plugin y la field note sigue generandose en el consumidor.
- **`bug-investigator` detecta el noisy-neighbor y verifica el aislamiento por Service Plan** (issue #45): el paso de verificacion de infra del triage de deploy se **re-ancla a ADR-0020 del harness** como fuente de verdad del aislamiento por plan (antes apuntaba vagamente "al ADR de hosting del proyecto consumidor"; el consumidor puede tener un ADR local complementario que no la contradiga) e incorpora el chequeo de que cada Function App corre en su propio App Service Plan dedicado (`asp-<proyecto>-<env>-<dominio>`), con comando de verificacion (`az appservice plan show --query numberOfSites`) y su equivalente en Terraform (`service_plan_id` por dominio). Se anade la seccion **"Patron de diagnostico: noisy neighbor por plan compartido"** que ensena a reconocer la firma del sintoma (CPU del plan alta en reposo con 0 requests y 0 mensajes de Service Bus), su verificacion (`az monitor metrics list --metric CpuPercentage` cruzado con el trafico real), la causa raiz (`DurabilityMode.Solo` con su agente de durabilidad always-on) y el eje de mitigacion critico (solo vertical o aislar por app; **nunca** escalar out con `Solo`). Alinea el agente con ADR-0020 (#44); origen #43.
- **Sync verificado y fail-loud de `main` entre eslabones del secuencial interno** (issue #46): `mefisto-batch-pipeline.sh` reemplaza el viejo `git pull origin main || warn (continuando)` (best-effort, que silenciaba el fallo y dejaba que el siguiente eslabon partiera de un main potencialmente atrasado) por un sync **verificado** tras cada merge: `git fetch origin main`, fast-forward (`--ff-only`) de main local a `origin/main` y **confirmacion** de que el commit de merge del PR quedo presente en main local antes de arrancar el siguiente issue. Si el sync no se concreta y aun quedan eslabones, **aborta la cadena** en vez de construir el siguiente sobre un main desactualizado (degrada a warning solo en el ultimo eslabon, del que nada depende). Ademas el motor exige arrancar en `main`/`master` y aborta con mensaje claro si no, porque cada worktree del tooling-pipeline se crea desde la rama activa del repo. Se documenta en el header del script y en la seccion "Sincronizacion verificada entre eslabones" de `/mefisto-sequential`. Habilita cadenas con dependencias seguras (prerequisito de #47).
- **El secuencial interno admite cadenas cuyos bloqueos se resuelven por el orden del propio batch** (issue #47): `/mefisto-sequential` reescribe su paso 1.5 para **clasificar** cada dependencia abierta de un issue `bloqueado`, en vez de abortar ante cualquier `OPEN`: (a) *satisfactible por el batch* -- es otro issue del mismo batch que aparece **antes** en el orden, asi que la cadena `pipeline -> PR -> merge -> sync -> siguiente` (con el sync verificado de #46) la resuelve durante la ejecucion; o (b) *bloqueo real* -- esta fuera del batch y sigue `OPEN`, o esta dentro del batch pero **despues** en el orden (mal ordenada). Si todas son (a) -- o ya estan `CLOSED`/`MERGED` -- el batch se lanza y se **retira** el label `bloqueado` al validar (CA-5); si hay al menos un bloqueo (b), **aborta** mostrando cual y por que, y si es intra-batch mal ordenada sugiere el reordenamiento concreto (p. ej. "mueve #44 antes de #43"). La mutacion de labels es atomica: solo ocurre si todo el batch pasa la validacion. Habilita lanzar cadenas como `/mefisto-sequential 44 43 45` (donde #43 y #45 dependen de #44). Solo afecta al skill interno; la version publicada `commands/sequential.md` no tiene paso 1.5. Depende de #46.

## [0.3.0] - 2026-06-17

### Added

- **Set de skills, agentes y pipelines internos** para evolucionar el propio harness (no se publican vía marketplace; viven en `.claude/`): agentes `mefisto-planner` y `mefisto-investigator`, y comandos `/mefisto-plan`, `/mefisto-tooling`, `/mefisto-bug`, `/mefisto-fix-review`, `/mefisto-merge`, `/mefisto-work-status`, con sus pipelines `_mefisto-common.sh`, `mefisto-tmux-pipeline.sh` y `mefisto-tooling-pipeline.sh`.
- **Skill interno `/mefisto-release`** para versionar y publicar el plugin siguiendo SemVer y Keep a Changelog (fases prepare y publish).
- **Skill interno `/mefisto-sequential`** para procesar varios issues internos en cadena dentro de una sesión tmux, con `mefisto-batch-pipeline.sh`.
- **Routing de drafts cross-repo**: el `planner` y el `tooling-investigator` publicados pueden crear drafts (`estado:borrador`) en el repo de Mefisto cuando detectan que un problema del consumidor pertenece al harness.
- **Slug del repo de Mefisto configurable** en `validate_consumer_scope_changes`, en lugar de estar hardcoded.
- **Guards defensivos "cwd != Mefisto"** en todos los skills publicados, en los pipelines TDD/IaC/scaffold/tmux y en los scripts auxiliares (`appinsights-query`, `eda-lint`, `setup-github-*`), para que el tooling publicado nunca opere por error sobre el repo del propio plugin.
- **Scope-gate del `/tooling` publicado**, que lo restringe a archivos del consumidor y rechaza el PR si toca rutas reservadas al plugin.
- **ADR-0019** "Skills publicados vs internos", que formaliza la separación entre el paquete distribuido y el tooling interno.
- **Suite de tests de guards y scope** en `scripts/tests/test-guards.sh`.
- **LICENSE MIT** al publicar el repositorio.

### Changed

- **Documentado el modelo de skills publicados vs internos** en `CLAUDE.md` y `README.md`.
- **Regla de entrega**: todo cambio se hace en rama y se entrega vía Pull Request; nunca contra `main` directo (documentado en `CLAUDE.md`).
- **`eda-modeler`**: eliminados los dominios y contratos hardcoded de un consumidor concreto.
- **`tooling-investigator`**: aclarados sus límites y retirada la etiqueta `dom:tooling` (inexistente en Mefisto).
- **Eliminado el `git push` directo a `main`** en el agente `historiador` y en los skills `fix-review` / `mefisto-fix-review`; ahora pasan por rama + PR.
- **README**: añadida la sección "El nombre" con la cita de Fausto que origina el nombre interno `mefisto`.

### Fixed

- **Resolución del repo objetivo del consumidor** en `tmux-pipeline.sh` y `eda-lint.sh`: ya no se deriva de la ubicación del script (`$SCRIPT_DIR/..`, que bajo el modelo de plugin apunta al propio harness) sino del toplevel del repo activo vía `git rev-parse --show-toplevel`.
- **Referencias rotas** en los skills `draft`, `health-check` y `show-flow`, y en el ADR-0007.
- **Referencias a ADRs con numeración del consumidor** en los agentes publicados (`planner`, `reviewer`, `test-writer`) y en `implement`.
- **Resolución del pane del script en tmux** vía `pane_id` en lugar del índice de pane, frágil ante reordenamientos.

## [0.2.0] — 2026-05-15

### Changed (BREAKING)

- **Renombrar el plugin de `eda-evsourcing-azure-harness` a `mefisto`** para acortar el namespace de los skills. Los skills ahora son `/mefisto:implement`, `/mefisto:scaffold`, `/mefisto:infra`, etc. (antes `/eda-evsourcing-azure-harness:implement`, etc.).
- `.claude-plugin/plugin.json.name`: `eda-evsourcing-azure-harness` → `mefisto`.
- `.claude-plugin/marketplace.json.plugins[0].name`: `eda-evsourcing-azure-harness` → `mefisto`.

El nombre del marketplace (`augusto-romero-arango-harness`) y la URL del repositorio (`eda-evsourcing-azure-harness`) no cambian.

### Migración para consumidores

Quien tuviera v0.1.x instalado debe reinstalar:

```
/plugin uninstall eda-evsourcing-azure-harness@augusto-romero-arango-harness
/plugin marketplace update augusto-romero-arango-harness
/plugin install mefisto@augusto-romero-arango-harness
```

Y reemplazar referencias en `CLAUDE.md` del proyecto: `/eda-evsourcing-azure-harness:*` → `/mefisto:*`.

## [0.1.2] — 2026-05-15

### Fixed

- `hooks/hooks.json`: el contenido debía estar envuelto en `{ "hooks": {...} }` en lugar de tener los eventos como root. Sin este fix, `claude plugin doctor` reporta: `Hook load failed: expected record, received undefined at path "hooks"`.

## [0.1.1] — 2026-05-15

### Fixed

- `.claude-plugin/marketplace.json`: el `source` del plugin pasa de `"."` (no soportado, falla con "This plugin uses a source type your Claude Code version does not support") a `"./"`, formato esperado por el schema (pattern `^\.\/.*`). Sin este fix, `/plugin install` falla.

## [0.1.0] — 2026-05-15

### Added

- Extracción inicial del harness desde el repo `Bitakora.ControlAsistencia` como Claude Code Plugin independiente.
- **14 skills** (slash commands) en `commands/`: scaffold, implement, tooling, sequential, show-flow, draft, fix-review, infra, parallel, health-check, work-status, bug, merge, eraser-diagram.
- **16 agentes** en `agents/`: planner, test-writer, implementer, reviewer, smoke-test-writer, domain-scaffolder, eda-modeler, event-stormer, historiador, infra-writer, infra-reviewer, infra-applier, infra-bootstrap, pr-sync, bug-investigator, tooling-investigator.
- **Pipelines bash** en `scripts/`: tdd, tooling, iac, scaffold, parallel, batch, tmux, pr-sync, eda-lint, appinsights-query, setup-github-ci, setup-github-labels, _pipeline-common.
- **`scripts/_pipeline-common.sh`** con `load_harness_config()` que parsea `.claude/harness.config.json` del consumidor y exporta variables `HARNESS_*`.
- **18 ADRs** del marco en `docs/adr/`, renumerados secuencialmente:
  - ADR-0001 Service Bus topics por evento
  - ADR-0002 Estrategia de testing con event sourcing
  - ADR-0003 Event Sourcing con Marten y Wolverine
  - ADR-0004 Manejo de errores en ES
  - ADR-0005 Naming y versionado de eventos
  - ADR-0006 Convenciones de nombramiento de funciones Azure
  - ADR-0007 Gestión de proyecto con GitHub Issues
  - ADR-0008 Knowledge Crunching como propósito del planner
  - ADR-0009 Mensajes con .resx per-aggregate
  - ADR-0010 Pipeline de conocimiento del dominio
  - ADR-0011 Definition of Ready
  - ADR-0012 Estilo de modelado de objetos de dominio
  - ADR-0013 Smoke tests contra entorno dev
  - ADR-0014 Coverage gate en pipeline TDD
  - ADR-0015 Snapshots de Marten como excepción
  - ADR-0016 Convención de naming de tests
  - ADR-0017 Archivo señal de refactor fuera de `.claude/`
  - ADR-0018 Heurísticas de evolución y reuso
- **`hooks/hooks.json`** con PostToolUse para logging de archivos modificados, tests y operaciones Terraform.
- **`.claude-plugin/plugin.json`** y **`marketplace.json`** con metadata del plugin y catálogo del marketplace `augusto-romero-arango-harness`.
- **`CLAUDE.md`** y **`README.md`** documentando el harness, su contrato con el consumidor y la instalación.

### Contrato con el consumidor (nuevo)

- `.claude/harness.config.json` con campos: `projectName`, `namespacePrefix`, `solutionFile`, `infraResourceGroupPrefix`, `terraformStateStorage`, `githubServicePrincipalName`, `appInsightsApp`, `domainLabels`.
- Sección "Tokens del harness" en `CLAUDE.md` raíz del consumidor declarando `RootNamespace`, `SolutionFile`, `ProjectDisplayName`.

### Notas

- Algunos ADRs referencian ADRs del proyecto consumidor (Function App por dominio, Contracts, control de costos de App Insights, hosting de Azure Functions). Esas referencias quedan como "ADR del proyecto consumidor sobre X" hasta que cada proyecto las nombre.
- Los agentes `reviewer` e `implementer` mantienen el placeholder literal `ADR-XXXX` en sus plantillas de reporte (no es un bug; el agente lo sustituye en tiempo de ejecución por el número real del ADR aplicable).
- Los ejemplos de código en `test-writer.md`, `implementer.md` y `smoke-test-writer.md` conservan nombres concretos de un proyecto consumidor (`Programacion`, `ControlHoras`) anotados en el "Contrato con el consumidor" de cada agente como ejemplos pedagógicos.

[Unreleased]: https://github.com/augusto-romero-arango/eda-evsourcing-azure-harness/compare/v0.13.1...HEAD
[0.13.1]: https://github.com/augusto-romero-arango/eda-evsourcing-azure-harness/compare/v0.13.0...v0.13.1
[0.13.0]: https://github.com/augusto-romero-arango/eda-evsourcing-azure-harness/compare/v0.12.0...v0.13.0
[0.12.0]: https://github.com/augusto-romero-arango/eda-evsourcing-azure-harness/compare/v0.11.0...v0.12.0
[0.11.0]: https://github.com/augusto-romero-arango/eda-evsourcing-azure-harness/compare/v0.10.0...v0.11.0
[0.10.0]: https://github.com/augusto-romero-arango/eda-evsourcing-azure-harness/compare/v0.9.0...v0.10.0
[0.9.0]: https://github.com/augusto-romero-arango/eda-evsourcing-azure-harness/compare/v0.8.0...v0.9.0
[0.8.0]: https://github.com/augusto-romero-arango/eda-evsourcing-azure-harness/compare/v0.7.0...v0.8.0
[0.7.0]: https://github.com/augusto-romero-arango/eda-evsourcing-azure-harness/compare/v0.6.0...v0.7.0
[0.6.0]: https://github.com/augusto-romero-arango/eda-evsourcing-azure-harness/compare/v0.5.0...v0.6.0
[0.5.0]: https://github.com/augusto-romero-arango/eda-evsourcing-azure-harness/compare/v0.4.1...v0.5.0
[0.4.1]: https://github.com/augusto-romero-arango/eda-evsourcing-azure-harness/compare/v0.4.0...v0.4.1
[0.4.0]: https://github.com/augusto-romero-arango/eda-evsourcing-azure-harness/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/augusto-romero-arango/eda-evsourcing-azure-harness/compare/v0.2.0...v0.3.0
[0.1.0]: https://github.com/augusto-romero-arango/eda-evsourcing-azure-harness/releases/tag/v0.1.0
