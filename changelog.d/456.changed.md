- El modulo `container-app` que genera `infra-base-scaffolder` (Paso 1.9.3) cede a CI la propiedad de la imagen del worker de proyecciones: `azurerm_container_app.this` gana un bloque `lifecycle { ignore_changes = [template[0].container[0].image] }`, para que el `az containerapp update --image` del pipeline de imagen (fuera de alcance de este agente) no sea revertido por el siguiente `terraform plan`. `var.image`/`var.projections_worker_image` y su default placeholder (`mcr.microsoft.com/k8se/quickstart:latest`) no cambian -- solo alimentan el `create` del bootstrap. Se corrige, ademas, la prosa del modulo, la nota de placeholder y el reporte final (Paso 5) para dejar de instruir que hay que actualizar `projections_worker_image` para desplegar la imagen real.
