- El `planner` reconoce la señal de dato ajeno entre dominios (un dominio necesita un dato cuya verdad pertenece a otro y ningún front lo resuelve) y propone el enriquecimiento coreografiado por el dueño del dato (MEF-ADR-0046) como opción preferida frente a la réplica local, partiendo el trabajo en dos issues (dueño y consumidor) con sus `dom:` correctos.
