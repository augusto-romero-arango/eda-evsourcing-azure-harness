- Se alinea el trato de `pipeline-state/` con MEF-ADR-0017: el `.gitignore` del repo Mefisto y el bloque byte-fijo del `.gitignore` raiz que emite `infra-base-scaffolder` (Paso 2c) ahora lo listan, y el auto-commit de `tooling-pipeline.sh` deja de versionarlo (se conservan intactas las allowlists de escritura de MEF-ADR-0019:37).
