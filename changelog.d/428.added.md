- Se anade un hook `SessionStart` que anota `session_id`, `transcript_path`, `cwd`,
  `source` y un timestamp de cada sesion de `claude -p` en `.claude/pipeline/sessions.jsonl`
  del consumidor, para correlacionar cada stage de un pipeline publicado con su
  transcript completo (`~/.claude/projects/<slug>/<session_id>.jsonl`) sin instrumentar
  ningun call site existente.
