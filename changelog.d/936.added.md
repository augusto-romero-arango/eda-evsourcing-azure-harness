- Se anade `src/internal/scripts/mefisto-next-order.sh` (canonico, solo lectura), que calcula el orden topologico de lanzamiento de los issues `estado:listo` abiertos: extrae dependencias forward de `## Dependencias`, detecta ciclos y bloqueos externos abiertos, y cierra siempre con la linea `/mefisto-sequential <orden>` lista para copiar.
