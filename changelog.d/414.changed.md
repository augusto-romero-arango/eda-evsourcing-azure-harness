- `classify_file` del coverage gate (`scripts/tdd-pipeline.sh`, Stage 4) se alinea con MEF-ADR-0014/0034/0035: **(a)** `*Projection.cs` (la clase de proyeccion companion) ahora clasifica `logic` y exige el 95% de cobertura, sin gatear por `IS_PROJECTION` -- si tu BC ya adopta proyecciones (`projections.enabled`) y sus clases de proyeccion no tienen unit tests (`Create`/`Apply`/`ShouldDelete`, los escribe `projection-test-writer`), un pipeline TDD que antes pasaba ahora **fallara en Stage 4** hasta que completes esos tests en `tests/<RootNamespace>.Projections.Tests/{Dominio}/`; **(b)** la exclusion de records DTO reconoce el estilo canonico del marco (`public sealed record X(...)`, MEF-ADR-0035 seccion 2) -- ya no exige `public record` adyacente ni un limite de 3 lineas, asi que un read model multilinea que antes caia en `not_evaluated` (sin medirse, sin excluirse, en silencio) ahora clasifica `excluded` correctamente.
- Verificado por medicion real (no por inspeccion, CA-4 del issue): el `[GeneratedEvolver]` que emite `JasperFx.Events.SourceGenerator` para una clase de proyeccion `partial` aparece en el reporte Cobertura como una clase y un `filename` **distintos** de `{Concepto}Projection.cs` (basename generado del tipo `global__{Namespace}_{Concepto}Projection.Evolver.g.cs`), asi que `extract_file_coverage` (que empareja por basename) nunca lo atribuye al archivo escrito a mano -- no diluye su line-rate y no hizo falta ningun cambio en `extract_file_coverage`.
