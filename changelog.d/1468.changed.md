- `STAGE1_PROMPT` y `STAGE2_PROMPT` del pipeline interno de tooling (`mefisto-tooling-pipeline.sh`) incorporan un bloque `NEUTRALIDAD DE RUNTIME:` unico (definido una vez e interpolado en ambos) que explica, sin nombrar los literales que persigue, que R1-R3 del gate de neutralidad son grep sobre texto crudo -- mencionar un literal cuenta igual que usarlo, tambien para negarlo, y una sola fuga aborta la corrida entera al cierre del stage -- y remite a la cabecera del gate y a MEF-ADR-0050; el reviewer gana ademas la instruccion de reformular esos literales en su propia revision si aparecen en el diff, nunca reportarlos sin corregir ni proponer ampliar la allowlist.
- `src/internal/scripts/lib/mefisto-state.sh` deja de necesitar su excepcion nominal R3 en `neutrality-allowlist.json` (la entrada se retira): el comentario que negaba las variables de entorno de un runtime concreto se reformula citando MEF-ADR-0050 y la regla R3 del gate, y la ruta legada se arma una sola vez en `MEFISTO_LEGACY_STATE_REL` -- de la que ahora derivan tanto la resolucion inicial como `mefisto_state_read_paths`. El archivo queda bajo la vigilancia de R3 en vez de exonerado en bloque, que era lo que lo convertia en falso precedente para los writers.
