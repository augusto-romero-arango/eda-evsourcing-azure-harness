- `STAGE1_PROMPT` y `STAGE2_PROMPT` del pipeline interno de tooling (`mefisto-tooling-pipeline.sh`) incorporan un bloque `NEUTRALIDAD DE RUNTIME:` unico (interpolado en ambos) que explica, sin nombrar los literales que persigue, que R1-R3 del gate de neutralidad son grep sobre texto crudo -- mencionar un literal cuenta igual que usarlo, tambien para negarlo -- y remite a la cabecera del gate y a MEF-ADR-0050; el reviewer gana ademas la instruccion de reformular esos literales si aparecen en el diff, nunca reportarlos ni proponer ampliar la allowlist.
- El comentario de `src/internal/scripts/lib/mefisto-state.sh` que documentaba por que la resolucion de `MEFISTO_STATE_DIR`/`MEFISTO_LEGACY_STATE_DIR` evita `CLAUDE_PROJECT_DIR`/`CLAUDE_PLUGIN_ROOT` queda reformulado sin esos literales (cita MEF-ADR-0050 y la regla R3 del gate); la ruta legada se deriva ahora de una variable separada para el directorio oculto, sin repetir el patron `.claude/pipeline` en texto crudo, y la excepcion nominal de ese archivo desaparece de `neutrality-allowlist.json` (ya no la necesita).
