- Se anade `.claude/scripts/mefisto-stream-watch.sh`, un visor en vivo del flujo de acciones del agente durante una corrida del pipeline interno de tooling (issue #434). Resuelve el vacio del pane de tmux que hoy solo ve dos lineas por corrida completa (`=== STAGE 1 ===` / `=== STAGE 2 ===`) sin forma de notar que el agente esta dando vueltas: sigue incrementalmente `<log_base>.stream.jsonl` (la traza cruda que #431 ya deja creciendo en vivo en `.claude/pipeline/logs/`, sin `tail -f` -- lee con `sed -n` un contador de lineas consumidas cada ~1s, portable en bash 3.2/macOS) y renderiza una linea legible por accion del agente: hora, delta desde la accion anterior (el reloj que revela los round-trips de ~20s medidos en el diagnostico del issue), herramienta y objetivo -- ruta para `Read`/`Edit`/`Write`/`NotebookEdit`, comando para `Bash`, patron+ruta para `Grep`/`Glob`. Los comandos `Bash` multilinea se aplanan a una sola linea y se truncan al ancho del pane; los turnos de solo texto (sin tool call) tambien se señalan, y la repeticion sobre un mismo archivo dentro del stage se marca con un sufijo `(xN)` (la señal barata de "dando vueltas" -- no se detectan comandos `Bash` identicos, medido como rarisimo). Sin argumentos descubre por si solo el stream mas reciente en `.claude/pipeline/logs/` y cambia de stream automaticamente cuando aparece uno mas nuevo (stage 2, o el siguiente issue de un batch/secuencial), imprimiendo un encabezado con issue/stage/agente derivado del propio nombre del archivo; acepta opcionalmente una ruta de stream concreta para inspeccionar una corrida pasada. Al llegar el evento `result` imprime el cierre del stage (turnos, costo, duracion API vs no-API) y sigue esperando el siguiente stream sin terminar el proceso -- `Ctrl-C` lo corta limpiamente. Solo lectura y aislado: no modifica ningun pipeline ni archivo existente, no escribe en `.claude/pipeline/` (solo en un directorio temporal propio) y tolera una linea a mitad de escritura reintentandola en el siguiente ciclo en vez de descartarla o abortar. Test nuevo `.claude/scripts/tests/test-stream-watch.sh` con fixtures de stream (Bash multilinea, repeticion sobre un archivo, turno de solo texto, evento `result`, linea final truncada) que no invoca el CLI ni depende de una corrida real.
