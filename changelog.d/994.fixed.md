- `agents/test-writer.md` ya no asume `AcceptedResult` para todo endpoint HTTP de comando: lee el codigo de exito declarado en el contrato del issue (MEF-ADR-0043) y assertea el tipo exacto de `IActionResult` -- `CreatedResult` (mas `Location` cuando el contrato declara una URI canonica de lectura), `NoContentResult`, `OkObjectResult` con su `Value`, o `AcceptedResult` solo cuando el contrato trae la justificacion de procesamiento pendiente.
