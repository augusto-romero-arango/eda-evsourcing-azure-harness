- `implementer` resuelve `RootNamespace` con `{{mefisto:instructions-path}}`
  (canonico primero, fallback legacy y aborto con diagnostico de onboarding si
  falta el contrato del consumidor) en vez de leer exclusivamente el archivo
  legacy de instrucciones (MEF-ADR-0053 seccion 4). Al desaparecer su ultimo
  acoplamiento de runtime, `implementer` sale de la excepcion transitoria
  `legacy` del validador de artefactos publicados y vuelve a rechazar
  referencias a Claude/OpenCode en su cuerpo.
