- Se añade el skill publicado `/mefisto:upgrade` (`commands/upgrade.md` + `scripts/update-plugin.sh`), que reemplaza el flujo manual de 4 pasos para actualizar el plugin (UI de `/plugin`, reescribir `.claude/pipeline/.plugin-root` a mano, podar el cache de versiones viejas, `/reload-plugins`) por 1 comando + el reload final. Detecta el marketplace por glob sobre el cache (sin hardcodear su nombre, para que funcione en un fork instalado vía `repoSlug`), actualiza el catálogo y el plugin sin interacción, reescribe `.claude/pipeline/.plugin-root` a la versión más reciente, imprime el delta de `CHANGELOG.md` entre la versión cargada y la nueva, y poda el cache de versiones viejas bajo confirmación explícita del usuario -- nunca borra la versión cargada en la sesión activa ni la nueva.
