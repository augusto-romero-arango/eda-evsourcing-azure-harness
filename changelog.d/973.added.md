- `parallel-pipeline.sh` deja de lanzar issues nuevos de la cola mientras haya una espera (hold) activa por limite de uso o caida del proveedor, y refleja "en espera" con la causa y la proxima sonda en su dashboard en vez del stage congelado (issue #973).
- `batch-pipeline.sh` anota en su tracker cuando un eslabon esperó por limite de uso durante el pipeline, sin que eso cuente como fallo, dispare `--stop-on-error` ni cambie el exit code (issue #973).
- `/work-status` distingue avanzando / en espera / sin novedades desde hace X leyendo las lineas `[hold]` de `events.log` (issue #973).
