- El modulo `function-app` de `infra-base-scaffolder` (agents/infra-base-scaffolder.md, seccion 1.7) gana el output `default_hostname`, valor computado por Azure a usar en vez de concatenar `name + ".azurewebsites.net"` (se rompe con hostnames regionalizados).
