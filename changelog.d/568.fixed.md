- Se unifican por exclusion los tres filtros de deteccion de cambios de `scripts/tooling-pipeline.sh` (gate `HAS_UNSTAGED` del writer, `auto_commit_if_needed` y la heuristica de recuperacion de `run_agent`), que mantenian tres listas a mano de rutas (`tests/ src/ scripts/ .github/ infra/ .claude/`, en combinaciones distintas entre si) desincronizadas de la allowlist de escritura real del skill `/tooling` (que ademas permite `docs/adr/`, `docs/adr-proyecto/`, `docs/bitacora/`, `.claude/harness.config.json` y archivos de raiz como `CLAUDE.md`). Los tres puntos ahora usan el mismo patron de exclusion que ya usaba `scaffold-pipeline.sh` (`git status --porcelain -- . ':!.claude/pipeline'`): detectan todo menos el estado runtime que el propio pipeline escribe, en vez de listar a mano las rutas permitidas. `auto_commit_if_needed` ademas excluye `pipeline-state/` (MEF-ADR-0017) tanto en el chequeo como en el `git add`, para que ningun commit automatico incluya esa senal. Se corrige el bug real: un cambio puramente documental (p. ej. una enmienda de ADR en `docs/adr/`) caia fuera de las tres listas y el pipeline abortaba con "El writer no genero ningun cambio" pese a que el writer si habia producido el cambio correcto, dejandolo huerfano en el worktree. El gate de scope (`validate_consumer_scope_changes`) no cambia: sigue revisando el diff completo contra `is_path_in_consumer_blocklist`, que es donde vive la seguridad real de este pipeline.
