- Stage 2 (reviewer) de `mefisto-tooling-pipeline.sh` ya no incrusta el diff completo del writer en su prompt: en su lugar recibe el commit base, el HEAD del writer, `git diff --stat=120` y `git diff --name-status` sobre el mismo rango `$SNAPSHOT_COMMIT..HEAD`. El diff completo sigue disponible en el worktree (`git diff <base>..HEAD -- <ruta>` bajo demanda). En la corrida de #1407 el diff alcanzo 3.229.352 bytes y el prompt resultante (3.237.916 bytes) supero `ARG_MAX`, tumbando el reviewer antes de arrancar (#1447); ahora el tamano del prompt de Stage 2 deja de depender del tamano del diff.
