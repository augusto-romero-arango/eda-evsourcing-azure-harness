- `apim-gateway-scaffolder` provisiona las dos consecuencias del verbo `QUERY` (RFC 10008/MEF-ADR-0042) en el gateway APIM: la politica global (`<cors>` del modulo `api-management`) ahora enumera `<method>QUERY</method>` en `<allowed-methods>` (CA-1, enumeracion explicita, nunca `*`) y las operaciones wildcard del modulo `apim-function-api` suman `QUERY` a su default (CA-2, `operation_methods = ["GET", "POST", "QUERY"]`).
- Para gateways ya provisionados (`infra/modules/api-management/main.tf` existente), el agente detecta si `<allowed-methods>` todavia no incluye `QUERY` y reporta el fragmento XML exacto a aplicar a mano en el Paso 7, sin editar el archivo (CA-3, respeta la aditividad CA-6).
- El checklist post-deploy del reporte final suma el gate `QUERY` con token valido y `Content-Type: application/json` -> llega a la Function App (CA-4), referenciando el punto NO VERIFICADO "APIM Consumption reenviando QUERY end-to-end" de MEF-ADR-0042 seccion 6.
