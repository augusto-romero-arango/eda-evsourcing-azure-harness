- OpenCode usa defaults versionados por perfil (`fast` Luna, `balanced` Terra,
  `deep` Sol) en agentes, comandos y pipelines headless, sin impedir overrides
  mediante `--models` o `.mefisto/models.json`.
