- Se propaga MEF-ADR-0037 (identidad del stream) a la doctrina read-side: `skills/projections/read-apis.md` fija la firma del GET (identidad `Guid` -> `Guid.TryParse` + `400` explicito + `ToString()` sin argumentos antes de `LoadAsync`/`AggregateStreamAsync`/`FetchStreamAsync`; clave compuesta -> componentes tipados + `ComputarStreamId(...)`) y proscribe recibir la clave ya armada como `string` en la ruta; `naming.md` corrige la afirmacion de que el `id` de ruta nunca es una identidad `Guid`; `SKILL.md` resume la regla en Nivel 1-2; `agents/projection-implementer.md` corrige su ejemplo vigente de GET (que el propio MEF-ADR-0037 señalaba como desalineado) y agrega la prohibicion correspondiente.
