- Se retira la dependencia del MCP de Rider/JetBrains de `implementer`, `projection-implementer` y `reviewer`: ya no declaran `mcp__jetbrains__*` ni prescriben ese servidor como primera opcion. Navegacion y lectura usan `Glob`/`Grep`/`Read`, diagnostico usa `dotnet build`/`dotnet test`, formato usa `dotnet format`, y el renombrado de simbolos en `reviewer` se verifica con busqueda textual (`Grep`) + build/tests, conservando integro el guardrail de MEF-ADR-0036 sobre el renombrado de eventos persistidos.
