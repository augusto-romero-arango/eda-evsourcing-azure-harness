- `test-writer` resuelve `RootNamespace` con `{{mefisto:instructions-path}}`
  (canonico primero, fallback legacy y aborto con diagnostico de onboarding si
  falta el contrato del consumidor) en vez de leer exclusivamente el archivo
  legacy de instrucciones; `{Dominio}` sigue derivandose del issue o de la
  estructura `src/` (MEF-ADR-0053 seccion 4).
