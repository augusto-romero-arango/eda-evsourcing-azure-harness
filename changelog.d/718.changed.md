- Se cierra el gap de `mt_version` en la doctrina read-side: `skills/projections/read-apis.md` fija la receta canonica `Schema.For<TView>().UseNumericRevisions(true)` como condicion hermana de tenancy (citando la doc oficial de Marten para la premisa del default), y `skills/projections/config-test.md` fija el par de config-tests espejo (write-side <-> read-side) que la guarda siempre-activa en cada `dotnet test`. MEF-ADR-0034 seccion 6 registra `mt_version` como segunda instancia del par de compatibilidad 2, y `agents/reviewer.md` gana la fila y el mandato de correccion correspondientes en su tabla de verificacion.
