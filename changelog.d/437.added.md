- `infra-reviewer` gana un chequeo (Paso 2, MEF-ADR-0034 seccion 8) que detecta un entorno que instancia el worker de proyecciones (`container-app-environment`/`container-app`) sin `Microsoft.App` registrado en `resource_providers_to_register` del `provider "azurerm"`, evitando el `409 MissingSubscriptionRegistration` del primer `apply` en entornos donde el HCL ya existe.
