- El agente `historiador` deja de ser mono-dia: ahora escanea todas las field notes pendientes en `docs/bitacora/field-notes/*.md` (el filtro por fecha queda como parametro opcional), agrupa por dia y produce una entrada `docs/bitacora/YYYY-MM-DD.md` por cada dia con notas pendientes. Cada entrada acota su git log, sus issues y su tabla "Numeros del dia" al dia que le corresponde. El cierre atomico mueve a `procesadas/` solo las field notes efectivamente integradas (movimiento selectivo, no por glob) y produce un solo PR (rama `docs/bitacora-hasta-YYYY-MM-DD`) con todas las entradas nuevas; el agente reporta el numero de ese PR en su mensaje final.
