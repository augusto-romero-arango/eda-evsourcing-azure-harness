- El pipeline `tooling-pipeline.sh` (lado publicado) acepta el flag
  `--models 'agente=modelo[,agente=modelo...]'` para experimentos A/B de
  desempeno del harness: sobreescribe el modelo de un stage puntual
  (`writer`/`reviewer`) sin tocar el default del resto. Sin el flag, el
  comportamiento es byte a byte el actual. `_pipeline-common.sh` gana el
  parser compartido (`parse_stage_models`/`resolve_stage_model`), validado
  ANTES de crear el worktree; `tmux-pipeline.sh` lo reenvia intacto en el
  camino `--tooling` y lo rechaza con mensaje explicito en el resto de los
  modos, que todavia no lo implementan.
