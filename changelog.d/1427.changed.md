- `{{mefisto:config-path}}` deja de traducirse a la ruta canonica literal y
  pasa a resolver la ruta efectiva de lectura del config del consumidor en
  ambos adaptadores, con la misma precedencia, mensajes y politica de
  `resolve_harness_config_path read` (`scripts/_pipeline-common.sh`): canonico
  primero, fallback legacy de lectura solo si falta y aborto si no existe
  ninguno. Un artefacto que usa una o ambas directivas nuevas emite un unico
  bloque de inicializacion del contrato efectivo, aunque las use varias veces
  inline.
