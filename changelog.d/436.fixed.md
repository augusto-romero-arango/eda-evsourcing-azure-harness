- Se elimina la sustitucion textual de la lista de issues en el bloque bash de validacion 1.5 de `.claude/commands/mefisto-sequential.md`: una corrida real de la sustitucion manual (pedida por el skill para reemplazar `BATCH="44 43 45"`) alcanzo tambien el cuerpo de `pos_in_batch`, dejando `local target="427"` fijo en vez de `local target="$1"` -- toda llamada a la funcion buscaba el issue 427 sin importar el argumento, y la clasificacion de dependencias intra-batch (satisfactible vs mal ordenada vs bloqueo real) daba resultados falsos en silencio (sin exit code que lo delatara). Ahora la lista de issues entra como argumentos posicionales (`bash -s -- 44 43 45 <<'BASH'` con `BATCH="$*"` dentro): el heredoc queda citado y su cuerpo se copia verbatim, sin una sola linea editable dentro del bloque; lo unico que varia entre invocaciones es la lista de numeros despues de `--`, fuera del heredoc.
