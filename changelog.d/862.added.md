- El adaptador OpenCode (`src/internal/scripts/lib/adapter-opencode.sh`) ahora emite un bloque `permission` cerrado por defecto en cada agente generado, derivado de sus capacidades neutrales (`src/internal/contract/opencode-permissions.json`): las 17 claves del vocabulario de OpenCode 1.18.29 reciben siempre un valor explicito (`allow`/`ask`/`deny`), nunca heredan un default global -- requisito de `opencode run --auto`, que solo respeta `deny`.
