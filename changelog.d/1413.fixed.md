- Se corrige `test-tooling-agents.sh`, en rojo desde que `tdd-pipeline.sh` entro en `TOOLING_CLOSURE_ASSETS` (#1365/PR #1390) porque fijaba `37`/`15`/`16` como copias manuales del tamano de la clausura publicada. La prueba ahora deriva la clausura desde `TOOLING_CLOSURE_ASSETS` con el mismo patron de extraccion de `test-tdd-pipeline-closure.sh`, exige explicitamente `scripts/tdd-pipeline.sh` y `src/runtime/lib/mefisto-process.sh` en ambos runtimes, y contrasta cada entrada contra los inventarios Claude y OpenCode con source, destination, mode y SHA-256. El conteo agregado de archivos desaparece: cada distribucion se compara como conjunto contra su propio inventario, y lo no-clausura se afirma semanticamente (manifiesto en Claude; observabilidad, MCP y todos los archivos de `skills/` en OpenCode).
