- Registrar `.claude/settings.json` (entrada exacta) en la allowlist interna `is_path_in_mefisto_scope`, habilitando un futuro hook `PostToolUse` versionado para el gate de scope (issue #522, paso 2 del plan de velocidad del pipeline interno).
