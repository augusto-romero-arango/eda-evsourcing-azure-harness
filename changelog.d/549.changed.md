- MEF-ADR-0039 (composicion de ensamblados por rol del evento): la decision 2 pasa de un grafo encadenado (`PublicEvents <- PrivateEvents <- {Dominio}.DomainEvents <- Function App`) a **tres islas sin `ProjectReference` entre si** -- el Function App de cada dominio referencia los tres ensamblados de eventos directamente, el worker sigue referenciando unicamente `{Dominio}.DomainEvents` + `ReadModels`. La decision 6 se extiende a **payload por rol**: un tipo de payload no cruza ensamblados de eventos; cuando el mismo dato viaja por bus y se persiste, cada ensamblado declara su propio record plano, con todo el mapeo concentrado en el Function App. La decision 7 se ajusta para retirar la transitividad hacia `PublicEvents` que `PrivateEvents.Tests` ya no tiene bajo el grafo de islas.
- MEF-ADR-0039: se anade la seccion 10 (enforcement por tests de arquitectura), que fija dos prohibiciones mecanicamente verificables -- cero `ProjectReference` entre los tres ensamblados de eventos; el worker de proyecciones nunca referencia un Function App -- y exige que se hagan cumplir con tests de arquitectura en el repo consumidor.
