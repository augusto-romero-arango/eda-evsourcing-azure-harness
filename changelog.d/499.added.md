- Se agrega MEF-ADR-0037, que fija la doctrina de identidad de stream y su representacion string canonica: dos clases de identidad (nacida `Guid` / clave natural compuesta), el principio de punto unico de conversion por aggregate con el canonico "D" minusculas via `Guid.ToString()` sin argumentos, la politica de normalizacion por tipado en el borde HTTP, las tres superficies afectadas (store, bus, read-side) y el descarte de un helper de runtime a favor de doctrina + reviewer como guardrail real.
