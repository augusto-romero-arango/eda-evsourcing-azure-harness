- El pipeline interno (`mefisto-tooling-pipeline.sh`) ahora estampa `harness_version` y `harness_sha` en cada entrada de `pipeline-history.jsonl` (feliz y aborto), calculados una sola vez en el prologo sobre el repo principal. `_mefisto-common.sh` expone los helpers `get_harness_version` (version de `.claude-plugin/plugin.json`) y `get_harness_sha` (SHA corto de HEAD), ambos degradando a vacio sin abortar.
