- `agents/reviewer.md` anade el gate de identidad de stream de MEF-ADR-0037 (seccion 3, subseccion nueva tras "Compatibilidad de configuracion Marten"): flagea `ToString(<formato explicito>)`/`.ToUpper()`/`.ToLower()` sobre una identidad usada como stream key, `GroupId` o id de `LoadAsync` -- incluido dentro del propio `ComputarStreamId` --, la construccion manual (concatenacion/interpolacion) de una clave de stream fuera de ese metodo estatico del aggregate, y el GET de read-side que recibe la clave ya armada como `string` en la ruta en vez de un parametro `Guid` tipado o componentes tipados + `ComputarStreamId`. Los tres chequeos citan MEF-ADR-0037 como autoridad; se agrega tambien la fila correspondiente al checklist del reporte final (paso 8).
