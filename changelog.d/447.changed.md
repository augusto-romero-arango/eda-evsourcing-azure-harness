- `agents/reviewer.md` gana, en su seccion "Proyecciones y read-side", una verificacion de compatibilidad de configuracion Marten entre write-side y read-side: corre solo bajo gate (el diff toca la version de `Cosmos.EventSourcing.CritterStack`/`Cosmos.EventSourcing.Abstractions` o configuracion de Marten), decompila la version exacta del paquete con `ilspycmd` (mismo procedimiento que documenta `agents/bug-investigator.md`) y verifica los dos pares de compatibilidad -- eventos (write-side -> worker) y read models (worker -> query-side) -- contra la regla "debe coincidir lo que determina como se interpreta lo ya persistido; no debe coincidir lo que es propiedad del proceso", con dos tablas de clasificacion como ilustracion. Una divergencia se corrige en el read-side en el mismo PR; solo se escala como hallazgo si corregirla exige tocar el write-side.
- MEF-ADR-0034 seccion 6 deja de presentar la guarda de metadata del config-test como la doctrina completa de compatibilidad write-side/read-side. Decompilando `Cosmos.EventSourcing.CritterStack` 2.3.1 se verifico que el metodo que cada dominio invoca del lado write (`Commands.MartenEventStoreExtensions.AgregarConfiguracionMartenComandos`) fija diez atributos de Marten, no tres -- la guarda existente cubria solo un subconjunto. El ADR conserva esa guarda (barata, siempre-activa) pero fija la regla de corte, nombra los dos pares de compatibilidad y declara que la verificacion completa la hace el reviewer bajo gate. Nombra ademas, por primera vez en cualquier ADR, el par read models/query-side (`Policies.AllDocumentsAreMultiTenanted()` como su instancia conocida). Absorbe como casos de la misma familia la instancia de `StreamIdentity` (consumidor #253/PR #254) y la de serializacion (consumidor #238/#252), y supersede el issue #423.
- `skills/projections/config-test.md` y `skills/projections/SKILL.md` dejan de describir la guarda de metadata del config-test como la compatibilidad completa; remiten al criterio del reviewer.
- Barrido de referencias en `docs/adr/mef-adr-0011-definition-of-ready.md`, `agents/planner.md`, `agents/projection-test-writer.md`, `agents/projection-implementer.md`, `agents/domain-scaffolder.md` y `commands/scaffold-projections.md`: ninguna describe ya la compatibilidad Marten como cubierta por la replica de metadata.
