- El adaptador de runtime OpenCode calcula `estimated_cost_usd` por paso con el catálogo de tarifas Models.dev (tier por contexto, cache y razonamiento incluidos) en vez del `.part.cost` crudo, que bajo OAuth de ChatGPT queda fijo en cero; el catálogo se prepara una única vez por corrida para que la traducción en vivo y la final no diverjan.
