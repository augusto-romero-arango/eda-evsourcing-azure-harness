- Se corrige que una corrida de `mefisto-herdr-pipeline.sh` (interfaz herdr de los pipelines internos) pudiera robar o cerrar el pane del visor de una corrida de otro runtime: el pool de panes de reporte (`herdr-report-panes.txt`) ahora guarda el runtime junto al `pane_id` y la seleccion/poda solo consideran panes del mismo runtime que la corrida en curso (resuelto con `mefisto_resolve_runtime`, MEF-ADR-0049).
