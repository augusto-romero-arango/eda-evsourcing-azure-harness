- El contrato del runner neutral (`src/runtime/`) gana un canal de stdin no sujeto a `ARG_MAX`: `runtime_<id>_build_cmd` puede fijar `MEFISTO_RUNTIME_STDIN_FILE` (archivo regular que el watchdog conecta a la entrada estandar del proceso) y `mefisto-run-agent.sh` expone `MEFISTO_RUNTIME_WORK_DIR` antes de invocar `build_cmd` para que un adaptador materialice ahi ese archivo; el adaptador `fake` lo adopta (guion `dump-stdin`) para una regresion reproducible con un prompt mayor que `ARG_MAX` (incidente de #1407, reviewer de OpenCode 1.18.29 con un prompt de 3.237.916 bytes rechazado por el kernel al `exec`).
