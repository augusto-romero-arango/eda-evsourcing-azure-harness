- Se alinean `projection-test-writer`, `projection-implementer` y el Agent Skill `projections` a la composicion de ensamblados por rol del evento (MEF-ADR-0039): los tipos que tipan `Create`/`Apply` llegan unicamente via la `ProjectReference` del worker a `{Dominio}.DomainEvents`, nunca via una referencia a un Function App. El camino de backfill de `projection-test-writer` (dominio scaffoldeado antes de que el BC adoptara proyecciones) ahora asegura esa `ProjectReference` de forma idempotente antes de escribir el stub del seam. Se documenta ademas la regla de payload por rol en el read-side: el read model nunca embebe un tipo de evento.
