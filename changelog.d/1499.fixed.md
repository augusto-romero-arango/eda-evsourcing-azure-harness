- Se elimina la deteccion de colisiones cuadratica con `jq` en `generate-published-adapters.sh`: los tres bucles que lanzaban un proceso `jq` por cada plan de asset ya acumulado (colisiones de assets estaticos, colisiones de assets por adaptador y `is_supplemental_asset` en `--check`) ahora leen arrays bash paralelos (`ASSET_PLAN_ADAPTERS`, `ASSET_PLAN_IDS`, `ASSET_PLAN_DESTINATIONS`) sin invocar procesos externos; con ~174 assets, `--check` pasa de 98-109 s a un orden de magnitud menor y `--out` de 49 s a un tiempo comparable, sin cambiar el contrato ni la salida generada.
