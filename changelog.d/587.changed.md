- `MEF-ADR-0006` (convenciones de nombramiento de Functions Azure) enmendado: la seccion de Functions de query pasa a cubrir GET **y** QUERY; fija que `Listar{X}s` conserva nombre y ruta al cruzar de GET a QUERY (MEF-ADR-0042) -- solo cambia el verbo declarado en el `HttpTriggerAttribute`.
- `skills/projections/read-apis.md` suma la mecanica del metodo QUERY (RFC 10008): ejemplo canonico con `HttpTrigger(..., "query")`, parseo tipado del filtro, sesion acotada al tenant identica al GET, paginacion por cursor y mapeo de los codigos 400/415/422 del RFC a `ObjectResult` con mensaje.
- `skills/projections/naming.md` documenta que `Listar{X}s` conserva nombre y ruta sea GET o QUERY (MEF-ADR-0042) -- el verbo es lo unico que distingue.
