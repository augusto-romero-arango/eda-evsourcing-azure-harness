- `scripts/metrics-report.sh` agrega ahora una dimension `by_version` por
  pipeline: cada `harness_version` presente en `pipeline-history.jsonl` (issue
  #660) reporta su total de corridas, instrumentadas, y los mismos agregados
  de wallclock/turnos/costo (medias por corrida) que el resto del reporte,
  restringidos a las corridas de esa version — responde si una version nueva
  del plugin mejoro o empeoro las corridas. El historial previo a #660 (o
  cualquier linea sin el campo) cae bajo la clave `(sin version)`, mismo
  patron que `(sin-pipeline)`. El render textual agrega la seccion "POR
  VERSION DE HARNESS" con el mismo estilo de tabla que el resto del reporte.
