- `scripts/herdr-pipeline.sh` ya no reporta exito con solo que `herdr pane run` devuelva 0: `cmd_pane_runner` escribe un marcador de arranque antes de lanzar el sub-pipeline, `dispatch_to_pane`/`cmd_parallel` lo esperan hasta un timeout configurable (`HERDR_DISPATCH_CONFIRM_TIMEOUT`, default 15s) y, si no aparece, `dispatch_to_pane` reintenta una unica vez en un pane nuevo (nunca reescribe el pane sospechoso) antes de fallar visible nombrando ambos paneles.
