- Se corrige `agents/smoke-test-writer.md`: el agente ya no fija `HttpStatusCode.Accepted` como
  status del camino feliz; lo lee del contrato HTTP declarado en el issue (MEF-ADR-0011) y lo
  asierta tal cual, sin default asumido (MEF-ADR-0013, issue #992). Suma verificaciones especificas
  por codigo -- `Location` para `201`, body vacio para `204`, representacion declarada para `200` --
  y deja el ejemplo del dominio publicador con su `202` explicitamente justificado (procesamiento
  primario diferido) en vez de ensenarlo como respuesta universal. Aclara ademas que el polling del
  GET sobre una proyeccion `Async` nunca justifica que el smoke test del POST espere `202`.
