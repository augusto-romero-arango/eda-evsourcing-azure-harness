- Se audita sistematicamente toda expansion `"${arr[@]}"` de `scripts/*.sh` y `.claude/scripts/*.sh` frente a la trampa de `set -u` en bash 3.2 (macOS) que motivo #611: las ~36 expansiones reales fuera de la ya corregida por #611 ya contaban con guardia (chequeo de longitud previo, invariante de bucle, o array literal/llenado incondicionalmente no vacio), verificado por reproduccion con `/bin/bash -c 'set -euo pipefail; ...'` sobre cada patron de guardia presente en el codigo. No se detectaron expansiones desprotegidas adicionales; no fue necesario modificar codigo de los 10 scripts auditados.
