- `run_neutrality_gate` del pipeline interno de tooling (`mefisto-tooling-pipeline.sh`) ya no aborta de inmediato ante una fuga de neutralidad: registra el aviso en `events.log` y le da al mismo rol (writer o reviewer) EXACTAMENTE un intento de correccion -- un `run_agent "<stage>-fix" <rol> ...` con la salida cruda del gate mas el remedio por regla (`mefisto_neutrality_remedy`, issue #1469) y la prohibicion explicita de tocar `neutrality-allowlist.json` -- antes de repetir el gate de scope y el de neutralidad. Si el intento corrige la fuga, el stage continua igual que si nunca hubiera fallado; si persiste, aborta con el mismo mensaje de antes (violaciones + remedio + retoma `--from-stage N`), precedido de una frase que deja constancia de que el intento ya fallo. Las variables `LAST_AGENT_*` que Stage 1/2 leen tras el gate siguen reflejando la corrida principal del rol, no el intento de correccion.
