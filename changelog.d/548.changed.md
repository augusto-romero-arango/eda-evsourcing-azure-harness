- El Paso 3b de `domain-scaffolder` ya no emite el named store de proyecciones ciego: agrega la `ProjectReference` del worker hacia `{Dominio}.DomainEvents` (idempotente via `dotnet add reference`), verifica mecanicamente que ninguna `ProjectReference` resultante apunte al Function App del dominio (MEF-ADR-0039 decision 4) y registra `IdentidadEventos{Dominio}.TiposPersistidos` en el `EventGraph` del named store (`AddEventTypes`, MEF-ADR-0036) dentro del seam `ConfiguracionMartenProjections{Dominio}`.
