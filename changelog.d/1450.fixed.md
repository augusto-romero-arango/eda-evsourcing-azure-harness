- Stage 2 (reviewer) de `scripts/tooling-pipeline.sh` y Stage 3 (reviewer, rama no-refactor) de `scripts/tdd-pipeline.sh` ya no incrustan el diff completo del stage anterior en su prompt: en su lugar reciben el commit base, el HEAD del writer/test-writer, `git diff --stat=120` y `git diff --name-status` sobre el mismo rango `$SNAPSHOT_COMMIT..HEAD`. El diff completo sigue disponible en el worktree del consumidor (`git diff <base>..HEAD -- <ruta>` bajo demanda por archivo). Espejo publicado del issue interno #1449: un scaffold grande del writer o un test-writer que toque muchos archivos ya no arriesga un prompt de reviewer de megabytes que pueda superar `ARG_MAX` (ver #1407/#1447).
