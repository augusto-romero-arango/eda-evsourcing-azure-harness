- `domain-scaffolder` crea el ensamblado propio `<RootNamespace>.{Dominio}.DomainEvents` (`dotnet new classlib`, net10.0, sin ningun `PackageReference`) al dar de alta un dominio nuevo, y emite `IdentidadEventos{Dominio}.cs` ahi en vez de en `Infraestructura/` del Function App (MEF-ADR-0039, MEF-ADR-0036). Cambio aditivo: `Contracts` sigue intacto hasta el issue hermano de retiro.
