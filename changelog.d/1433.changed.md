- `projection-implementer` resuelve `RootNamespace` con `{{mefisto:instructions-path}}`
  (canonico primero, fallback legacy y aborto con diagnostico de onboarding si falta
  el contrato del consumidor) en vez de remitir de forma ambigua al contrato de
  `implementer`; `{Dominio}` se deriva del issue/estructura, sin depender de que otro
  cuerpo de agente sea interpretado primero (MEF-ADR-0053 seccion 4).
