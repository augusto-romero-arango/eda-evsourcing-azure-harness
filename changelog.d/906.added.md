- Nueva funcion `agent_events_error_kind` (`src/internal/scripts/lib/_mefisto-common.sh`): imprime el `error.kind` del evento terminal del JSONL neutral de un stage, unico lugar donde vive la lectura estructurada que antes hacia un grep de texto sobre el log derivado.
