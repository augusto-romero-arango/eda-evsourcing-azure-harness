- `domain-scaffolder` (etapa b de tenancy, `tenancy.strategy = "multi-tenant-header"`): ya no auto-cablea `Cosmos.MultiTenancy.CritterStack.AgregarTenantResolverHibrido()` (`ProxyTenantResolver`, probado roto en el worker aislado para HTTP, issue #802). Si `src/{RootNamespace}.TenantResolver/` ya existe (la crea `/install-apim`), referencia la biblioteca con `ProjectReference` + `services.AgregarTenantResolverAsyncLocal()` + `builder.UsarTenantContextMiddleware()`; si no existe todavia, degrada a "proponer" dejando el resolver transitorio de etapa (a) como placeholder registrado con un `// TODO(tenancy etapa b)` que apunta a `/install-apim`. El mismo TODO en el resolver de etapa (a) se actualiza para apuntar al patron nuevo (MEF-ADR-0028 seccion 3, enmendada por el issue #802).
