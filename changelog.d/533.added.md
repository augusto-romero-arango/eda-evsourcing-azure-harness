- Se agrega el skill interno `/mefisto-tooling-verbose`, que delega integramente en `/mefisto-tooling` y lanza el pipeline con el flag `--verbose` (#435) para abrir el visor en vivo en un tercer pane.
