- Se corrige el checklist post-deploy de `/install-apim`: deja de exigir `202 Accepted` fijo en el POST autenticado y en su lugar pide elegir un endpoint POST real y verificar el codigo de exito declarado en su contrato HTTP (`200`/`201`/`202`/`204`, MEF-ADR-0043). La evidencia de que APIM autentico y reenvio la request (App Insights) queda separada del status devuelto por el backend -- un 2xx aislado ya no se confunde con la prueba de forwarding (MEF-ADR-0032 B2). Las verificaciones de 401, headers de identidad, QUERY y operacion APIM faltante (B11) no cambian.
